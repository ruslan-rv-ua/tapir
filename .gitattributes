# Normalize line endings to LF for all text files.
# This overrides core.autocrlf and ensures consistent behavior
# across Windows and Unix contributors.
* text=auto eol=lf

# Windows-only scripts must use CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
