---
slug: transport-boundary-silent-in-background
title: "Межа списку у фоні не має носія — «наступний» на краю мовчить так само, як незареєстрована клавіша"
priority: P2
type: planned
status: draft
effort: S
kind: bug
target: 0.2.0
updated: 2026-09-01
a11y: true
depends_on: [transport-skip-silent-failure]
blocks: []
touches:
  - src/lib/transportControl.ts
  - src-tauri/src/tray/notify.rs
  - src-tauri/src/i18n.rs
  - src/i18n/messages/uk.json
  - src/i18n/messages/en.json
gates: [pnpm test, pnpm vite:build, cargo test]
notes:
  - "Знахідка grilling transport-skip-silent-failure (2026-09-01), §6 «контракт відгуку». Винесено окремо свідомо: інший клас події — «межа», а не «невдача»."
  - "Поверхня питанням не є: правило нового ADR («вухо, вікно, система») відповідає на неї однозначно — звук мовчить, отже вирішує фокус вікна, отже у фоні нативний тост. Питання рівно одне: чи взагалі."
  - "У вікні прогалини немає — кнопки на межі стають disabled, і це видимий носій."
---

# Межа списку у фоні не має носія — «наступний» на краю мовчить так само, як незареєстрована клавіша

> **Контекст:** знайдено під час grilling
> [transport-skip-silent-failure](p1-transport-skip-silent-failure.md) §6, винесено окремо.
> Поверхню обирати не треба — треба вирішити, чи потрібен відгук узагалі.

## Опис

`executeTransportSkip` виходить мовчки, коли дія не резолвиться
([transportControl.ts:50](../../src/lib/transportControl.ts:50), `action.kind === "none"`).

**У вікні прогалини немає:** на межі списку кнопки «попередній / наступний» стають `disabled`,
і це повноцінний видимий носій. **У фоні кнопок не видно**, а звук не змінюється — отже
натискання на краю списку не залишає жодного сліду.

Батьківський запис зробив цю тишу **значущою**: невдача тепер говорить, тож мовчання почало
означати «межа». Це чесний контракт, але він читається лише зсередини — людина, яка натиснула
клавішу з іншого застосунку, спостерігає той самий нуль, що й при клавіші, яку ОС не
зареєструвала (пор. [hotkey-registration-silent-at-startup](p1-hotkey-registration-silent-at-startup.md)).

Ускладнення: `none` — це **не один випадок, а три**
([playbackNeighbors.ts](../../src/stores/playbackNeighbors.ts) повертає `NONE`):

- край списку — далі йти нікуди;
- нічого не відтворюється — джерела немає взагалі;
- прев'ю станції з браузера — транспорт у ньому свідомо вимкнений.

Зараз усі три дають однакову тишу, і у вікні це нормально (стан видно). У фоні — ні.

## Питання, які запис має вирішити

- **Чи потрібен відгук узагалі.** Межа списку — нормальна подія, а не помилка, і ADR про
  видимий носій §1 каже, що відповіддю на дію є стан. Але у фоні стану не видно, і саме тому
  цей випадок опинився поза межею того ADR.
- **Один текст чи три.** Розрізняти край / нічого не грає / прев'ю — чи сказати одне
  «далі нічого немає»? Три тексти дорожчі за ключами й майже напевно зайві для прев'ю, де
  транспорт вимкнений задумом.
- **Що робити з утриманням клавіші.** На краю списку клавішу тримають так само, як усюди;
  сповіщення на кожен повтор перетворилось би на чергу плашок. Потрібен або дебаунс, або
  правило «одна репліка на серію» — прецедент є в
  [volume-hotkey-no-feedback](done/p1-volume-hotkey-no-feedback.md) §3—§4, але там серія
  живе в Rust, а тут рішення ухвалює вебв'ю.
- **Чи не поглинає це сусідній дефект.** Якщо межа заговорить, тиша знову стане
  неоднозначною — цього разу між «клавіша не зареєструвалась» і «нічого не сталося». Варто
  звірити з [hotkey-registration-silent-at-startup](p1-hotkey-registration-silent-at-startup.md),
  щоб два записи не лікували один симптом із різних боків.

## Критерії готовності

- [ ] Рішення «потрібен відгук / не потрібен» ухвалено явно й записано з причиною
- [ ] Якщо потрібен — поверхня взята з ADR «Три поверхні відповіді», а не обрана заново
- [ ] Вирішено, скільки текстів має три випадки `none`
- [ ] Утримання клавіші на краю не породжує черги сповіщень
- [ ] `docs/help/` оновлено — або зазначено, що запис видимої поведінки не змінює
- [ ] NVDA-прогін — якщо відгук додається
- [ ] `pnpm test`, `pnpm vite:build`, `cargo test` зелені

## Свідомо не робимо

- **У вікні нічого не міняємо** — там носій є (кнопки на межі `disabled`), і додавати до
  нього репліку означало б показувати той самий стан двічі.

## Документи

- [transport-skip-silent-failure](p1-transport-skip-silent-failure.md) §6 — звідки знахідка; там же контракт чотирьох мовчазних виходів
- [ADR 2026-08-31 — видимий носій](../decisions/2026-08-31-visible-carrier-for-announced-facts.md) §1, §3 — чому цей випадок опинився поза межею
- [volume-hotkey-no-feedback](done/p1-volume-hotkey-no-feedback.md) §3—§4 — прецедент «одна репліка на серію натискань»
- [hotkey-registration-silent-at-startup](p1-hotkey-registration-silent-at-startup.md) — сусідній симптом тієї самої тиші
- Код: `src/lib/transportControl.ts`, `src/stores/playbackNeighbors.ts`
