---
slug: help-content-polish
title: "Вбудована довідка (F1): специфікація покриття, стилю й розбиття"
priority: P1
type: planned
status: done
effort: L
kind: chore
target: 0.1.0
updated: 2026-08-09
completed: 2026-08-08
a11y: false
depends_on: []
blocks:
  - help-intro
  - help-recording
  - help-listening
  - help-automation
  - help-config
  - help-troubleshooting
touches:
  - docs/help/uk/
  - docs/help/en/
  - src/components/common/HelpDialog.tsx
  - src/i18n/messages/uk.json
  - src/i18n/messages/en.json
gates: []
notes:
  - "Парасолька: власного коду не має. Текст пишуть шість дочірніх записів; цей — специфікація, на яку вони посилаються."
  - "Попередня редакція вимагала «текст для сліпого користувача» — скасовано, див. «Принцип»."
  - "2026-08-09 (grilling help-intro): карту покриття звірено з кодом; 8 тверджень виправлено. navigation.md — F5/Shift+F5 більше не інертні (це «копіювати/перенести в профіль» на Потоках, streams-transfer-hotkeys); рядок стану має 3 сегменти, не 5; Ctrl+K шукає дії й потоки, не станції та пісні; F2 і Ctrl+C працюють лише на Потоках і винесені в streams.md; Space не наскрізний перемикач, Delete не діє в Браузері; модифікатори подано правилом, бо жоден із трьох не наскрізний; рядок про інертні F3/F7/F11 знято."
  - "2026-08-09 (там же): overview.md — з портативності прибрано «реєстр» (увімкнений автозапуск пише HKCU\\…\\Run, нюанс переїхав у background.md); мітку кнопки виправлено на дослівну «Додати приклади потоків»; рядок сумісності формулюється «працює з», ніколи «перевірено з» (проганявся лише NVDA). Стиль-гайд, правило 6 — navigation додано третім винятком (600 слів). status: done не змінювався: це виправлення карти, не перевідкриття запису."
  - "2026-08-13 (grilling help-recording): виправлено 6 позицій мапи, з них 3 — розбіжність із реалізацією, а не формулювання. recording.md — перепідключення за замовчуванням ВИМКНЕНЕ (max_retries: 0 = жодної спроби), поріг вільного місця не зупиняє запис, а не дає його почати; streams.md — кнопка зветься «Зупинити запис» і перейменовується за виділенням, поля пошуку на екрані немає взагалі; wishlist.md — додано вхід із меню потоку. Спільна причина всіх трьох: мапу писали з наміру, а не з коду, тож у дочірніх записах критерій «звірено з кодом» поширено з чисел на поведінку. status: done не змінювався."
  - "2026-08-13 (там же): два рішення сесії наскрізні, тож живуть тут, а не в дочірньому записі — правило 6 стиль-гайду тепер міряє обсяг по англійському файлу, а конвенція розмітки 2 забороняє посилатись словами на вкладку, якої на момент злиття ще не існує (потрібне з неї описується по суті на місці). Обидва обов'язкові для чотирьох записів, що лишились."
  - "2026-08-13 (grilling help-listening): виправлено 15 позицій мапи в player/songs/browser; числами були лише три (крок гучності 5%, поріг «попередній» 0 = вимкнено, крок перемотування 5 с), решта — поведінка. Найважливіші: панель виводу пристрій лише ПОКАЗУЄ (вибір і оновлення списку — налаштування → Аудіо); «наступний» ходить по ВІДФІЛЬТРОВАНОМУ списку відповідного екрана й ніколи не змішує ефір із записами; «продовжувати з позиції» діє лише на холодний старт, не на паузу; позначка «неповний трек» береться з суфікса `_incomplete` в імені файлу, тож перейменування її знімає; у популярних станцій довантаження немає взагалі. Двічі повторився один патерн — одиничний і масовий шлях поводяться по-різному (видалення записів: помилка проти тихого пропуску; додавання станцій: «вже додано» проти двох цифр). status: done не змінювався."
  - "2026-08-13 (там же): значок «Запис» у плеєрі означає ФАЙЛ, а не процес, чим порушує розведення термінів у CONTEXT.md; лагодити мовчки в довідці заборонено конвенцією розмітки 5, тож заведено окремий запис player-recording-badge-term (P2). Довідка описує мітку як є."
  - "2026-08-13 (grilling help-automation): виправлено/додано 9 позицій у wishlist і 11 у scheduling — числом була одна (стелі запасу 30/60), решта поведінка. Найважливіші: патерн ЯКІРНИЙ (звіряється весь рядок «Виконавець - Назва», `Tycho` не ловить `Tycho - Dive`); вішліст НЕ фільтрує — збіг лише повідомляє; ігнорований трек не стає файлом, але в суцільний файл ефіру пише далі; ігнор-лист ПОТОКУ з інтерфейсу не заповнити взагалі, тож порядок правил зведено з чотирьох кроків до двох; планувальник працює лише поки Tapir запущений і лише для АКТИВНОГО профілю; «потік уже записувався» фіксується раз на вікно й блокує старт навіть після зупинки ручного запису; правка або вимкнення пункту під час запису його зупиняє."
  - "2026-08-13 (там же): два наскрізні рішення. Правило 6 отримало четвертий виняток — scheduling до 600 (єдиний екран, де застосунок діє без користувача, тож «чому не сталося» мусить стояти там, де планують, а не в troubleshooting, на який ще й посилатись не можна за конвенцією 2). І другий патерн, повторений з help-listening: підпис вужчий за поведінку — заведено wishlist-pattern-hint-anchoring (підказка патерна мовчить про якірність) і tray-notifications-label-scope (прапорець «Сповіщення при зміні треку» гейтить і сповіщення розкладу). Обидва — P2, довідка описує поля як є."
  - "2026-08-13 (grilling help-config): правило 6 переписано — одна стеля 200–1000 слів по англійському файлу для всіх 14 розділів, дворівнева система й обидва винятки (navigation, scheduling) зняті, формулювання «стеля, а не ціль». Зміна НЕ ретроактивна: десять написаних розділів не перевідкриваються, а єдине місце, де стара стеля змінила зміст (відсутній список восьми текстів результату в scheduling.md), лишається як є. Причина: дворівневість була раціонуванням дефіцитного місця, а при 1000 дефіциту немає — лишались два обґрунтування, які нічого не вирішують, зате їх перечитують на кожному записі."
  - "2026-08-14 (grilling help-troubleshooting): мапу troubleshooting звірено з кодом І З УСІМА 13 НАПИСАНИМИ РОЗДІЛАМИ — головна знахідка сесії саме друге. Сім із дванадцяти позицій виявились уже вичерпно поясненими у своїх розділах, тож розділ отримав жанр «відповідь без механіки»: причина + дія на місці, механіка лишається у власника. Додано дві позиції, яких мапа не знала: звук зник після від'єднання пристрою (запам'ятовується за назвою, фолбеку на системний немає — engine.rs:946) і треків збереглося менше, ніж було в ефірі (перший неповний + коротші за 30 с; recording.md називає обидва з боку фічі, не з боку симптому). Форма — заголовок ### на симптом, 13 штук, порядок за стадією, на якій симптом помічають."
  - "2026-08-14 (там же): скрінрідери втратили власний заголовок — одне речення всередині ескалаційної позиції («не озвучується = дефект Tapir, а не ваші налаштування»); окремий заголовок обіцяв би рецепт, а всі можливі рецепти є обходами дефектів. Критерій переформульовано з «рівно двічі» на «рівно два МІСЦЯ»: рядок сумісності називає трьох читачів в одному реченні, тож дослівний підрахунок входжень хибний. Анти-заглушковий тест замінено тестом меж обсягу (120 обидві локалі / 1000 en) — нижня межа строго перекриває чорний список фраз і не конфліктує з обов'язковим «поки що недоступно» зі стиль-гайду."
  - "2026-08-14 (там же): NVDA-прогін скасовано розробником — a11y всіх шістьох записів тепер false, чекліст не створюється. Дві знахідки заведено записами: unknown-format-falls-back-to-mp3 (невідомий content-type мовчки стає Mp3, файл дістає чуже розширення) і about-app-info (застосунок ніде не показує ні версії, ні адреси проєкту — а довідка веде користувача саме до звіту про проблему). ADR не заводився: усі рішення сесії редакційні й відкочуються правкою тексту."
  - "2026-08-16 (grilling help-muted-playback-symptom): мапа troubleshooting отримала 14-ту позицію («Відтворення йде, а звуку немає»), лічильник 13 → 14, решта позицій перенумерована. Головна знахідка сесії — про поверхні, а не про текст: F9 і Ctrl+M віддають відповідь ВИКЛЮЧНО в sr-only-регіон (LiveAnnouncer.tsx:56), тостa немає. Отже порада «натисніть F9» є текстом, який має сенс лише для користувача скрінрідера, тобто прямим порушенням принципу універсального дизайну — і будь-яка майбутня позиція, що спирається на клавішу-відповідь, мусить називати видиму поверхню поруч. Заведено sound-hotkeys-feedback-announce-only (P2)."
  - "2026-08-16 (там же): позиція вводить у розділ конвенцію, якої в ньому не було, — передачу читача сусідній позиції. Дозволено рівно там, де заголовок-симптом стоїть перед заголовками-причинами того ж симптому (трійка «що чути»); як загальне правило розділу НЕ поширювати. Друга знахідка — нульова гучність дає ту саму тишу, але глобальною клавішею, липкіше (переживає перезапуск) і без жодної згадки в F9; лікується не текстом, а зведенням станів у коді: zero-volume-reads-as-muted (P2) + ADR 2026-08-16-silence-is-mute-or-zero-volume."
  - "2026-08-13 (там же): наскрізний шов між розділами — settings.md відповідає на «ДЕ це вмикається», background.md на «ЩО станеться», profiles.md на «ЯК дійти»; у кожної позиції рівно один власник пояснення, мітка може прозвучати двічі, пояснення — ніколи. Правило народилося з того, що п'ять фонових важелів фізично лежать у діалозі програми, і при новій стелі обидва розділи вміщали б повний опис того самого. Поширено й на пару profiles.md ↔ settings.md."
  - "2026-08-13 (там же): виправлено/додано 9 позицій у profiles, 8 у settings і 13 у background. Найважливіші: перемикача профілю в лівій панелі НЕ ІСНУЄ (там кнопка переходу на екран); активний профіль не перейменувати й не видалити — обмеження важче за Default, а в мапі його не було; перемикання профілю зупиняє й відтворення, але питає лише про запис; імпорт із наявним іменем не перезаписує й не додає суфікса; обидва діалоги налаштувань мають бокову смугу вкладок (3 і 4), а не плаский список; закриття вікна за типових налаштувань НЕ виходить із програми, тому підтвердження виходу під час запису на ньому не з'являється взагалі; переміщений tapir.exe автозапуск не лагодить, а вимикає; другий запуск виконує передані аргументи, але --profile і --minimize у форварді ігнорує."
  - "2026-08-13 (там же): увесь фоновий шар (меню трею, підказка значка, діалог підтвердження виходу, шість тостів) зашитий у Rust українською — i18n там не використовується. Наслідок для довідки: фоновий шар описується ЗА ДІЄЮ, без жирних міток, симетрично в обох локалях. Українською це збігається з екраном дослівно (мітки — прості дієслова), а текст переживе локалізацію. Дефект заведено окремо: tray-layer-not-localized (P1)."
  - "2026-08-13 (там же): п'ять нових записів-знахідок — tray-layer-not-localized (P1), hotkey-registration-silent-at-startup (P2), key-recorder-silent-rejection (P2), settings-labels-wording (P2), profile-delete-key-dead-end (P2). Повторився патерн попередніх сесій: підпис або поверхня вужчі за поведінку. ADR не заводився — межу налаштувань уже пояснює decisions/2026-08-08-global-vs-profile-settings-boundary.md, решта рішень редакційні. Заразом CONTEXT.md: «Глобальні налаштування» отримали користувацький термін «налаштування програми», і розведено три «авто» (автозапуск / автовідтворення при запуску / автоперехід)."
---

# Вбудована довідка (F1): специфікація покриття, стилю й розбиття

> **Контекст:** запис перероблено 2026-08-08 із «дописати 5 заглушок» на **специфікацію**
> всієї вбудованої довідки. Роботи всередині нього немає: він фіксує принцип, структуру,
> стиль, конвенції розмітки й мапу «фіча → файл», а текст пишуть шість дочірніх записів
> (`help-intro` … `help-troubleshooting`). Читати першим — «Принцип» і «Мапа покриття».

## Чому запис переписано

Попередня редакція ставила задачу як «замінити 5 заглушок реальним контентом» і описувала
цільову аудиторію словами «короткий практичний текст **для сліпого користувача**». Обидва
формулювання хибні:

1. **Обсяг.** Сім вкладок не покривають застосунок навіть близько: поза довідкою лишалися
   Потоки, Браузер станцій, Записи, плеєр, налаштування, трей, автозапуск, глобальні
   хоткеї, відновлення після збою, зони `F6`, палітра `Ctrl+K` і питання «де лежать файли».
2. **Аудиторія.** Tapir — звичайний застосунок для всіх, а не застосунок для незрячих.
   Текст, написаний «для сліпого користувача», автоматично отримує інтонацію спецрежиму
   й описує те, чого зрячий читач не бачить (що саме промовляє скрінрідер).

## Принцип: універсальний дизайн

> Довідка пишеться за принципом універсального дизайну: Tapir — звичайний застосунок для
> всіх, а не застосунок для незрячих. Клавіатурний шлях документується як **рівноправний**,
> а не як «режим доступності»; мишачий — так само. Тексту, який має сенс лише для
> користувача скрінрідера, у довідці немає.

Практичні наслідки, обов'язкові для всіх шістьох дочірніх записів:

- Дію описуємо **від наміру**, а не від пристрою вводу: «Додайте потік: кнопка
  **Додати потік** на панелі дій або `Ctrl+N`». Обидва шляхи в одному реченні, коли обидва
  існують. Не «клацніть мишею по…», не «перейдіть `F6` до списку, стрілками станьте на
  рядок і натисніть `Enter`».
- Зворотний зв'язок описуємо **нейтрально щодо модальності**: не «NVDA скаже „Запис
  розпочато"», а «Tapir повідомить, що запис розпочато» — однаково правдиво для
  повідомлення на екрані, для озвучення й для зміни статусу в рядку.
- Слово «NVDA» (як і «скрінрідер») з'являється на всю довідку **рівно двічі**: рядок
  сумісності в `overview.md` і, за потреби, один абзац у `troubleshooting.md`. У решті
  дванадцяти розділів його немає.
- Розділ `navigation.md` називається «Навігація і клавіатура», **не** «Доступність»:
  зони `F6`, стрілки в списках, `Ctrl+Space`/`Ctrl+A`, клавіша контекстного меню корисні
  будь-якому клавіатурному користувачу.

## Структура довідки

15 вкладок = 14 markdown-файлів + генерована вкладка «Гарячі клавіші» (`ShortcutsHelp.tsx`).

| # | Файл | Вкладка uk | Вкладка en |
|---|------|-----------|-----------|
| 1 | `overview.md` | Огляд і перші кроки | Overview & first steps |
| 2 | `navigation.md` | Навігація і клавіатура | Getting around |
| 3 | — (генерується) | Гарячі клавіші | Keyboard shortcuts |
| 4 | `profiles.md` | Профілі | Profiles |
| 5 | `streams.md` | Потоки | Streams |
| 6 | `browser.md` | Браузер станцій | Station Browser |
| 7 | `wishlist.md` | Вішліст | Wishlist |
| 8 | `scheduling.md` | Розклад | Schedule |
| 9 | `songs.md` | Записи | Recordings |
| 10 | `recording.md` | Як працює запис | How recording works |
| 11 | `player.md` | Прослуховування | Listening |
| 12 | `templates.md` | Файли й імена | Files & names |
| 13 | `settings.md` | Налаштування | Settings |
| 14 | `background.md` | Робота у фоні | Running in the background |
| 15 | `troubleshooting.md` | Якщо щось не працює | Troubleshooting |

Три рішення, вшиті в таблицю:

1. **Назви шести «екранних» вкладок дослівно дорівнюють міткам ActivityBar**
   (`streams_section`, `browser_section`, `wishlist_section`, `schedule_section`,
   `songs_section`, `profiles_section` у `src/i18n/messages/`), і йдуть у **тому ж порядку**,
   що в `src/lib/sections.ts` — тобто Профілі попереду Потоків, як у самому барі. Ціна:
   новачок дійде до «Профілів» раніше; виграш — розділ шукається тією ж моделлю, що й екран.
2. **`recording.md` у вкладці зветься «Як працює запис»** — щоб не сусідити із «Записами»
   (`songs_section`). «Запис» і «Записи» поруч у списку вкладок нерозрізнювані на слух.
   Ім'я файлу лишається `recording.md`, ключ `help_section_recording` лишається.
3. **`templates.md` лишається під тим самим іменем**, але розширюється з «синтаксис
   шаблонів» до «куди зберігаються файли + як вони називаються» (мітка «Файли й імена»).

## Межа: що входить у довідку, а що ні

`AGENTS.md` розводить README (кінцевий користувач) і DEVELOPERS.md (технічні деталі).
Довідка F1 — **четверта аудиторія**: людина, яка зараз усередині застосунку. Прикордонні
випадки вирішено так:

| Тема | Вердикт | Чому |
|---|---|---|
| Синтаксис патернів (`*`, `?`, регістр, порядок правил) | **входить** (`wishlist.md`) | без нього вішліст непридатний; збіг із DEVELOPERS.md — не дублювання, а різні аудиторії |
| Налаштування логів (рівень, макс. розмір, де лежить `tapir.log`) | **входить** (`troubleshooting.md`) | важіль стоїть у UI, і це перше, що просять при скарзі |
| Вкладка «Постобробка» (вимкнена заглушка) | **входить**, один рядок (`settings.md`) | вкладка фокусована й доступна; мовчання довідки читається як поломка |
| CLI-аргументи | **входить** підрозділом «Запуск з параметрами» (`background.md`) | сценарій «автозапуск у конкретному профілі, згорнутим» з UI недосяжний узагалі — це єдина документація фічі для того, хто нею користуватиметься |
| Внутрішні файли (`data/state.json`, `.tapirprofile`, `clean_shutdown`, протоколи) | **не входить** | входить лише *поведінка*: «після аварійного завершення записи піднімаються самі, Tapir про це повідомляє» |

## Стиль-гайд

Вісім правил, обов'язкових для кожного розділу:

1. **Друга особа й дія:** «Натисніть `Ctrl+N`, введіть адресу…». Не «користувач може»,
   не «ви можете» — одразу дієслово.
2. **Розділ відповідає на «як мені зробити X»**, а не описує поля по черзі. Перелік
   налаштувань доречний лише там, де налаштування і є темою (`settings.md`).
3. **Заборонена лексика реалізації:** IPC, стор, бекенд, компонент, подія, назви модулів.
   «ICY-метадані» вводяться один раз у `recording.md` як **«дані, які станція передає в
   ефір разом зі звуком»**, далі всюди — **«метадані ефіру»** (термін закріплено в
   [CONTEXT.md](../../../CONTEXT.md)).
4. **Дію описуємо від наміру, зворотний зв'язок — нейтрально** (див. «Принцип»).
5. **Кожен розділ відкривається одним реченням «навіщо це»**, далі — кроки.
6. **Обсяг 200–1000 слів** на розділ, міряний по **англійському** файлу (українська на
   тому самому змісті виходить коротшою, тож стелю тримає саме `en`). **Це стеля, а не
   ціль:** розділ пишеться настільки коротко, наскільки дозволяє зміст, і довший за 600
   слів мусить мати причину в змісті, а не в старанності. Тисяча — межа, за якою розділ
   треба **розщеплювати**, а не привід дописувати.

   Стелю піднято 2026-08-13 (grilling `help-config`) з дворівневої системи «база 400,
   виняток 600». Дворівневість була механізмом **раціонування** дефіцитного місця; коли
   дефіциту немає, вона лише тримає живими два обґрунтування (`navigation` везе наскрізний
   канон, `scheduling` — єдиний екран, де застосунок діє без користувача), які вже нічого
   не вирішують, зате їх доводиться перечитувати на кожному записі. Обидва зняті разом із
   рівнями.

   **Зміна не ретроактивна.** Десять розділів, написаних під стару стелю, не
   перевідкриваються: у них не бракує позицій мапи, вони лише щільно написані, а стеля,
   що не є ціллю, не є й приводом. Єдине відоме місце, де стара стеля змінила **зміст**, —
   рішення не робити в `scheduling.md` списку восьми текстів результату («місце потрібне
   під механіку»); лишається як є.
7. **Дефолти й числа називати прямо** («за замовчуванням `Ctrl+Shift+R`»), звіряючи
   **з кодом**, а не з документацією: користувач не бачить значення, поки не дійде до
   налаштувань.
8. **Ні маркетингу, ні вибачень:** не «потужний застосунок», не «на жаль, поки не
   підтримується» — просто «поки що недоступно».

## Конвенції розмітки

Пайплайн: `docs/help/<locale>/<section>.md` → `build/markdownHelpPlugin.ts`
(remark-parse + remark-gfm + rehype-sanitize) → готовий HTML у бандлі →
`HelpContent.tsx` → CSS-клас `.help-content` (`src/styles.css:35`).

1. **Таблиць немає.** `remark-gfm` їх розпарсить, але в `.help-content` правил для
   `table`/`th`/`td` немає — вийде злиплий текст. Замість таблиці — список визначень:
   `` - `%a` — виконавець ``.
2. **Посилань немає — жодних.** Обробника кліків у застосунку немає, а
   `default-src 'self'` верхньорівневу навігацію не стримує: клік по `http`-посиланню
   вивів би webview із застосунку без дороги назад — той самий клас аварії, від якого
   стоїть `useWebviewGuard`. Перехресні згадки — словами: «див. розділ „Профілі"».

   **Посилатися словами можна лише на вкладку, що вже існує** на момент злиття свого
   запису — включно з тими, чий текст ще заглушка (вкладка фокусується, обіцянка не
   бреше). Те, що потрібне з іще-неіснуючого розділу, описується **по суті на місці**:
   не «поріг вільного місця — див. „Налаштування"», а «поріг задається в налаштуваннях
   профілю, вкладка **Запис**». Правило додано 2026-08-13 (grilling `help-recording`)
   і обов'язкове для чотирьох записів, що лишились: інакше кожен наступний змушував би
   перечитувати попередні на предмет «а тепер уже можна посилатись» — ревізія, якої
   ніхто не запланував і яку ніщо не нагадає.
3. **Заголовки — тільки `##` і `###`.** `#` заборонено (стереже `helpContent.test.ts`:
   `<h1>` зламав би шкалу діалогу). Кожен файл починається з `##`, що дослівно дорівнює
   назві своєї вкладки.
4. **Клавіші, шляхи, змінні шаблонів — у backtick'ах** (`` `Ctrl+K` ``, `` `recordings\` ``,
   `` `%a` ``): `code` — єдиний оформлений інлайновий стиль.
5. **Жирний — лише для дослівних міток інтерфейсу** при першій згадці («кнопка
   **Додати потік**»), і мітка мусить збігатися з рядком у `src/i18n/messages/uk.json`.
   Курсиву не використовуємо. Якщо в довідці написано «Додати станцію», а на кнопці
   «Додати потік» — користувач шукає кнопку, якої немає.

## Наскрізні механіки: канон + дельта

`F6`-зони, стрілки в списку, `Enter`/`Space`, `Shift+F10`, `Ctrl+Space`/`Ctrl+A`/`Shift+↑↓`,
`Escape`, `Delete` з підтвердженням, `F2`, `Ctrl+C`, `Ctrl+K` однакові на всіх шести екранах.

- **Повне пояснення — один раз, у `navigation.md`.**
- **Кожен екранний розділ закінчується блоком «Клавіші на цьому екрані»** на 3–6 пунктів,
  куди входять **лише** дії, що тут значать своє: `Enter` (Потоки — запис або відтворення
  за налаштуванням «дія подвійного кліку»; Браузер — додати; Записи — відтворити; Профілі —
  перемкнути; Вішліст — редагувати), модифікатори (`Shift+Enter` слухати, `Ctrl+Enter`
  записати або показати в Провіднику, `Alt+Enter` відкрити у зовнішній програмі), масова
  дія саме цього екрана (у Браузері це «Додати виділені», а не `Delete`), `Ctrl+N`.
- Плюс одне речення-відсилання: «Загальні правила навігації — у розділі „Навігація і
  клавіатура"».

Джерело правди по клавішах — `docs/keyboard-shortcuts.md` і `src/lib/shortcuts.ts`,
але **звіряти з кодом**: у документі вже є дрейф (перемикач системних медіа-кнопок
стоїть у вкладці **Аудіо**, а не «Гарячі клавіші», як там написано).

## Мапа покриття «фіча → файл»

Нижче — що саме зобов'язаний згадати кожен файл. Перелік не є планом абзаців: це
контрольний список, за яким приймається дочірній запис. Порожньої позиції лишати не можна;
якщо позиція виявилась неактуальною — правити тут, у парасольці, а не мовчки в тексті.

### `overview.md` — Огляд і перші кроки

- Що робить Tapir одним абзацом: слухає інтернет-радіо, пише ефір, ріже його на окремі
  треки за метаданими ефіру й проставляє теги.
- Портативність: жодного встановлення, `data\` і `recordings\` з'являються поруч із
  `tapir.exe` ([portable.rs](../../../src-tauri/src/portable.rs)), AppData не задіяно.
  **Слова «реєстр» тут немає** (виправлено 2026-08-09): увімкнений автозапуск пише
  `HKCU\…\Run`, тож обіцянка чистого реєстру була б неправдою — нюанс живе в
  `background.md`.
- Шість екранів по одному-два речення кожен, у порядку ActivityBar.
- Перший запуск: додати потік уручну або через Браузер станцій; кнопка **Додати приклади
  потоків** — дослівно, це `streams_empty_add_examples` / "Add example streams".
  Кнопка вішліста «Додати приклад» (`wishlist_add_example`) — інший екран, в `overview`
  не згадується.
- `F1` — довідка, `Ctrl+K` — палітра команд, `Alt`+цифра — перехід між екранами.
- Один рядок сумісності: Tapir **працює з** NVDA, JAWS і Narrator (перша з двох згадок на
  всю довідку). Формулювання «перевірено з» / «протестовано з» — **заборонене**:
  проганявся лише NVDA, і твердження про перевірку було б хибним. «Працює з» — правда про
  архітектуру (стандартні ARIA/UIA, які читають усі три) і збігається з
  [README](../../../README.md) та [accessibility.md](../../accessibility.md).

### `navigation.md` — Навігація і клавіатура

> **Звірено з кодом 2026-08-09.** Попередня редакція цього переліку розійшлася з
> реалізацією у восьми твердженнях — усі виправлені нижче. Перелік читати як є;
> повторно звіряти не треба, але й покладатись на пам'ять про «як було» — теж.

- Вікно поділене на зони; `F6` / `Shift+F6` — перехід між ними вперед і назад.
- Зони **не перелічувати поіменно**: їх близько двох десятків, і склад залежить від
  відкритого екрана. Назвати лише три постійні — **Бокова панель**, **Програвач**,
  **Статус** — і сказати, що решта залежить від екрана, а Tapir називає зону при переході
  в неї. Формулювати нейтрально щодо модальності: «Tapir називає зону», **не** «NVDA
  оголосить назву».
- Усередині зони — стрілки, `Home`/`End`; `Tab` виводить із зони.
- `Enter` — головна дія рядка. `Space` здебільшого робить те саме; справжнім перемикачем
  він є лише там, де в рядка є стан «увімкнено/вимкнено» (Розклад), а в Браузері станцій
  не діє взагалі ([StationList.tsx](../../../src/components/browser/StationList.tsx)
  відсіює все, крім `primary`).
- Модифікатори — **правилом, не таблицею**: діють лише з `Enter`, завжди на сфокусований
  рядок (на відміну від `Delete`, що бере все виділення), а конкретне значення залежить
  від екрана й перелічене в його блоці «Клавіші на цьому екрані». Наскрізного значення не
  має жоден із трьох: `Shift+Enter` живе на Потоках і в Браузері, `Alt+Enter` — лише на
  Записах, а `Ctrl+Enter` означає різне (Потоки — записати, Записи — показати
  в Провіднику).
- Меню рядка: правий клік, клавіша меню або `Shift+F10`. **Поза списками контекстного
  меню в застосунку немає взагалі** — це свідомо, натискання просто нічого не робить.
- Виділення кількох рядків: `Ctrl+Space` (перемкнути), `Ctrl+A` (усі видимі),
  `Shift+↑`/`Shift+↓` (діапазон), `Escape` (зняти).
- `Delete` — видалити рядок або все виділене, завжди з підтвердженням; у Браузері станцій
  видаляти нічого, тому там він не діє.
- `Ctrl+K` — палітра команд: **дії й потоки** одним пошуком. Станцій каталогу й
  збережених пісень вона поки що не шукає
  ([CommandPalette.tsx](../../../src/components/common/CommandPalette.tsx) будує список зі
  статичних дій і потоків профілю).
- `Alt+0`…`Alt+5` — екрани; `F1` — довідка; `Ctrl+,` — налаштування програми;
  `Ctrl+Shift+,` — налаштування профілю.
- Рядок стану внизу — **три** сегменти: скільки записується, вільне місце, найдовший
  активний запис ([StatusBar.tsx](../../../src/components/layout/StatusBar.tsx)).
  Кількості потоків у профілі й кількості помилок там немає.

**Чого в цьому розділі свідомо немає:**

- **`F2`, `F4` і `Ctrl+C`.** `useCompositeList` синтезує їх у всіх композитних списках,
  але гілки мають не всі (уточнено 2026-08-14, `hotkeys-expansion`): `F2` — Потоки
  ([StreamList.tsx](../../../src/components/streams/StreamList.tsx)) **і Записи**
  ([SongsList.tsx](../../../src/components/songs/SongsList.tsx) → перейменування), `F4` —
  лише Записи (теги), `Ctrl+C` — лише Потоки; на Профілях, Розкладі, Патернах і в
  Браузері всі три мовчки нічого не роблять. Двох екранів із шести на канон не досить,
  тож висновок той самий, лише підстава оновлена: це дії конкретних екранів, і живуть
  вони в `streams.md` («Редагування (`F2`)», «Копіювання адреси») та `songs.md`
  («Перейменування, теги й видалення» + блок «Клавіші на цьому екрані»).
- **Рядка про інертні `F3`/`F7`/`F11`.** Пункт про контекстне меню лишається — клавішу
  меню користувач тисне з наміром, і тиша читається як поломка. `F7` з наміром не тисне
  ніхто, а чесна відповідь на «чому саме ці три» — акселератори WebView2 — заборонена
  правилом 3 стиль-гайду.
- **`F5`/`Shift+F5` як «інертних».** Вони більше не інертні: це «Копіювати / Перенести
  в профіль» на Потоках (`streams-transfer-hotkeys`, прийнято 2026-08-09) — і вони вже
  в карті `streams.md`.

### `profiles.md` — Профілі

> **Звірено з кодом 2026-08-13** (grilling `help-config`). Дев'ять позицій виправлено або
> додано — зокрема «перемикач профілю в лівій панелі», якого не існує. Перелік читати як є.

- Що таке профіль: іменований набір потоків, вішліст, ігнор-лист, розклад і налаштування
  запису, відтворення й інтерфейсу. Активний профіль один.
- Перемикання профілю **зупиняє і запис, і відтворення, але питає лише про запис**
  (виправлено 2026-08-13): підтвердження з'являється за активних записів — окремим
  текстом, якщо серед них плановий, — а відтворення глушиться без питання
  ([ProfilesPanel.tsx:104](../../../src/components/profile/ProfilesPanel.tsx:104),
  [profile_commands.rs:193](../../../src-tauri/src/commands/profile_commands.rs:193)).
  Назвати обидва боки одним реченням: інакше тиша на відтворенні читається як збій.
- Розклад іншого профілю не працює — одним підрядним реченням із відсиланням словами на
  розділ «Розклад» (вкладка вже існує); механіку вдруге не пояснювати.
- Створити, дублювати, перейменувати, видалити, експортувати — кнопками просто в рядку.
  Заборони **вплітати в кроки**, а не зводити в окремий регламент (рішення 2026-08-13,
  стиль-гайд правило 2):
  - `Default` не видалити й **не перейменувати** (додано 2026-08-13,
    [profile.rs:648](../../../src-tauri/src/profile.rs:648));
  - **активний профіль так само не перейменувати й не видалити** (додано 2026-08-13,
    [profile_commands.rs:37](../../../src-tauri/src/commands/profile_commands.rs:37);
    масове видалення його мовчки пропускає, повідомляючи «активний профіль пропущено»).
    Подавати **позитивно й дією**: «щоб перейменувати або видалити профіль, спершу
    перемкніться на інший».
- Назва стає ім'ям файлу: перелічити **заборонені символи** `\ / : * ? " < > |` і межу
  довжини; **зарезервовані імена Windows не перелічувати** (рішення 2026-08-13 — профілів
  на ім'я `LPT1` не буває, а якщо трапиться, Tapir скаже сам). Назвати профіль «Default»
  не можна в жодному регістрі ([profile.rs:494](../../../src-tauri/src/profile.rs:494)).
- Експорт і імпорт файлу профілю; **розклад до нього входить**. **Імпорт із наявним іменем
  не перезаписує й не додає суфікса** (додано 2026-08-13): операція просто не відбувається,
  поки не ввести іншу назву, — а мовчазна невдача читається як поломка. Про зрізання
  паролів потоку **не писати**: полів для них в інтерфейсі немає взагалі (див. «Межа»).
- **У лівій панелі перемикача профілю немає** (виправлено 2026-08-13): там кнопка
  «Управління профілями — {активний}», яка лише переводить на екран «Профілі»
  ([ActivityBar.tsx:91](../../../src/components/layout/ActivityBar.tsx:91)). Повне
  керування — на самому екрані (`Alt+0`, звірено з `sections.ts`).
- Налаштування можна відкрити для **будь-якого** профілю, зокрема неактивного, але єдиний
  вхід веде через **меню рядка** (кнопка ⋯ або `Shift+F10`): інлайн-кнопки немає, а
  `Ctrl+Shift+,` завжди націлений на активний і при вже відкритому діалозі його
  **закриває**, а не перенацілює (уточнено 2026-08-13).
- Автовідтворення при старті вмикається окремо для кожного профілю. Сам перемикач і його
  дефолт називає `player.md` (див. нижче) — тут лише «окремо для кожного профілю».
- Блок «Клавіші на цьому екрані» — **три** пункти: `Enter` (перемкнутися), `Ctrl+N` (новий
  профіль), меню рядка (`Shift+F10` або ⋯) як єдині двері до налаштувань чужого профілю.
  `Delete`, `Ctrl+A`, `Ctrl+Space` роблять канонічне й не входять; `F2` тут інертний і
  **не згадується** — «Навігація» його не обіцяла.

### `streams.md` — Потоки

- Додати потік (`Ctrl+N` або кнопка): адреса, назва; адресою може бути й плейлист
  `.pls`/`.m3u` — Tapir дістане з нього справжню адресу.
- Перед додаванням Tapir перевіряє, чи потік відповідає; якщо ні — можна додати попри це.
- Попередження про дубль адреси і про збіг назв; пропозиція взяти офіційну назву станції.
- Редагування (`F2`): назва й адреса; під час запису адресу правити не можна.
- Видалення — одного або всіх виділених, із підтвердженням.
- Фільтр (усі / записуються / з помилками) і сортування (за назвою, за датою додавання).
  **Поля пошуку на екрані немає** (виправлено 2026-08-13): у `StreamsPanel` немає ні поля,
  ні `Ctrl+F` у `SHORTCUTS`, а ключі `streams_search_label` і `zone_streams_toolbar` —
  мертві. Потреба «знайти потік за назвою» закривається палітрою `Ctrl+K`.
- Імпорт і експорт списку потоків у `M3U8` / `PLS`; при імпорті кожен потік перевіряється,
  дублікати позначаються й не додаються.
- Копіювання й перенесення потоків в інший профіль — для рядка під фокусом і для
  виділених; потік, що пишеться, не переноситься.
- Копіювання адреси, «Відкрити у медіаплеєрі».
- Масовий запис на панелі дій. Кнопки **перейменовуються за виділенням** (виправлено
  2026-08-13): без виділення — **Записати все** / **Зупинити запис**, з виділенням —
  **Записати виділені (N)** / **Зупинити виділені (N)**. Мітки «Зупинити все» в
  `uk.json` не існує ([StreamsPanel.tsx:590](../../../src/components/streams/StreamsPanel.tsx:590)).
- Блок «Клавіші на цьому екрані».

### `browser.md` — Браузер станцій

- Що це: пошук по спільнотному каталозі радіостанцій, щоб не шукати адресу вручну.
- Пошук за назвою; фільтри — країна, мова, кодек, мінімальний бітрейт.
- **Два різні очищення** (розведено 2026-08-13): `Escape` або хрестик у полі пошуку
  прибирає **лише текст** — країна, мова, кодек і бітрейт лишаються, і список далі
  відфільтрований ([SearchForm.tsx:69](../../../src/components/browser/SearchForm.tsx:69)).
  Щоб зняти все й повернутися до популярних — окрема кнопка **Скинути фільтри**, і вона
  з'являється, лише поки якийсь фільтр активний.
- Популярні станції як стартовий список — **фіксована порція, довантаження немає**
  (виправлено 2026-08-13: [BrowserPanel.tsx:159](../../../src/components/browser/BrowserPanel.tsx:159)
  вимикає `hasMore` поза пошуком, а `loadPopularStations` не має `offset`).
- **Результати пошуку** довантажуються порціями, поки каталог їх віддає (кнопка є, лише
  поки остання порція прийшла повною — [browser.ts:42](../../../src/stores/browser.ts:42)).
- Прев'ю: послухати станцію, не додаючи її.
- **Додати одну станцію** в активний профіль: якщо ця адреса вже у профілі, Tapir скаже
  про це й другої не додасть; рядок при цьому показує стан «додано»
  ([StationList.tsx:59](../../../src/components/browser/StationList.tsx:59)).
- **Додати виділені** (розведено 2026-08-13): дублікати відкидаються — і ті, що вже у
  профілі, і **повтори всередині самої вибірки** (у каталозі та сама станція трапляється
  кількома записами — [browser_commands.rs:277](../../../src-tauri/src/commands/browser_commands.rs:277));
  Tapir називає дві цифри — скільки додано і скільки пропущено.
- Після додавання станція перевіряється у фоні; про невдачу буде повідомлення.
- **Позначка «Недоступна» на рядку** (додано 2026-08-13,
  [StationItem.tsx:141](../../../src/components/browser/StationItem.tsx:141)): або так
  каже сам каталог за своєю останньою перевіркою (`lastcheckok === 0` — дані зовнішні й
  можуть застаріти, станція може працювати), або тут і зараз не вдалося прев'ю. Друга
  тримається лише до оновлення списку. Позначена станція додається як будь-яка інша.
- Каталог зовнішній і буває недоступний — це не поломка Tapir.
- Блок «Клавіші на цьому екрані».

### `wishlist.md` — Вішліст

> **Звірено з кодом 2026-08-13** (grilling `help-automation`). Дев'ять позицій нижче
> виправлено або додано — зокрема `F2` (редагування патерна насправді на `Enter`/`Space`:
> `F2` синтезується в усіх композитних списках, але гілку має лише `StreamList`).
> Перелік читати як є.

- Навіщо: вішліст повідомляє, коли в ефірі з'явився бажаний трек; ігнор-лист не дає
  зберігати непотрібні (реклама, джингли).
- **Вішліст нічого не фільтрує** (додано 2026-08-13) — сказати прямо, одним реченням
  в абзаці «навіщо». Збіг лише повідомляє: трек ріжеться й зберігається так само, як
  будь-який інший ([manager.rs:975](../../../src-tauri/src/stream/manager.rs:975) веде
  в ту саму гілку спліттера, що й `Normal`). Назва обіцяє «записувати тільки це», і без
  прямого заперечення читач саме так її й прочитає. Фільтри колись планувались — сліди
  цього видно в мертвих полях `minBitrate` / `format` / `removeAfterRecord`; сама фіча
  живе окремим записом [wishlist-conditions](../p2-wishlist-conditions.md).
- **Патерн якірний** (виправлено 2026-08-13) — головне правило розділу, і без нього
  вішліст мовчить. Звіряється **весь** рядок, не підрядок:
  [matcher.rs:16](../../../src-tauri/src/wishlist/matcher.rs:16) — `Tycho` не ловить
  `Tycho - Dive`, треба `Tycho*` або `*Tycho*`. Далі вже `*` — будь-яка кількість
  символів, `?` — рівно один, регістр не важить. Приклади списком (не таблицею),
  з наростанням: точний рядок → `*слово*` → `Виконавець*` → `?`; перший приклад брати
  дослівно з набору кнопки (нижче). Підказка в діалозі патерна про якірність мовчить —
  окремий запис [wishlist-pattern-hint-anchoring](../p1-wishlist-pattern-hint-anchoring.md),
  довідка описує поле як є.
- **З чим звіряється патерн** (додано 2026-08-13): з рядком «Виконавець - Назва»,
  зібраним із метаданих ефіру ([matcher.rs:48](../../../src-tauri/src/wishlist/matcher.rs:48));
  якщо станція передає лише одне поле — з ним самим. Ні ім'я файлу, ні назва станції, ні
  жанр не беруть участі. Півречення про станцію без метаданих ефіру: на ній не працює ні
  вішліст, ні ігнор-лист (`TrackAction::Normal` без перевірки).
- Що відбувається при збігу: з вішлістом — Tapir повідомить; з ігнор-листом — трек не
  стає окремим файлом, **але із суцільного файлу ефіру не зникає** (додано 2026-08-13:
  [recorder.rs:50](../../../src-tauri/src/stream/recorder.rs:50) пише в `stream_file`
  незалежно від `track_file`). Це єдине місце довідки, де сказано «не зберігається», тож
  застереження обов'язкове.
- Порядок дії правил — **два кроки**: ігнор-лист сильніший за вішліст, далі звичайна
  поведінка (виправлено 2026-08-13). Попередні чотири кроки називали ігнор-лист **потоку**,
  якого з інтерфейсу не заповнити взагалі: усі команди `add/remove_from_ignorelist`
  адресують профільний список, `StreamInfo.ignorelist` редагується лише руками у файлі
  профілю — а внутрішні файли довідка не описує (див. «Межа»). Редактор для нього — запис
  [per-stream-ignorelist-ui](../p1-per-stream-ignorelist-ui.md); поки його немає, крок не
  документується.
- Дві вкладки екрана звуться **«Бажані треки»** і **«Ігноровані треки»**; у прозі —
  цими словами. Слова «ігнор-лист» в інтерфейсі немає, «Вішліст» — лише мітка розділу
  (терміни закріплено в [CONTEXT.md](../../../CONTEXT.md)).
- Додати (`Ctrl+N` — **в активну вкладку**), редагувати (`Enter` або `Space` на рядку,
  [PatternList.tsx:121](../../../src/components/wishlist/PatternList.tsx:121)), видалити
  (`Delete`, зокрема виділені).
- Кнопка **Додати приклад** у порожньому списку додає **набір**, а не один патерн, і свій
  для кожної вкладки (уточнено 2026-08-13): бажані — `*новин*`, `*news*`; ігноровані —
  `*реклама*`, `*джингл*`, `*advert*`, `*jingle*`, `*promo*`. Набір однаковий в обох
  локалях свідомо — метадані диктує станція, а не мова інтерфейсу; це сказати. Повторне
  натискання нічого не дублює. Мітку в однині цитувати як є.
- Додати поточний трек до вішліста або ігнор-листа просто з **меню рядка на екрані
  «Потоки»** (додано 2026-08-13): пункти **Додати до бажаних** / **Додати до ігнорованих**
  є, лише поки Tapir знає, що зараз в ефірі
  ([StreamContextMenu.tsx:135](../../../src/components/streams/StreamContextMenu.tsx:135)).
  Обидва кладуть патерн у **профільний** список.
- Блок «Клавіші на цьому екрані» — три пункти: `Enter` (редагувати), `Ctrl+N` (додати в
  активну вкладку), `←`/`→` (перемикання вкладок; вкладки живуть в одній зоні з кнопками,
  і це єдиний клавіатурний шлях до другої). `Delete` **не входить** — він робить рівно
  канонічне, як і на Потоках.

### `scheduling.md` — Розклад

> **Звірено з кодом 2026-08-13** (grilling `help-automation`). Розділ виявився найбільшим
> недооціненим у мапі: виправлено або додано одинадцять позицій, і йому одному піднято
> стелю обсягу (правило 6). Перелік читати як є.

- Навіщо: записати передачу, не сидячи біля комп'ютера.
- Додати запис (`Ctrl+N`): назва, потік, тип — одноразовий (дата) або повторюваний
  (дні тижня), час початку й час завершення.
- Запис через північ: кінець, менший за початок. Плюс півречення про те, що кінець,
  рівний початку, відхиляється («Час кінця не може дорівнювати часу початку») — тобто
  рівно доби одним пунктом не буває. Число `1439` не називати: це не налаштування, а межа,
  до якої користувач доходить через час кінця.
- **Дві умови, без яких розклад мовчить** (додано 2026-08-13) — обидві обов'язкові:
  1. **Tapir має працювати.** Вікно, що минуло цілком без запущеного застосунку, стає
     «✗ пропущено (Tapir не працював)». Але якщо застосунок запустили **посеред** вікна —
     запис стартує одразу й пише залишок, результат «почато із запізненням, N хв»
     (`catch_up_mid_window_is_late`). Це та частина, де розклад приємно дивує.
  2. **Працює лише активний профіль** ([timer.rs:88](../../../src-tauri/src/scheduler/timer.rs:88)
     читає `active_profile`, перемикання профілю робить `core.reset()`). Розклад у
     неактивному профілі не спрацює й навіть не позначиться пропущеним. Найтихіший спосіб
     втратити запис, який тут є: рядок «Увімкнено», наступний запуск показано, а о 20:00 не
     станеться нічого. Описувати **по суті на місці** — вкладки «Профілі» на момент злиття
     ще немає (конвенція розмітки 2).
- **Хто зайняв потік, той його й пише** (розгорнуто 2026-08-13) — одне правило, три
  наслідки; порізно кожен читається як дефект:
  потік зайнятий на початку вікна → «потік уже записувався» фіксується раз на вікно, і
  плановий запис **не стартує навіть після того, як ручний зупинили**
  ([core.rs:187](../../../src-tauri/src/scheduler/core.rs:187), тест
  `busy_stream_skips_once_per_window`); ви зупинили плановий вручну → у цьому вікні він не
  відновиться; запис **обірвався сам** → Tapir підніме його до кінця вікна
  (`vanished_recording_restarts_within_window`). Коротко: ручна дія остаточна на вікно,
  аварія — ні.
- **Життя одноразового пункту** (додано 2026-08-13): після відпрацювання Tapir сам знімає
  галочку, але рядок із результатом лишає. Увімкнути назад не вийде — `validate_for_enable`
  відхиляє пункт із минулим вікном; робочий шлях — змінити дату на майбутню або створити
  новий. Створити одноразовий у минулому теж не можна. Без цього автоматичне вимкнення
  читається як збій.
- Увімкнути/вимкнути пункт, не видаляючи його; коли наступний запуск (у вимкненого й у
  відпрацьованого одноразового — прочерк, [schedule_commands.rs:154](../../../src-tauri/src/commands/schedule_commands.rs:154)).
- **Правка пункту, який зараз пише, зупиняє запис** (додано 2026-08-13): суттєві поля —
  потік, час, дні, дата, тривалість ([schedule_commands.rs:220](../../../src-tauri/src/commands/schedule_commands.rs:220)),
  вимкнення галочкою (`:279`) і видалення. Зміна **лише назви** — не зупиняє, і цей виняток
  назвати: перейменувати пункт під час запису — природна дрібна дія. Повторне ввімкнення в
  тому ж вікні запис не поверне.
- Редагувати, видалити (зокрема виділені).
- Запас часу до й після: мітки **«Починати раніше, хв»** і **«Закінчувати пізніше, хв»**,
  налаштування профілю, вкладка «Запис». Типово `0` обидві, стеля `30` до і `60` після,
  клампиться мовчки ([profile.rs:217](../../../src-tauri/src/profile.rs:217)). Головне
  (додано 2026-08-13): це налаштування **профілю, а не пункту** — розсуває всі пункти
  одразу, дати запас одній передачі не можна.
- Результат минулого запуску: у коді **вісім** текстів
  ([scheduleFormat.ts:76](../../../src/lib/scheduleFormat.ts:76)) — «✓ записано N хв»,
  «почато із запізненням, N хв», «✗ пропущено (причина)», «зупинено вручну», «зупинено:
  переключення профілю», «зупинено: закриття додатка», «зупинено: розклад змінено»
  (у попередній редакції цього переліку бракувало), «потік уже записувався»; причини
  пропуску — «Tapir не працював», «не вдалося стартувати запис», «переведення годинника»;
  прочерк, поки запусків не було. **Списку визначень у тексті не робити** (рішення
  2026-08-13): решта — уже речення, а місце потрібне під механіку. Розкриваються прозою
  лише «пропущено» з трьома причинами і «потік уже записувався».
- **Розклад на видалений потік** (додано 2026-08-13): потік видаляється без попередження
  про розклад, пункт лишається ввімкненим і показує **«потік видалено»**, увімкнути такий
  теж дозволено (`validate_for_enable` навмисно не перевіряє потік), а о своїй годині він
  дасть «✗ пропущено (не вдалося стартувати запис)». Єдина з трьох причин, яку користувач
  створює сам, — тож речення ставити там, де розкрито «пропущено».
- **Сповіщення** (додано 2026-08-13): Tapir повідомляє про старт, завершення й про те, що
  запис не відбувся. Спливне вікно в треї гейтить `ui.tray_notifications`
  ([timer.rs:140](../../../src-tauri/src/scheduler/timer.rs:140)) — той самий прапорець, що
  підписаний **«Сповіщення при зміні треку»** (профіль → «Інтерфейс»). Важіль назвати з
  застереженням «попри назву, керує й сповіщеннями розкладу»; саму мітку правити окремим
  записом [tray-notifications-label-scope](p1-tray-notifications-label-scope.md).
- Блок «Клавіші на цьому екрані» — три пункти: `Enter` (редагувати), `Space`
  (увімкнути/вимкнути), `Ctrl+N` (додати). `Space` обов'язковий: `navigation.md` уже
  пообіцяв, що справжнім перемикачем він буває саме там, де в рядка є стан
  «увімкнено/вимкнено», і назвав Розклад. `Delete` не входить — робить канонічне.

### `songs.md` — Записи

- Що це: усі збережені треки в одному місці.
- Пошук, сортування (дата, назва, виконавець, розмір), фільтр за станцією.
- Відтворити / зупинити просто зі списку.
- Відкрити у зовнішній програмі (`Alt+Enter`), показати в Провіднику (`Ctrl+Enter`).
- Перейменувати файл (доповнено 2026-08-13; `F2` — 2026-08-14): вводиться **лише ім'я** — розширення Tapir
  зберігає, формат перейменуванням не змінити
  ([ops.rs:26](../../../src-tauri/src/songs/ops.rs:26)). Символи `< > : " / \ | ? *`
  заборонені, і поки вони є, зберегти не можна
  ([RenameDialog.tsx:14](../../../src/components/songs/RenameDialog.tsx:14)). Якщо файл із
  таким іменем уже є, Tapir додасть `_2` й назве ім'я, яке вийшло насправді
  ([ops.rs:44](../../../src-tauri/src/songs/ops.rs:44)) — у `templates.md` це саме правило
  описане щодо записуваних файлів, читач `songs.md` туди не піде.
- Редагувати теги: виконавець, назва, альбом, жанр. З клавіатури — `F4`, як `F2` для
  імені (додано 2026-08-14, `hotkeys-expansion`: конвенція Total Commander «`F2` — ім'я,
  `F4` — вміст»). Обидві діють лише на сфокусований рядок, не на виділення.
- Видалення — у кошик, із підтвердженням. Файл, що зараз відтворюється, поводиться
  **по-різному в двох шляхах** (виправлено 2026-08-13): видалення одного рядка
  **завершується помилкою** з проханням спершу зупинити відтворення
  ([songs_commands.rs:150](../../../src-tauri/src/commands/songs_commands.rs:150)), а
  видалення виділених **мовчки пропускає саме його** й видаляє решту — часткова вдача,
  не помилка ([songs_commands.rs:195](../../../src-tauri/src/commands/songs_commands.rs:195)).
  Описати обидва: користувач інакше не зрозуміє, чому «нічого не сталося» проти
  «зникло N−1».
- Позначка «неповний трек» (уточнено 2026-08-13): означає, що Tapir не бачив початку або
  кінця пісні; **береться з імені файлу** — воно закінчується на `_incomplete`
  ([scanner.rs:44](../../../src-tauri/src/songs/scanner.rs:44) щоразу перечитує ознаку з
  імені, збережених метаданих немає), а так іменує сам Tapir за шаблоном неповного треку
  (типово `%s\%a - %t_incomplete`, [profile.rs:189](../../../src-tauri/src/profile.rs:189)).
  Наслідок, який треба назвати: **перейменування, що прибирає суфікс, знімає й позначку**.
  Про шаблони — див. розділ «Файли й імена» (вкладка вже існує, посилатися словами можна).
- Блок «Клавіші на цьому екрані».

### `recording.md` — Як працює запис

- Почати й зупинити запис **одного потоку**: рядок (`Enter` за налаштуванням «Дія при
  активації потоку», типово запис; `Ctrl+Enter` завжди), кнопка панелі дій — одним
  згадуванням без переліку, глобальна гаряча клавіша. Масові дії описує `streams.md`:
  шов між розділами проведено по об'єкту — список проти процесу (уточнено 2026-08-13).
- Статуси рядка й що кожен означає: очікує, підключення, запис, запис і відтворення,
  перепідключення (зі спробою №), помилка.
- **Метадані ефіру** — термін вводиться саме тут: дані, які станція передає в ефір разом
  зі звуком; за ними Tapir розуміє, що почався новий трек, і ріже запис на файли.
- Перший трек після старту майже завжди неповний; його можна не зберігати (налаштування).
- Мінімальна тривалість треку; автоматичне виправлення регістру назв.
- Окремий суцільний файл усього ефіру — вмикається окремо.
- Обрив зв'язку: **за замовчуванням Tapir не перепідключається** — обрив зупиняє запис
  (виправлено 2026-08-13: `ReconnectConfig::default()` має `max_retries: 0`, і
  [manager.rs:618](../../../src-tauri/src/stream/manager.rs:618) на нулі виходить із циклу).
  Далі: як увімкнути спроби, і лише тоді числа — пауза від `5` с, множник `1.5`, стеля
  `300` с. Розбіжність «підказка обіцяє необмежено» — окремий запис
  [reconnect-zero-retries](p0-reconnect-zero-retries.md).
- Вільне місце: поріг (типово `1` ГБ, `0` вимикає) перевіряється **перед стартом** —
  ручним, «Записати все», плановим і при підйомі після аварії; **уже активний запис він
  не зупиняє** (виправлено 2026-08-13 — перевірки під час запису в коді немає взагалі).
  Якщо диск заповниться на ходу, запис завершується помилкою й не перепідключається.
- Кілька потоків можна писати одночасно.
- Станція без метаданих ефіру: запис іде, поділу на треки немає.
- Після аварійного завершення записи піднімаються самі, і Tapir про це повідомляє.
- У збережені файли пишуться теги виконавця й назви.

### `player.md` — Прослуховування

- Слухати ефір (`Shift+Enter` або кнопка) і слухати збережений файл.
- Різниця між паузою й зупинкою: ефір зупиняється, файл ставиться на паузу з позицією.
- Гучність (уточнено 2026-08-13): повзунок у панелі плеєра — `↑`/`↓` змінюють її на
  **крок** (типово `5`%, [settings.rs:183](../../../src-tauri/src/settings.rs:183);
  задається в налаштуваннях програми → **Аудіо**, діапазон `1`–`10`); `←`/`→` тут
  переходять до сусіднього елемента плеєра, а не змінюють звук
  ([VolumeSlider.tsx:67](../../../src/components/player/VolumeSlider.tsx:67)). Той самий
  крок використовують глобальні клавіші гучності. Вимкнення звуку — окрема кнопка
  **або `Ctrl+M` з будь-якого місця вікна** (доповнено 2026-08-14,
  `hotkeys-expansion`): обидва шляхи — те саме перемикання, і Tapir озвучує стан
  («Звук вимкнено» / «Звук увімкнено»), а не назву команди
  ([muteControl.ts](../../../src/lib/muteControl.ts)). Про мишу не писати (правило 4).
- **`F9` — «що зараз відтворюється»** (додано 2026-08-14, `hotkeys-expansion`):
  відповідає з будь-якого місця вікна, не рухаючи фокус — станція і трек в ефірі,
  ім'я й позиція для файлу, назва станції для прев'ю, «Нічого не відтворюється»,
  коли нічого не грає; вимкнений звук додається в кінці речення
  ([playbackAnnounce.ts](../../../src/lib/playbackAnnounce.ts) `describePlayback`).
  Власник пояснення — `player.md`: це відповідь **про плеєр**, а не навігація.
- Позиція у файлі (уточнено 2026-08-13): `↑`/`↓` перемотують на **`5` секунд**
  ([PlaybackPosition.tsx:48](../../../src/components/player/PlaybackPosition.tsx:48));
  `←`/`→` та `Home`/`End` тут переходять між елементами плеєра, а не рухають позицію.
  Повзунок є лише для файлу з відомою тривалістю; для ефіру на його місці смужка «Живий
  потік» — ефір не перемотати. Тримати одним реченням про клавіші: `player.md` — панель,
  окремого блоку «Клавіші на цьому екрані» в ньому немає.
- Попередній: **за замовчуванням поріг `0`, тобто вимкнено** (виправлено 2026-08-13 —
  [settings.rs:182](../../../src-tauri/src/settings.rs:182)), і кнопка завжди переходить
  до попереднього запису. Якщо задати поріг (`0`–`30` с, налаштування програми →
  **Аудіо**), то після цієї позиції «попередній» спершу починає поточний **файл**
  спочатку ([playbackTransport.ts:48](../../../src/lib/playbackTransport.ts:48) вимагає
  `> 0` і джерело-файл). На ефір поріг не діє. Наступний — без порогів.
- **Що таке «наступний»** (додано 2026-08-13): серед потоків, коли грає ефір, і серед
  записів, коли грає файл; порядок — той, що зараз на відповідному екрані, **з
  урахуванням фільтра й сортування** (`$visibleStreams` / `$filteredSongs` у
  [playbackNeighbors.ts](../../../src/stores/playbackNeighbors.ts)). Ефір і записи не
  змішуються; у прев'ю станції обидві кнопки неактивні, як і на межах списку. Без цього
  пункту кнопка виглядає зламаною: відфільтрував «Записи» — і «наступний» ходить лише
  всередині вибірки.
- Автоперехід (уточнено 2026-08-13): коли файл дограв, Tapir сам вмикає наступний —
  **типово увімкнено** ([profile.rs:345](../../../src-tauri/src/profile.rs:345)),
  вимикається в налаштуваннях профілю, вкладка **Відтворення**. Наступний береться з того
  самого списку, що й кнопка «наступний». Дійшовши до кінця, Tapir **зупиняється** —
  по колу не ходить ([playbackTransport.ts:60](../../../src/lib/playbackTransport.ts:60)).
- Продовження **останнього джерела** (уточнено 2026-08-13; звужене формулювання «останнього
  файлу» виправлено при реалізації — `resume_last` має і гілку `ColdStart::PlayStream`,
  тож станція теж повертається в ефір): коли нічого не грає, Tapir може
  підняти той файл, який слухали востаннє, — з позиції (типово,
  [profile.rs:346](../../../src-tauri/src/profile.rs:346)) або з початку; вибір — у
  налаштуваннях профілю, вкладка **Відтворення**. **На звичайну паузу це не впливає**:
  з паузи файл завжди продовжується з місця зупинки
  ([playback_control.rs:217](../../../src-tauri/src/playback_control.rs:217) не читає
  `resume_file_from`; сторожить тест на рядку 458). Механіку «підняти останнє» описувати
  **по суті на місці**: вкладки «Робота у фоні» на момент злиття запису ще немає.
  **Доповнено 2026-08-13** (grilling `help-config`): написана редакція описала механіку,
  але не назвала перемикач, який її вмикає, і не сказала, що той **типово вимкнений**
  ([profile.rs:344](../../../src-tauri/src/profile.rs:344)) — тобто довідка обіцяла
  поведінку, якої у свіжого Tapir немає, всупереч правилу 7. Розділ перевідкривається на
  одне речення в межах `help-config`: назвати **Автовідтворення при запуску** (налаштування
  профілю, вкладка **Відтворення**) і дефолт. Власником пояснення лишається `player.md` —
  дефолт є частиною механіки, а не складу діалогу. Українська мітка при цьому обіцяє не те,
  що англійська («Resume last playback on startup»); вирівнювання — запис
  [settings-labels-wording](../p2-settings-labels-wording.md), довідка цитує кожну локаль
  як є.
- Пристрій виводу звуку: панель плеєра лише **показує** поточний, змінити його звідси
  не можна (виправлено 2026-08-13). Вибір пристрою й кнопка оновлення списку — в
  налаштуваннях програми, вкладка **Аудіо**
  ([AudioTab.tsx](../../../src/components/settings/AudioTab.tsx)).
- Що показує панель плеєра (уточнено 2026-08-13): що зараз грає, і **пара значків
  джерела** — `LIVE` для ефіру станції та **Запис** для збереженого файлу
  ([PlayerPanel.tsx:289](../../../src/components/player/PlayerPanel.tsx:289) рендерить
  `RecordingBadge` безумовно в гілці файлу; на статуси записів він не дивиться).
  **Не називати це «ознакою запису»**: у довідці «запис» — процес
  ([CONTEXT.md](../../../CONTEXT.md)), а мітка вживає однину в значенні файлу. Сама
  мітка — окремий дефект, запис `player-recording-badge-term`; довідка описує те, що
  зараз на екрані (правило 5 конвенцій розмітки).
- Слухати одну станцію, поки пишеться інша, — нормально.

### `templates.md` — Файли й імена

- Де опиняються записи: тека `recordings\` поруч із `tapir.exe`, усередині — тека станції.
- Як змінити теку записів.
- Три шаблони: ім'я треку, ім'я неповного треку, ім'я файлу суцільного ефіру.
- Змінні шаблонів — **списком**, кожна з поясненням і прикладом.
- Готові приклади шаблонів.
- Символи, заборонені в іменах файлів Windows, і що Tapir із ними робить.
- Збіг імен: до імені додається `_2`, `_3` тощо.

### `settings.md` — Налаштування

> **Звірено з кодом 2026-08-13** (grilling `help-config`). Обидва діалоги мають **бокову
> смугу вкладок**: у програми три (Загальні / Аудіо / Гарячі клавіші), у профілю чотири
> (Запис / Відтворення / Інтерфейс / Постобробка) — попередня редакція подавала їх пласким
> переліком. Перелік читати як є.

- **Одне речення-правило перед переліками** (додано 2026-08-13): у налаштуваннях програми —
  те, що стосується **цього комп'ютера й самої програми** (пристрій виводу, те, що Tapir
  реєструє в системі, і те, що робить натискання клавіші); у налаштуваннях профілю — те, що
  описує **сценарій**: які дані, куди пишуться і як показані. Джерело —
  [ADR про межу](../../decisions/2026-08-08-global-vs-profile-settings-boundary.md), але
  чотири фільтри переказувати не можна: читачеві потрібна евристика, а не класифікатор.
  Свідомий виняток ADR («Сповіщення при зміні треку» в профілі) **не називати** — правило
  подається як орієнтир, і застереження про один виняток коштує дорожче, ніж дає.
- Межа **фізична**: область дії видно з того, в якому діалозі налаштування лежить —
  `Ctrl+,` проти `Ctrl+Shift+,`. Підпису на контролях немає.
- **Формат переліків — по секціях, а не по контролях** (рішення 2026-08-13): рядок на
  секцію, усі її мітки дослівно через кому, пояснення — лише для позицій, які не має кому
  пояснити деінде. Близько двадцяти контролів уже мають власника (`recording.md` — тека,
  три шаблони, файл потоку, фільтри треків, поріг диску, перепідключення; `scheduling.md` —
  запас часу; `player.md` — крок гучності, поріг «попередній», автоперехід, продовження
  файлу, автовідтворення), і **вдруге вони не пояснюються**: шов «один власник пояснення»
  (див. нижче, `background.md`).
- **Налаштування програми (`Ctrl+,`)**, три вкладки:
  - **Загальні** — «Інтерфейс» (Мова, Тема); «Трей» (Згортати до tray замість закриття);
    «Поведінка» (Показувати назву треку в заголовку вікна, Дія при активації потоку);
    «Автозапуск» (Запускати разом із Windows, Запускати мінімізованим — **неактивне, поки
    автозапуск вимкнено**); «Логування» (Детальне логування для діагностики + **згорнутий
    блок «Додатково»**: Рівень логування, Макс. розмір файлу логу).
  - **Аудіо** — Пристрій виведення й кнопка оновлення списку; «Медіа-інтеграція»
    (Інтеграція з системними медіа-кнопками); «Керування» («Попередній» рестартує трек…,
    Крок гучності).
  - **Гарячі клавіші** — вісім дій **із типовими комбінаціями** (див. нижче) і «Скинути до
    стандартних».
- **Налаштування профілю (`Ctrl+Shift+,`)**, чотири вкладки:
  - **Запис** — «Тека та шаблони», «Файл потоку», «Фільтри треків», «Планувальник» і
    **згорнутий блок «Перепідключення»**.
  - **Відтворення** — Автовідтворення при запуску, Відновлювати файл, Автоматично
    відтворювати наступний трек.
  - **Інтерфейс** — Сортування, Сповіщення при зміні треку.
  - **Постобробка** — один рядок: вкладка є, фіча поки що недоступна (запис
    [post-processing](../p1-post-processing.md)); без вибачень, стиль-гайд правило 8.
- **Згорнуті блоки називати явно** (додано 2026-08-13): чотири поля перепідключення й два
  поля журналу сховані в закритих блоках, і читач переліку не знайде їх на екрані.
- **Типові комбінації восьми глобальних клавіш називаються саме тут** (додано 2026-08-13):
  `Ctrl+Shift+R` запис, `Ctrl+Shift+K` відтворення, `Ctrl+Alt+↑`/`↓` гучність,
  `Ctrl+Shift+H` показати-сховати вікно, `Ctrl+Shift+S` зупинити весь запис,
  `Ctrl+Alt+←`/`→` попередній і наступний ([settings.rs:127](../../../src-tauri/src/settings.rs:127)).
  Це **єдине можливе місце**: генерована вкладка «Гарячі клавіші» покриває лише шар
  webview, глобальних у ній немає й не буде.
- Налаштування зберігаються самі — кнопки «Зберегти» немає, індикатора теж (є лише
  постійний напис у підвалі діалогу).
- **Три різні відмови при призначенні клавіші, усі три документуються** (розгорнуто
  2026-08-13):
  1. діалог відхиляє одразу — комбінація зарезервована за дією застосунку («Цю комбінацію
     зарезервовано для: {дія}») або вже призначена іншій дії списку;
  2. комбінацію **неможливо ввести**: рекордер приймає лише `Ctrl`/`Shift`/`Alt` плюс
     літеру, цифру, стрілку, `Space`, `Pause` або `F`-клавішу, а решту **ігнорує мовчки**
     ([KeyRecorder.tsx:20](../../../src/components/settings/KeyRecorder.tsx:20)) — виглядає як
     зависання. Формулювати **позитивно** (з чого комбінація складається), а не переліком
     заборон. Дефект самого рекордера — запис
     [key-recorder-silent-rejection](../p1-key-recorder-silent-rejection.md);
  3. комбінацію прийнято, але її забрала інша програма — це вже поведінка, і живе в
     `background.md`.
- Скидання — **тільки для всіх восьми** одразу; повернути типову комбінацію одній дії не
  можна, хрестик у рядку її **очищає**, а не відновлює (уточнено 2026-08-13).
- **`bandwidthLimitKbps` не згадується взагалі**: поле є в налаштуваннях на диску, але
  контролю в інтерфейсі немає (запис
  [bandwidth-limit-dead-setting](p1-bandwidth-limit-dead-setting.md)); довідка описує
  екран.

### `background.md` — Робота у фоні

> **Звірено з кодом 2026-08-13** (grilling `help-config`). Тринадцять позицій виправлено
> або додано. Найважливіше — увесь фоновий шар (меню трею, підказка значка, діалог виходу,
> шість тостів) **зашитий у Rust українською**, i18n там не використовується взагалі
> ([menu.rs:61](../../../src-tauri/src/tray/menu.rs:61),
> [notify.rs:54](../../../src-tauri/src/tray/notify.rs:54)). Перелік читати як є.

**Шов із `settings.md`** (рішення 2026-08-13): `settings.md` відповідає на «**де** це
вмикається», `background.md` — на «**що станеться**». У кожної позиції рівно один власник
пояснення; мітка може прозвучати двічі, пояснення — ніколи. П'ять фонових важелів фізично
лежать у діалозі програми (згортання в трей, автозапуск ×2, медіа-кнопки, вісім клавіш),
тож тут вони описуються за поведінкою, а місце називається одним півреченням.

- **Фоновий шар описується за дією, без жирних міток, симетрично в обох локалях**
  (рішення 2026-08-13). Конвенція розмітки 5 вимагає, щоб жирна мітка збігалася з рядком у
  `uk.json`, — а цих рядків там немає, і англійський користувач бачить український трей.
  Українською опис за дією однаково збігається з екраном («Грати», «Зупинити», «Зупинити
  всі записи», «Вихід» — прості дієслова), тож `uk`-читач нічого не втрачає, а текст
  переживе локалізацію. Сам дефект — запис
  [tray-layer-not-localized](p1-tray-layer-not-localized.md).
- **Розділ відкривається тим, що закриття вікна не виходить із програми** (рішення
  2026-08-13, найдорожча помилка розділу): за типових налаштувань вікно ховається, а
  записи, розклад і відтворення тривають. Помилка двобічна — хто вважає, що закрив
  програму, лишає запис іти; хто вважає, що Tapir у треї «просто згорнутий», не розуміє,
  чому заповнюється диск.
- **Підтвердження виходу під час запису стоїть на одних дверях, а не на всіх** (виправлено
  2026-08-13): за типових налаштувань на закритті вікна воно **не з'являється взагалі** —
  гілка ховання спрацьовує раніше перевірки
  ([lib.rs:245](../../../src-tauri/src/lib.rs:245)). Побачити його можна пунктом **Вихід**
  у меню значка або закриттям вікна, якщо згортання вимкнути.
- **Мінімізація вікна в трей не ховає** — тільки закриття (додано 2026-08-13: обробника
  мінімізації в коді немає). Півречення, інакше «згортання в трей» читається як реакція на
  кнопку «згорнути».
- Значок у треї; ліве клацання **перемикає** — ховає вікно, якщо воно видиме. Меню значка
  **динамічне** (уточнено 2026-08-13): «Зупинити» є, лише поки щось грає; рядок про
  активні записи й «Зупинити всі записи» — лише поки записи є.
- **Підказка значка** показує, що зараз грає й скільки потоків пишеться (додано
  2026-08-13) — спосіб перевірити стан, не відкриваючи вікна.
- Сповіщення про зміну треку (вмикається в налаштуваннях профілю; той самий прапорець
  гейтить і сповіщення розкладу — застереження вже несе `scheduling.md`).
- Глобальні гарячі клавіші: що вміють — запуск і зупинка запису, відтворення, гучність,
  показати/сховати вікно, зупинити всі записи, попередній і наступний. Типові комбінації
  називає `settings.md`; тут — відсилання «поточні дивитись у налаштуваннях програми».
  Плюс третя відмова (додано 2026-08-13): **комбінацію може забрати інша програма**, тоді
  дія просто не спрацює — лікується перепризначенням. Про те, що при старті це стається
  **без жодного знаку**, довідка не пише; це дефект, запис
  [hotkey-registration-silent-at-startup](../p1-hotkey-registration-silent-at-startup.md).
- Системні медіа-кнопки й кнопки гарнітури керують **відтворенням** так само, як кнопки
  плеєра (уточнено 2026-08-13: на ефірі «пауза» його зупиняє — правило вже пояснене в
  `player.md`, і суперечити йому не можна). Плюс півречення: з вимкненою інтеграцією Tapir
  зникає із системного оверлея й кнопки повертаються попередньому плеєру.
- Запуск разом із Windows і запуск згорнутим. **Переміщений `tapir.exe` — наслідком і
  дією** (рішення 2026-08-13): якщо перенести теку, автозапуск перестане працювати; коли
  Tapir запустять із нового місця, він автозапуск **вимкне** і про це скаже — треба
  ввімкнути знову. Звірку реєстру не переказувати (стиль-гайд правило 3); ланку «Tapir
  дізнається із запізненням» не розгортати — вона не дає дії.
- **Що саме створює автозапуск** (додано 2026-08-09): запис у реєстрі Windows
  (`HKCU\Software\Microsoft\Windows\CurrentVersion\Run`,
  [autostart.rs](../../../src-tauri/src/autostart.rs)), який зникає, коли опцію вимкнути.
  Це єдине, що Tapir лишає поза власною текою — тому портативність в `overview.md`
  описується без слова «реєстр», а чесне пояснення стоїть тут.
- Другий запуск не відкриває друге вікно, а активує вже відкрите — **і виконує в ньому
  передані аргументи** (додано 2026-08-13,
  [single_instance.rs:64](../../../src-tauri/src/single_instance.rs:64)).
- **Запуск з параметрами** — підрозділ, і перелік іде **двома групами** (рішення
  2026-08-13), бо код ділить вісім аргументів на дві несумісні половини:
  - **діють лише при запуску** — `--profile`, `--minimize`. У переданих на вже запущений
    Tapir вони **ігноруються** ([cli.rs:106](../../../src-tauri/src/cli.rs:106)), тож ярлик
    «запустити в профілі „Новини" згорнутим» робить обіцяне, лише коли Tapir ще не
    працює. Приклад: `tapir.exe --profile "Новини" --minimize`;
  - **діють завжди, зокрема на вже запущеному** — `--record`, `--play`, `--stop-recording`,
    `--stop-playback`, `--wish-add`, `--wish-remove`. Приклад: `tapir.exe --record "Jazz FM"`.

  Групування і є та модель, якої користувачу бракує: «ярлик, який **запускає** Tapir як
  мені треба» проти «ярлик, який **керує** тим, що вже працює». Плоский перелік їх змішує,
  а застереження в кінці читається після того, як ярлик уже складено.

  Плюс два півречення (додано 2026-08-13): **Tapir не пише в консоль** — результат кожної
  команди він повідомляє у власному вікні (тому `--help` і `--version` існують, але нічого
  не покажуть, і в перелік не йдуть); **`--record` і `--play` знаходять потік за точною
  назвою або точною адресою**, не за частиною назви — саме на цьому ярлик мовчки не
  спрацює.

  Формулювати як побутовий інструмент (ярлик, автозавантаження), а не як «режим для
  розробників».

### `troubleshooting.md` — Якщо щось не працює

> **Звірено з кодом і з усіма 13 написаними розділами 2026-08-14** (grilling
> `help-troubleshooting`). Дві позиції додано, одна втратила власний заголовок, а весь
> розділ змінив жанр — див. нижче. Перелік читати як є.

**Жанр: відповідь без механіки** (рішення 2026-08-14). Сім із дванадцяти позицій
попередньої редакції виявились уже вичерпно поясненими у своїх розділах — не згаданими, а
закритими реченням із дією: треки не діляться, місце на диску й аварійне завершення
(`recording.md`), клавішу забрала інша програма (`background.md`), плановий не стартував
(`scheduling.md`), файл не відкрився (`songs.md`), каталог недоступний (`browser.md`).
Тому розділ **не переказує механіку** — інакше на одну поведінку з'являється два джерела
правди й ламається шов «один власник пояснення». Але й не зводиться до відсилань:
симптомний розділ і розділ-власник відповідають на різні питання — «що зі мною зараз»
проти «як це працює». Формула позиції: **причина + дія, 1–3 речення**; півречення веде до
власника лише там, де механіка справді потрібна.

**Форма: заголовок `###` на симптом**, словами користувача, не назвою підсистеми, без
жирного й backtick'ів. Читач приходить рівно з одним симптомом і не читає розділ підряд, а
заголовок — єдина ціль переходу, яку виробляє конвеєр (посилань немає, таблиці заборонені
конвенцією 1, а конвенція 5 не дає виділити симптом усередині рядка списку). Ціна
визнана: 14 заголовків проти 3–6 у решті розділів. **Порядок — за стадією, на якій симптом
помічають** (джерело → що записалося → що чути → що поза застосунком → застосунок як
ціле); те саме правило для того, хто дописуватиме позицію.

1. Потік не підключається або постійно перепідключається — адреса могла змінитись;
   перевірити зовнішнім плеєром; після вичерпання спроб запис зупиняється.
2. Записується один суцільний файл, треки не діляться — станція не передає метадані ефіру.
3. **Треків збереглося менше, ніж було в ефірі** (додано 2026-08-14) — типово не
   зберігається перший неповний і все коротше за `30` с
   ([profile.rs:203](../../../src-tauri/src/profile.rs:203)). `recording.md` називає обидва
   фільтри **з боку фічі**, у розділі, який читають до запису; з боку симптому це «файл
   зник», і людина шукає його в теці, а не в налаштуваннях.
4. Закінчується місце на диску — поріг не дає почати, активний запис не зупиняє.
5. Плановий запис не стартував — причину показує рядок пункту; три найчастіші.
6. **Формат записується, але не відтворюється** — Tapir відтворює MP3 і AAC; станція з
   кодеком `AAC+` не грає всередині ([engine.rs:66](../../../src-tauri/src/player/engine.rs:66)
   — symphonia не підтримує HE-AAC/HE-AACv2). Кодек **називати прямо**: це дослівна мітка
   інтерфейсу (фільтр і колонка Браузера станцій), тож правило 3 не порушується, а
   профілактика стає можливою до аварії. Те, що нерозпізнаний формат зберігається з
   розширенням `.mp3`, **не описувати** — це дефект, запис
   [unknown-format-falls-back-to-mp3](../p1-unknown-format-falls-back-to-mp3.md).
7. **Відтворення йде, а звуку немає** (додано 2026-08-16) — звук вимкнено. Стан показує
   **кнопка плеєра**, `F9` називає його з будь-якого місця вікна, повертає `Ctrl+M`.
   Кнопку називати **обов'язково поруч із `F9`**: `F9` віддає відповідь лише в
   `sr-only`-регіон ([useGlobalShortcuts.ts:49](../../../src/hooks/useGlobalShortcuts.ts:49)),
   тож сама по собі вона є текстом для користувача скрінрідера — заборонено принципом.
   `Ctrl+M` — дія повернення, **не** перевірка: її відгук є сам звук, якого в цьому
   симптомі немає. Єдина позиція розділу, що **передає читача далі** («якщо звук
   увімкнено — причина в пристрої виведення»): заголовок найзагальніший у розділі й
   стоїть перед двома вужчими, сформульованими як причини. Нульова гучність тут не
   згадується — її зводить із вимкненим звуком код, запис
   [zero-volume-reads-as-muted](p2-zero-volume-reads-as-muted.md).
8. Звук іде не в той пристрій — панель плеєра пристрій лише показує.
9. **Звук зник після від'єднання пристрою** (додано 2026-08-14) — пристрій запам'ятовується
   **за назвою**, і фолбеку на системний немає
   ([engine.rs:946](../../../src-tauri/src/player/engine.rs:946)): відтворення просто не
   працює, поки пристрій не переобрати вручну. Єдина відмова в застосунку, яка не
   самолікується й про яку довідка мовчала.
10. Файл не відкрився у зовнішній програмі — немає програми за замовчуванням або файл
    переміщено.
11. Гаряча клавіша не спрацювала — її забрала інша програма; як призначити іншу.
12. Каталог станцій недоступний.
13. Tapir завершився аварійно — записи піднімаються самі, Tapir каже скільки.
14. **«Якщо нічого не допомогло» — заголовок дією, не назвою ресурсу**, і стоїть
    **останнім** як ескалаційний вихід розділу: де лежить журнал (`data\logs\tapir.log`),
    процедура «увімкнути → **перезапустити** → повторити → забрати» (перезапуск
    обов'язково назвати: рівень і розмір читаються один раз на старті, а напис про це
    стоїть лише під галочкою), підняти розмір для рідкісних проблем, **у теці два файли —
    брати обидва**, і куди повідомити. Механіку ротації не описувати: вона відповідає на
    тривогу, якої в користувача немає, поки довідка сама її не назве.

**Скрінрідери — друга й остання згадка на всю довідку, одним реченням усередині останньої
позиції, без власного заголовка** (рішення 2026-08-14): якщо щось не озвучується або до нього не
дійти з клавіатури — це дефект Tapir, а не налаштування системи користувача. Окремий
заголовок обіцяв би рецепт, якого немає; єдині можливі рецепти («перевідкрийте діалог»,
«перейдіть `F6`») є обходами дефектів і мусили б старіти разом із ними. Критерій рахує
**місця, а не входження**: рядок сумісності в `overview.md` називає трьох читачів в одному
реченні.

## Розбиття на дочірні записи

Кожен запис везе **свої** вкладки й i18n-ключі: вкладка з'являється разом зі своїм
текстом, порожніх вкладок-заглушок у застосунку не існує ніколи. Усі — `type: planned`,
`status: ready`, `kind: chore`, `target: 0.1.0`, `priority: P1`,
`gates: [pnpm test, pnpm vite:build]`, `depends_on` включає `help-content-polish`.

| Запис | Розділи | Нових вкладок | `effort` | `a11y` | Ще залежить від |
|---|---|---|---|---|---|
| `help-intro` | `overview` (переписати), `navigation` | 1 | M | false | — |
| `help-recording` | `streams`, `recording`, `templates` | 1 | M | false | `help-intro` |
| `help-listening` | `player`, `songs`, `browser` | 3 | M | false | `help-intro` |
| `help-automation` | `wishlist`, `scheduling` | 0 | S | false | `help-intro` |
| `help-config` | `profiles`, `settings`, `background` | 2 | M | false | `help-intro` |
| `help-troubleshooting` | `troubleshooting` + правило в `AGENTS.md` + тест меж обсягу | 1 | S | ~~true~~ **false** | усі попередні |

Разом 8 нових вкладок до наявних 7 = 15.

`help-intro` іде першим не за алфавітом: у ньому осідає канон наскрізних механік
(на нього посилаються всі екранні розділи) і тести парності локалей — тому решта п'ять
від нього залежать.

**`a11y: false` в усіх шістьох — прогону не буде** (переглянуто 2026-08-14). Попередня
редакція ставила `a11y: true` рівно один раз, на останньому записі: додати `Tab` у наявний
`TabList` — механічна операція, яку стереже `HelpDialog.test.tsx`, тож замість шести
прогонів по одному патерну планувався **один** чекліст на готовий діалог (15 вкладок
стрілками, «N з M», заголовки й навігація клавішею `H`, фокус при відкритті, `Escape`,
фолбек локалі).

Розробник скасував і цей єдиний прогін (рішення 2026-08-14, grilling
`help-troubleshooting`). Підстава: останній запис додає один `<Tab>`, один `<TabPanel>`,
два ключі i18n і два markdown-файли — **жодного нового патерну взаємодії**, шлях рендеру
той самий, яким уже щодня працюють 13 вкладок. Наслідок: `docs/testing/nvda-help-*.md` не
створюється, а весь шестизапис довідки приймається за `pnpm test`, `pnpm vite:build` і
перевіркою розробника.

Побічно звірено (може знадобитись, якщо прогін колись усе-таки робитимуть): **«N з M»
ніде не задано явно** — `aria-posinset`/`aria-setsize` у react-aria `Tabs` немає, позицію
рахує саме WebView2 із дерева доступності. Формулювати це як гарантію Tapir було б хибно.

## Захист від старіння

- **Тести парності** — у `help-intro`: набір файлів `docs/help/uk/` дорівнює набору
  `docs/help/en/`; набір `id` вкладок у `HelpDialog` дорівнює набору файлів. Перше
  критичне: розсинхрон локалей тихий, `getHelpHtml` мовчки падає на український фолбек
  (`helpContent.ts:20`), і англійський користувач роками читає українську.
- **Тест меж обсягу** — в `help-troubleshooting`: нижня межа `120` слів на **обидві**
  локалі, верхня `1000` — тільки на `en`. Замінює задуманий анти-заглушковий тест на дві
  заборонені фрази (рішення 2026-08-14): нижня межа **строго перекриває** чорний список —
  ловить заглушку за будь-якого формулювання, а не дві завчені фрази, — і не має його
  ризику, бо правило 8 стиль-гайду **зобов'язує** `settings.md` написати про Постобробку
  «поки що недоступно», тобто заборонена й обов'язкова лексика стоять упритул. Верхня межа
  додана тому, що правило 6 — єдина числова конвенція цієї специфікації й єдина, яку взагалі
  можна зробити самозастосовною: проза старіє тихо, тест — ні. Рахувати **в JS**
  (`split(/\s+/).filter(Boolean)`), не в оболонці: `wc -w` повертає `0` на кирилиці.
- **Правило в `AGENTS.md`** + рядок у `docs/backlog/_TEMPLATE.md` — теж в
  `help-troubleshooting`: якщо запис змінює видиму поведінку, у «Критеріях готовності»
  з'являється пункт «оновлено відповідний файл `docs/help/`». Раніше обидва були б
  хибно-червоні: вимагали б оновлювати розділи, яких ще немає.

  Уточнено 2026-08-14: в `AGENTS.md` іде **не однорядкове правило, а `docs/help/` як окрема
  поверхня** — що це, хто аудиторія (четверта), і посилання на цю специфікацію. Гола вимога
  «оновлюй `docs/help/`» без контексту породить рівно ті дві правки, які тут заборонені:
  таблиці й посилання. А пункт у шаблоні — **умовний** («оновлено — або зазначено, що
  видимої поведінки не змінює»): безумовний стояв би в кожному записі, зокрема суто
  внутрішньому, а чекбокс, який у половині випадків не стосується справи, ставлять не
  думаючи. Відповідь «не стосується» має бути легальною, але вимовленою.

## Технічні факти для виконавця

- `src/components/common/helpContent.ts:3` глобить `docs/help/*/*.md` **автоматично**
  (eager, обидві локалі). Новий файл коштує лише вкладки в `HelpDialog.tsx` і ключа
  `help_section_*` у двох файлах повідомлень.
- Розмітка компілюється **на збірці**, у бандл потрапляє готовий санітизований HTML;
  рантайм-парсера немає. Правки `.md` підхоплюються HMR.
- `.help-content` оформлює лише `h2`, `h3`, `p`, `ul`, `ol`, `li`, `code`, `pre`, `a`.
  Усе інше (таблиці, цитати, `kbd`) вийде неоформленим.
- Генерована вкладка «Гарячі клавіші» будується з `SHORTCUTS` і покриває **тільки**
  webview-шар. Глобальних (OS) хоткеїв у ній немає — їхні комбінації користувач
  перепризначає, а `SHORTCUTS` статичний. Так само немає клавіш виділення
  (`Ctrl+Space`, `Ctrl+A`, `Shift+↑↓`, `Enter`, `Space`, `Delete`, `Escape`).
  Цю прогалину закриває **проза**: `navigation.md` (клавіші списків і виділення) і
  `background.md` (глобальні хоткеї з відсиланням у налаштування). Робити генеровану
  вкладку повною — окрема інженерна задача (читати живий `HotkeyMap`), у цей запис
  не входить.
- Довідка відкривається `F1` і кнопкою в лівій панелі; обидві — той самий `$helpOpen`.

## Критерії готовності

- [x] Принцип, структура, межа, стиль-гайд, конвенції розмітки й мапа покриття зафіксовані
- [x] Створено шість дочірніх записів із front-matter за таблицею «Розбиття»
- [x] `docs/backlog/ROADMAP.md` містить шість рядків у v0.1.0
- [x] `CONTEXT.md` доповнено термінами: Потік, Трек, Запис / Записи, Метадані ефіру
- [x] Специфікацію прийнято розробником 2026-08-08 → `status: done`, `git mv` у `done/`,
      рядок перенесено в «Виконано» ROADMAP

## Документи

- `src/components/common/HelpDialog.tsx` — структура вкладок
- `src/components/common/helpContent.ts` — завантаження й фолбек локалі
- `build/markdownHelpPlugin.ts` — конвеєр Markdown → HTML
- `src/styles.css` — оформлення `.help-content`
- [keyboard-shortcuts.md](../../keyboard-shortcuts.md) — реєстр клавіш (звіряти з кодом)
- [implementation-phases.md](../../implementation-phases.md) — повний перелік фаз і фіч
- [CONTEXT.md](../../../CONTEXT.md) — терміни предметної області
