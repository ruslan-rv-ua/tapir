---
slug: zero-volume-reads-as-muted
title: "Нульова гучність не читається як вимкнений звук"
priority: P2
type: planned
status: done
effort: M
kind: bug
target: 0.1.0
updated: 2026-08-17
completed: 2026-08-17
a11y: true
depends_on: []
blocks: []
touches:
  - src/lib/muteControl.ts
  - src/lib/playbackAnnounce.ts
  - src/hooks/useGlobalShortcuts.ts
  - src/components/player/PlayerPanel.tsx
  - src/stores/player.ts
  - src/App.tsx
  - CONTEXT.md
  - docs/decisions/2026-08-16-silence-is-mute-or-zero-volume.md
gates: [pnpm test, pnpm vite:build]
notes:
  - "Знахідка grilling help-muted-playback-symptom (2026-08-16). Той самий клас, що key-recorder-silent-rejection і hotkey-registration-silent-at-startup: стан існує, а поверхня про нього мовчить."
  - "Модель узгоджена з практикою: YouTube/video.js тримають стани окремо, а значок і вихід — похідні від volume === 0 || muted. Відкинуте об'єднання станів записане в ADR, щоб рефакторинг «приберіть зайве поле muted» не почався заново."
  - "Правку CONTEXT.md робити РАЗОМ із кодом: до нього твердження «це один стан» неправдиве, а глосарій описує наявне, не заплановане."
  - "Реалізація 2026-08-16: предикат — muteControl.isSoundOff (там же, де дія, обидва викликачі його вже імпортують); пам'ять рівня — muteControl.rememberVolumeLevel, викликається з App.handlePlayerStatus і з початкового getPlayerStatus. Шов саме на події player-status, бо Ctrl+Alt+Up/Down обробляє Rust і webview самого жесту не бачить — лише наслідок. FALLBACK_VOLUME переїхав у stores/player.ts: його єдина роль тепер — дефолт savedVolume, тобто порожня пам'ять."
  - "a11y: true, попри те що запис заведено з grilling довідки. Прецедент «усі записи довідки — false» тут не діє: він про тексти, які читають, а цей міняє поведінку, яку слухають — клауза f9_muted починає спрацьовувати для нового стану, aria-pressed кнопки міняє джерело. Клас той самий, що hotkeys-expansion (a11y: true, чекліст на 13 сценаріїв)."
---

# Нульова гучність не читається як вимкнений звук

> **Контекст:** знайдено під час grilling [help-muted-playback-symptom](p2-help-muted-playback-symptom.md),
> коли з'ясовували, скільки причин має симптом «відтворення йде, а звуку немає».
> Рішення про модель — [ADR 2026-08-16](../../decisions/2026-08-16-silence-is-mute-or-zero-volume.md),
> читати першим.

## Опис

Тиша при справному відтворенні досягається двома шляхами, і застосунок бачить лише один.

**Перемикач** — кнопка плеєра або `Ctrl+M`. Стан живе в `$muteState.muted`, кнопка
міняє підпис і `aria-pressed`, `F9` додає «звук вимкнено»
([playbackAnnounce.ts:129](../../../src/lib/playbackAnnounce.ts:129), ключ `f9_muted`).

**Рівень** — `Ctrl+Alt+Down` до дна або повзунок. `muted` лишається `false`, і далі
мовчать усі:

- кнопка показує «Вимкнути звук» — тобто стверджує, що звук увімкнено
  ([PlayerPanel.tsx:373](../../../src/components/player/PlayerPanel.tsx:373), значок від
  `isMuted`);
- `F9` про рівень не знає взагалі: `describePlayback` несе лише поле `muted`;
- вихід із стану не названий ніде.

Порівняння двох шляхів пояснює, чому цей бік важчий, а не легший:

| | перемикач | нульовий рівень |
|---|---|---|
| Досяжність випадкового натискання | лише при сфокусованому вікні | **глобальна клавіша**, з автоповтором ([shortcuts.rs:88](../../../src-tauri/src/shortcuts.rs:88)) |
| Живучість | гасне сама на `stopped` і на зміні джерела ([muteCleanup.ts](../../../src/lib/muteCleanup.ts)) | не гасне; рівень — сесійне поле профілю ([app_state.rs:120](../../../src-tauri/src/app_state.rs:120)), тож **переживає перезапуск** |
| Названий у відповіді `F9` | так | ні |

### Найгірше — не мовчання, а відповідь

`Ctrl+M` при нульовій гучності сьогодні поводиться так: `toggleMute` бачить `!muted`,
бере `restoreTo = FALLBACK_VOLUME` (бо `volume === 0`), ставить гучність у нуль — вона
й так нуль, — і оголошує **«Звук вимкнено»** вже беззвучному плеєру
([muteControl.ts:37-43](../../../src/lib/muteControl.ts:37)). Звук повертає лише **друге**
натискання, одразу на 75%.

Тобто контрол не просто не допомагає — він підтверджує стан, у який щойно нібито
перейшов, хоча нічого не змінив.

## Рішення

Стани лишаються окремі; об'єднуються **поверхня** і **вихід** — модель YouTube/video.js.
Повне обґрунтування й відкинута альтернатива (звести `muted` і `volume` в один стан) —
в [ADR](../../decisions/2026-08-16-silence-is-mute-or-zero-volume.md).

1. **Предикат тиші.** Одна функція — «звук вимкнено» = `muted || volume === 0` — живить
   значок і підпис кнопки, `aria-pressed` і клаузу `f9_muted`. Нових текстів не
   потрібно: наявні `player_muted` / `player_unmuted` / `f9_muted` описують стан, а не
   спосіб входу в нього.
2. **Вихід через `Ctrl+M`.** При `volume === 0 && !muted` натискання **піднімає**
   гучність до останнього ненульового значення, а не «вимикає звук» ще раз.
3. **Пам'ять рівня.** «Останнє ненульове» живиться з **обох** шляхів — і з явного
   перемикача, і зі зміни рівня, що дійшла до нуля.
4. **`FALLBACK_VOLUME` звужується, а не зникає.** Холодний старт із нулем у профілі
   лишає пам'ять порожньою: гучність переживає перезапуск, `$muteState` — ні. Константа
   стає явним запасним рівнем саме для цього випадку.
5. **`muteCleanup.ts` не чіпається.** Обидві його гілки спрацьовують лише при
   `muted === true`, тож нульовий рівень і далі переживає зупинку та зміну станції —
   що для рівня є правильною поведінкою.

## Критерії готовності

- [x] Зведення гучності в нуль (клавішею й повзунком) перемикає кнопку плеєра в стан
      «вимкнено»: значок, підпис, `aria-pressed`
- [x] `F9` при нульовій гучності називає стан тією самою клаузою, що й при перемикачі
- [x] `Ctrl+M` при нульовій гучності **піднімає** звук до останнього ненульового рівня
      й оголошує «Звук увімкнено» — жодного «Звук вимкнено» беззвучному плеєру
- [x] Перетягування повзунка з `60%` одразу в нуль і повернення через `Ctrl+M` дає `60%`.
      Уточнення реалізації: пам'ять — це **останній ненульовий рівень із подій
      `player-status`**, тож покроковий спуск (`Ctrl+Alt+Down`, крок 5%) повертає `5%`,
      а не `60%` — стрибок і спуск сходами дають різні відповіді за визначенням
- [x] Холодний старт із нульовою гучністю в профілі: стан названий, `Ctrl+M` дає
      `FALLBACK_VOLUME`
- [x] Підняття гучності при `muted === true` і далі знімає стан (наявна гілка
      `muteCleanup.ts:22` не зламана)
- [x] Явний `Ctrl+M` при звичайній гучності поводиться як раніше, включно з
      відновленням на зупинці й на зміні джерела
- [x] `CONTEXT.md`: додано термін «Вимкнений звук» (текст — нижче)
- [x] ADR [2026-08-16](../../decisions/2026-08-16-silence-is-mute-or-zero-volume.md) на місці
- [x] `pnpm test`, `pnpm vite:build` зелені
- [x] NVDA-прогін проведено 2026-08-17: усі 8 сценаріїв пройдено, зауважень немає.
      Чекліст (`docs/testing/nvda-zero-volume-reads-as-muted.md`) видалено на
      прийманні за конвенцією. Що він доводив понад автотести: що при нульовій
      гучності кнопка **читається** як натиснута, що `F9` вимовляє клаузу стану без
      розриву речення, і що `Ctrl+M` оголошує «Звук увімкнено» **після** того, як
      звук справді з'явився, а не до

## Текст для `CONTEXT.md`

Ставити поруч із «Сесійні поля». Формулювання узгоджене, переобговоренню не підлягає:

> ## Вимкнений звук
>
> **Вимкнений звук** — стан виводу, у якому справне відтворення не дає звуку. Шляхів
> у нього два: перемикач (кнопка плеєра, `Ctrl+M`) і зведення рівня гучності в нуль.
> Для користувача це **один** стан: той самий індикатор, та сама відповідь на запит
> «що зараз грає», той самий вихід. Яким саме шляхом у нього потрапили — не значить
> нічого.
>
> Не плутати з **рівнем гучності**: рівень — сесійне поле профілю й переживає
> перезапуск, вимкнений звук перемикачем — ні.

## Поза скоупом

- **Персистити «останнє ненульове» в `player_session`.** Прибрало б холодний випадок
  замість запасного рівня, але коштує поля в агрегаті профілю з усім, що за ним іде
  (знімок, коміт, сумісність наявних файлів). Піднімати, лише якщо холодний старт із
  нулем виявиться частим.
- **Видимий відгук на самі клавіші.** `Ctrl+M` і `F9` й після цієї зміни пишуть лише в
  `sr-only`; це окремий запис
  [sound-hotkeys-feedback-announce-only](../p2-sound-hotkeys-feedback-announce-only.md).
- **Об'єднання станів `muted` і `volume`.** Розглянуто й відкинуто — див. «Відхилені
  варіанти» в ADR. Не піднімати без нових аргументів.

## Документи

- [ADR 2026-08-16](../../decisions/2026-08-16-silence-is-mute-or-zero-volume.md) — модель і відхилені варіанти
- [help-muted-playback-symptom](p2-help-muted-playback-symptom.md) — позиція довідки, з
  якої знайдено; залежності між записами немає свідомо
- [videojs/video.js#3909](https://github.com/videojs/video.js/issues/3909) — той самий
  баг у чужій кодовій базі й еталонна поведінка YouTube
- Код: `src/lib/muteControl.ts`, `src/lib/muteCleanup.ts`, `src/lib/playbackAnnounce.ts`,
  `src/hooks/useGlobalShortcuts.ts`, `src/components/player/PlayerPanel.tsx`,
  `src/components/player/VolumeSlider.tsx`
