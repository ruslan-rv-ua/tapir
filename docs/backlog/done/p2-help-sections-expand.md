---
slug: help-sections-expand
title: "Довідка: дописати всі 14 розділів до нової межі й увімкнути сторожів"
priority: P2
type: planned
status: done
effort: L
kind: chore
target: 0.2.0
updated: 2026-09-05
completed: 2026-09-05
a11y: false
depends_on:
  - help-word-floor
blocks: []
touches:
  - docs/help/uk/
  - docs/help/en/
  - build/helpContent.test.ts
gates:
  - pnpm test
  - pnpm vite:build
notes:
  - "28 файлів: чотирнадцять розділів × дві локалі. Парність імен стереже тест, парність ЗМІСТУ тепер стереже відносний поріг uk ≥ 0.75 × en."
  - "Розбіжність «специфікація обіцяє 600, тест стереже 120» закрита останнім кроком: FLOOR = 600, CEILING = 3000, uk ≥ 0.75 × en. Обидва нові пороги перевірені підсадженою помилкою — обрізаним en і обрізаним uk — перед злиттям."
  - "Запис не розколювався: письмо пішло однією сесією, по одному розділу за крок, у порядку критеріїв. Шестикутник help-intro / help-recording / … лишився запасним планом, який не знадобився."
  - "Письмо знайшло дві розбіжності з кодом, обидві виправлені на місці (секція «Відкрите свідомо» це й дозволяла): save_stream_file типово УВІМКНЕНЕ, а не вимкнене, як читався попередній текст recording.md; кнопка «Скинути фільтр» на Потоках з'являється лише коли фільтр не лишив жодного рядка, а не стоїть у панелі завжди."
---

# Довідка: дописати всі 14 розділів до нової межі й увімкнути сторожів

> **Контекст:** [`help-word-floor`](p2-help-word-floor.md) підняв межі правила 6 до
> 600–3000 слів і додав правило 9 («що саме купує обсяг»), але тексту не чіпав. Цей запис
> — власне письмо: усі чотирнадцять розділів обох локалей, і останнім кроком — перемикання
> сторожів на нову межу. Читати першим — правила 6 і 9 у
> [стиль-гайді](p1-help-content-polish.md).

## Опис

Довідка місцями незрозуміла — не тому, що бракує розділів, а тому, що в кожному з них
пояснення програло стислості. Дев'ять розділів не проходять нову нижню межу взагалі, ще
п'ять проходять, але писались під стелю 1000 і теж щільні.

Робота одна на всі чотирнадцять: довести розділ до **≥ 600 слів по `en`** так, щоб слова
пішли на речі з **правила 9**, а не на переказ. Правило 9 тут не цитується — воно живе в
стиль-гайді, і копія розійшлася б із оригіналом.

Українська пишеться **парою** до англійської в тому самому кроці. Не «переклад потім»:
`uk` — типова локаль (`getHelpHtml` падає на неї для будь-якої невідомої), тож розділ,
у якому англійська виросла, а українська лишилась старою, бачить більше людей, ніж
навпаки.

## Обсяги на 2026-09-05

Слів по `en` / по `uk`; дев'ять перших не проходять нижню межу.

| Розділ | `en` | `uk` | Нижня межа |
|--------|-----:|-----:|------------|
| `overview` | 200 | 184 | **+400** |
| `templates` | 352 | 318 | **+248** |
| `navigation` | 390 | 328 | **+210** |
| `recording` | 416 | 340 | **+184** |
| `streams` | 422 | 379 | **+178** |
| `songs` | 447 | 386 | **+153** |
| `profiles` | 451 | 398 | **+149** |
| `browser` | 552 | 456 | **+48** |
| `wishlist` | 599 | 541 | **+1** |
| `scheduling` | 634 | 553 | проходить |
| `settings` | 651 | 569 | проходить |
| `player` | 678 | 606 | проходить |
| `background` | 731 | 591 | проходить |
| `troubleshooting` | 999 | 833 | проходить, але **ужатий** |

`troubleshooting` — окремий випадок: він стояв упритул до старої стелі, і три записи
поспіль (`about-app-info`, `sound-hotkeys-feedback`, `volume-hotkey-no-feedback`)
дописували симптом, ужимаючи сусідній текст. Це єдиний файл із документованим слідом
**втраченого через межу змісту**; його не «дописують до межі» — його **розтискають**.

## Критерії готовності

Кожен розділ — обидві локалі в одному кроці, `en` ≥ 600 слів, зміст за правилом 9,
без води за його забороною.

- [x] `overview` — 200 → 722
- [x] `navigation` — 390 → 853
- [x] `profiles` — 451 → 725
- [x] `streams` — 422 → 824
- [x] `browser` — 552 → 720
- [x] `wishlist` — 599 → 717
- [x] `scheduling` — 634 → 757
- [x] `songs` — 447 → 633
- [x] `recording` — 416 → 796
- [x] `player` — 678 → 799
- [x] `templates` — 352 → 720
- [x] `settings` — 651 → 859
- [x] `background` — 731 → 868
- [x] `troubleshooting` — 999 → 1349: **розтиснуто**, не дописано. Три записи звірені; сліди
      ужимання знайшлись у чотирьох позиціях, і всі чотири були одним і тим самим —
      відповідь, згорнута в ланцюжок через крапку з комою. Розтиснуті: зайнята гаряча
      клавіша (`hotkey-registration-silent-at-startup` — вона ж дістала дію, якої не мала:
      відкриття вкладки «Гарячі клавіші» перевіряє комбінації заново й знімає позначку без
      перезапуску), ескалація в журнал (`about-app-info` — адреса проєкту була втиснута в
      дужки), файл, що не відкрився, і аварійне завершення. Позиція про низький рівень
      гучності (`volume-hotkey-no-feedback`) сусідів не ужимала — перевірено, лишена як є
- [x] Жоден розділ не перетнув 3000 слів; найдовший — `troubleshooting` на 1349
- [x] **Останнім кроком** — ворота: у `build/helpContent.test.ts` знято `FLOOR = 120`,
      з'явились `en ≥ 600` і `uk ≥ 0.75 × en` для того самого розділу; антизаглушковий
      коментар переїхав до відносного порогу, бо тепер заглушку ловить саме він
- [x] Датований рядок у `notes` стиль-гайду: секція «Захист від старіння» описує сторожів
      як **чинні**, а не майбутні
- [x] `pnpm test`, `pnpm vite:build`

### Знайдене під час письма

Секція «Відкрите свідомо» дозволяла дописувати знайдене в критерії цього запису, а не
заводити новим. Скористались двічі, обидва рази — розбіжність тексту з кодом, а не
неясність формулювання:

- **`recording.md` читався так, ніби суцільний файл ефіру типово вимкнений.** Насправді
  `save_stream_file` типово `true` ([profile.rs:225](../../../src-tauri/src/profile.rs:225)),
  тобто довідка обіцяла свіжому Tapir поведінку, якої в нього немає, — рівно та вада, від
  якої стоїть правило 7. Виправлено разом із рештою фільтрів треків: усі чотири названі з
  дефолтами.
- **`streams.md` подавав «Скинути фільтр» як постійну кнопку панелі.** Вона з'являється
  тільки тоді, коли фільтр не лишив жодного рядка, і стоїть на місці порожнього списку
  ([StreamsPanel.tsx:710](../../../src/components/streams/StreamsPanel.tsx:710)); повертає
  повний список кнопка **Усі**.

## Документи

- [help-word-floor](p2-help-word-floor.md) — межі, правило 9 і всі рішення грилінгу
- [help-content-polish](p1-help-content-polish.md) — стиль-гайд: правила 6 і 9,
  мапа покриття «фіча → файл», конвенції розмітки
- шляхи коду: `docs/help/{uk,en}/*.md`, `build/helpContent.test.ts`
