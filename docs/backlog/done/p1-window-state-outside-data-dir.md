---
slug: window-state-outside-data-dir
title: "Геометрія вікна пишеться в AppData замість data/"
summary: "Геометрія в `data/window.json`, плагін знято; ADR: новий файл поза `data/` — дефект; позиція зовнішня, розмір внутрішній; один запис за сеанс."
priority: P1
type: planned
status: done
effort: M
kind: bug
target: 0.1.0
updated: 2026-09-04
completed: 2026-09-04
a11y: true
depends_on: []
blocks: [tech-stack-doc-drift]
touches:
  - src-tauri/src/window_state.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/app_state.rs
  - src-tauri/src/portable.rs
  - src-tauri/Cargo.toml
  - src-tauri/capabilities/default.json
  - README.md
  - DEVELOPERS.md
  - docs/architecture.md
gates: [cargo test, cargo clippy, pnpm test, pnpm vite:build]
notes:
  - "Огрилено 2026-09-04 (11 розвилок). Межа портативності винесена в ADR 2026-09-04-portable-boundary."
  - "tauri-plugin-window-state 2.4.1 пише лише в app_config_dir; опції каталогу немає, PR with_dir відкрито з 2025-10, мейнтейнер відклав на v3."
  - 'Профіль WebView2 (%LOCALAPPDATA%\ua.ruslanrv.tapir\EBWebView, 123 МБ) свідомо лишається в AppData — перенесення вимагає створювати головне вікно з коду й переписувати стартову послідовність. Рішення й ціна — в ADR.'
  - "a11y: true, бо застосування геометрії стоїть у стартовій послідовності перед show → set_focus, від якої залежить, чи NVDA озвучує вікно при запуску."
---

# Геометрія вікна пишеться в AppData замість data/

> **Контекст:** знахідка аудиту 2026-09-04, огрилено того ж дня. Рішення ухвалені,
> запис готовий до реалізації. Правило, за яким одні файли переїжджають у `data\`, а
> інші лишаються в AppData, живе в [ADR про межу портативності](../../decisions/2026-09-04-portable-boundary.md) —
> читати перед роботою.

## Опис

Розмір і позицію головного вікна зберігає `tauri-plugin-window-state`. Він будує шлях
як `app_config_dir().join(".window-state.json")` і не має жодної опції каталогу: у
`Builder` є лише `with_filename`, `with_state_flags`, `with_denylist`, `with_filter`,
`skip_initial_state`, `map_label`. Upstream-PR з `with_dir()` відкрито 2025-10-01,
мейнтейнер відклав його на v3 — чекати не варіант.

Через це геометрія вікна — єдине рішення користувача, яке **не їде разом із
застосунком**. Tapir на флешці пам'ятає розмір вікна окремо на кожній машині, тоді як
налаштування, профілі, потоки й вішліст подорожують у `data\`. Це, а не чистота чужого
AppData, і є причиною правки: чистим AppData однаково не буде (див. ADR).

Плагін заразом тягне пастку, яка сьогодні не стріляє випадково. При
`StateFlags::all()` він зберігає поле `visible` і при відновленні **сам** викликає
`show()` + `set_focus()` — ще до `setup()`, бо вікна з конфігу створюються раніше
(`tauri-2.10.3/src/app.rs:2373`). Не стріляє лише тому, що `CloseRequested` завжди
робить `window.hide()` перед виходом, тож у файлі назавжди `visible: false`.

## Рішення

**Свій модуль `window_state.rs`, файл `data\window.json`, плагін знято.**

1. **Формат — п'ять полів:** `width`, `height`, `x`, `y` (завжди звичайного, не
   розгорнутого вікна) і `maximized`. Не зберігаємо `visible` (це рішення поточного
   запуску: `--minimize` і трей), `fullscreen` (режим
   [відхилено 2026-08-17](p3-window-fullscreen-f11.md)), `decorated` (константа,
   вимога NVDA mouse tracking) і пару запасних координат `prev_x`/`prev_y` (плагіну
   вони потрібні лише тому, що він не розрізняє переїзд від розгортання).
2. **Шлях — `portable::window_state_path()`**, окремим файлом, як
   `hotkeys-reported.json`: у `settings.json` це виглядало б як налаштування, якого
   немає на екрані, у `state.json` стало б чужим вантажем для писаря crash-recovery.
3. **Диск бачить один запис — при виході**, у `graceful_shutdown` (обидва шляхи виходу
   сходяться там). Нормальний прямокутник тримається в пам'яті й оновлюється на
   `Moved`/`Resized`, коли вікно не розгорнуте й не згорнуте, — інакше при виході з
   розгорнутого вікна не було б чого записати, крім координат розгорнутого.
   Вікно згорнуте в момент виходу — файл не перезаписуємо взагалі (Windows віддає для
   згорнутого вікна `-32000`).
4. **Правило видимості:** верхня смуга вікна мусить лежати в робочій області якогось
   монітора з достатнім перекриттям по горизонталі; інакше вікно центрується на
   головному моніторі. Розмір обрізається до робочої області обраного монітора.
   Перевірка плагіна («бодай один із чотирьох кутів у якомусь моніторі») цього не
   ловить: вікно, у якого видно лише нижній край, її проходить, а заголовок висить над
   екраном. Правило пишеться чистою функцією (прямокутник + список моніторів →
   прямокутник), щоб покрити тестами без справжнього вікна.
5. **Порядок старту не подовжується:** файл читається до `tauri::Builder`, поряд із
   налаштуваннями; у `setup()` прихованому вікну виставляються позиція, розмір і за
   потреби розгортання, і аж потім — `show()` та `set_focus()`, які лишаються
   останніми й сусідніми.
6. **Залишки в AppData не чіпаємо кодом.** Разова подія не варта коду, що житиме
   вічно; у DEVELOPERS.md — рядок для тих, хто користувався ранніми збірками.

Координати фізичні, як у плагіна: вікно, перенесене між моніторами з різним
масштабуванням, збереже фізичний розмір. Відома межа, не регресія.

## Критерії готовності

- [x] `docs/help/` — запис видимої поведінки не змінює; перевірити, що
      [settings.md](../../help/en/settings.md) і [background.md](../../help/en/background.md)
      не називають місць зберігання, які стають неправдою
- [x] Розмір, позиція і `maximized` відновлюються з `data\window.json`; після виходу
      застосунок не створює в `%APPDATA%` каталогу `ua.ruslanrv.tapir` заново
- [x] Вікно, збережене на моніторі, якого зараз немає, відкривається по центру
      головного монітора; вікно, більше за поточний екран, обрізається до робочої області
- [x] Стартова послідовність показу й фокусу вікна не змінена: NVDA озвучує головне
      вікно при запуску, як описано в
      [screenreader-startup-foreground.md](../../notes/screenreader-startup-foreground.md)
- [x] `tauri-plugin-window-state` прибрано з `Cargo.toml`, `lib.rs` і
      `capabilities/default.json`
- [x] README.md втратив обіцянку чистої системи й має одне чесне речення без жаргону;
      DEVELOPERS.md називає всі три сліди: кеш WebView2, `AppUserModelId`, `Run`
- [x] architecture.md більше не описує плагін як механізм збереження стану вікна
- [x] NVDA-прогін старту проведено 2026-09-04, усі 8 сценаріїв пройдено
      (озвучений старт, геометрія в `data/`, розгорнуте вікно, `--minimize`,
      координати поза екраном, п'ять перезапусків, вихід зі згорнутого вікна,
      згортання в трей) — зауважень немає
- [x] `cargo test`, `cargo clippy`, `pnpm test`, `pnpm vite:build` без помилок

## Спадок

Геометрія вікна (розмір, позиція, `maximized`) переїхала з
`%APPDATA%\ua.ruslanrv.tapir\.window-state.json` у `data/window.json`,
`tauri-plugin-window-state` знято з `Cargo.toml`, `lib.rs` і `capabilities`. Причина в записі
виявилась не тією, яку він називав: обіцянку README «AppData stays completely clean» ламав не
файл на 210 байтів, а **123 МБ профілю WebView2** у `%LOCALAPPDATA%`, про який у репозиторії не
було жодної згадки — Tauri підставляє цей шлях примусово (`manager/webview.rs:508`), а конфігове
поле `dataDirectory` не рятує, бо примусово трактується як відносне до `data_local_dir`
(`webview/mod.rs:413`). Перенести кеш можна лише через `WebviewWindowBuilder::data_directory()`,
тобто `"create": false` і створення головного вікна з коду — ціною переписаної стартової
послідовності, від якої залежить, чи заговорить NVDA. **Відхилено свідомо**; правило й винятки —
[ADR про межу портативності](../../decisions/2026-09-04-portable-boundary.md): у `data/` те, що
вирішує Tapir, поза ним лише кеш WebView2, `AppUserModelId` і `Run`, а будь-який **новий** файл
поза `data/` — дефект, а не ще один виняток. Тому й причина правки інша: геометрія — рішення
користувача, і воно має їхати з флешкою, а не лишатись на чужій машині. Спадок — чотири речі.
**Плагін сам показував вікно**: вікна з конфігу створюються **до** `setup()`
(`tauri/src/app.rs:2373`), а при `StateFlags::all()` плагін кликав `show()` + `set_focus()`,
якщо у файлі `visible: true`; не стріляло лише тому, що `CloseRequested` завжди робить `hide()`
перед виходом. Свій формат поля `visible` не має взагалі, як не має `fullscreen` (режим
відхилено 2026-08-17) і `decorated` (константа NVDA mouse tracking). **Позиція зовнішня, розмір
внутрішній** — саме так їх віддає й приймає Tauri; змішати означає ростити вікно на товщину
рамки з кожним запуском, і сторожем цього став окремий сценарій прогону (п'ять перезапусків
поспіль). **Розміщення питає про досяжність заголовка**, а не «бодай один із чотирьох кутів у
якомусь моніторі», як плагін: вікно, що висить над екраном, ту перевірку проходило, а заголовок
лишався поза межами; коли жоден монітор не годиться — центрування на головному, розмір завжди
обрізається до робочої області, і все це чистою функцією над прямокутниками (11 тестів без
справжнього вікна). **Диск бачить один запис за сеанс** — із `graceful_shutdown`, куди сходяться
обидва шляхи виходу; звичайний прямокутник тримається в пам'яті на `Moved`/`Resized`, бо сеанс,
що закінчився розгорнутим, інакше не мав би що записати, крім координат розгорнутого, а згорнуте
вікно не пишеться зовсім (Windows описує його координатами `-32000`). Залишки в AppData кодом не
чистяться — рішення розробника, натомість рядок у DEVELOPERS.md. README втратив обіцянку чистої
системи заради одного чесного речення, DEVELOPERS.md дістав секцію «What Tapir Leaves Behind» з
усіма трьома слідами. **NVDA-прогін проведено 2026-09-04, усі 8 сценаріїв пройдено, зауважень
немає**

## Документи

- [ADR: межа портативності](../../decisions/2026-09-04-portable-boundary.md) — що живе в `data\`, а що ні, і чому `EBWebView` лишається
- [portable.rs](../../../src-tauri/src/portable.rs) — усі шляхи `data/`
- [store.rs](../../../src-tauri/src/store.rs) — `write_json_atomically`
- [hotkey_busy.rs](../../../src-tauri/src/hotkey_busy.rs) — зразок маленького файлу під `data/`
- [lib.rs](../../../src-tauri/src/lib.rs) — стартова послідовність
- [screenreader-startup-foreground.md](../../notes/screenreader-startup-foreground.md) — чому старт чутливий до NVDA
- upstream: https://github.com/tauri-apps/plugins-workspace/issues/3020 і PR https://github.com/tauri-apps/plugins-workspace/pull/3022
