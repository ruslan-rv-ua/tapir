---
slug: recording-stats
title: "Статистика запису — скільки записано, топ станцій"
priority: P3
type: idea
status: draft
effort: S
kind: feature
target: 0.3.0
updated: 2026-09-24
a11y: true
depends_on: []
blocks: []
touches: [src-tauri/src/profile.rs, src-tauri/src/commands/songs_commands.rs, src/components/settings, src/components/songs]
gates: [pnpm test, cargo test]
notes:
  - "Панель «над / під списком треків» упирається в ADR 2026-09-08 «Екран не повторює власне число»: числа в ній мають бути власними (сумарний час, топ станцій), а не повтором того, що вже стоїть на екрані пісень. Прочитати ADR перед вибором форми."
  - "Звірка 2026-09-24: типу `SavedSong` у коді немає; ті самі дані несе `Song` (`src-tauri/src/songs/mod.rs`, `src/types/song.ts`) — поля `duration_ms` і `station`. Текст запису виправлено."
---

# Статистика запису — скільки записано, топ станцій

> **Контекст:** Користувач хоче знати, скільки всього він записував і які станції
> використовує найчастіше. Дані вже частково є у `Song` (тривалість, станція).

## Опис

Відображення агрегованої статистики на основі збережених пісень:

- **Загальний час запису** — сума тривалостей усіх збережених файлів
- **Топ-3 (або топ-5) станцій** по загальному часу запису
- **Кількість треків** всього і за поточний профіль
- **Статистика за período** — за поточний день / тиждень / місяць (опціонально)

Відображення — у SettingsDialog (секція «Статистика» або «Загальні») або як окрема
панель у SongsPanel (над / під списком треків).

## Критерії готовності

- [ ] Підрахунок загального часу з `Song.duration_ms` (або `files` у recordings/)
- [ ] Топ-3 станцій за загальним часом запису (з `Song.station`)
- [ ] Відображення у UI з доступними мітками (aria-label на кожному рядку)
- [ ] NVDA: статистика читається послідовно (list або group)
- [ ] Статистика per-profile (не глобальна)

## Відкриті питання

- Де зберігати агреговані дані: обчислювати on-the-fly з `Song` при кожному відкритті
  (простіше, але повільніше при великій бібліотеці) або кешувати в профілі?
- Чи потрібна статистика за período (день/тиждень/місяць), чи достатньо «всього»?
