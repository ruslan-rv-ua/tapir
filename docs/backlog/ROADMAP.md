# ROADMAP беклогу

Навігаційна карта записів [`docs/backlog/`](README.md), згрупована за `target`
(semver-версією з front-matter). **Front-matter кожного запису — першоджерело**;
цей файл підтримується вручну й може дрейфувати — за розбіжності вір файлу запису.

> Це не той самий roadmap, що [`docs/implementation-phases.md`](../implementation-phases.md)
> (офіційний фазовий roadmap застосунку). Цей файл впорядковує **беклог** —
> те, що ще не стало фазою — за версією, яку розробник планує його зробити.

Посилання — за **slug**, стабільні (не нумерація `#N`). Секції йдуть за зростанням
semver; `unscheduled` — наприкінці.

---

## v0.1.0

> **Тема — чесний інтерфейс.** Усе вже побудоване доводиться до стану, у якому воно
> не бреше й не мовчить: мертві налаштування прибрані, мовчазні відмови дістають видиму
> поверхню, хибні мітки виправлені, обидві локалі повні, масштаб інтерфейсу працює.
> Нових можливостей версія не додає — три записи типу `feature` тут закривають
> прогалини, а не розширюють функціонал. 7 записів: 6 × S, 1 × M.

Порядок рядків у секціях — за пріоритетом, а в межах пріоритету **більші першими**:
саме вони визначають, чи версія закриється, тож беруться раніше за дрібні.

| Slug | P | Тип | Стан | Зусилля | Залежить від | Розблоковує |
|------|---|-----|------|---------|---------------|-------------|
| [webview-zoom-hotkeys](p1-webview-zoom-hotkeys.md) | P1 | planned | ready | S | — | — (прогалина доступності для слабозорих; перший крок — вимірювання, не правка) |
| [key-recorder-bare-key](p1-key-recorder-bare-key.md) | P1 | planned | ready | S | [key-recorder-silent-rejection](done/p1-key-recorder-silent-rejection.md) ✅ | — (гола літера реєструється системною комбінацією; знахідка grilling 2026-09-02) |
| [preview-player-presentation](p2-preview-player-presentation.md) | P2 | planned | **draft** | S | — | — (плеєр пропонує поставити ефір на паузу; сусід [player-recording-badge-term](done/p2-player-recording-badge-term.md) ✅ — обидва про позначки джерела) |
| [reconnect-max-in-status](p2-reconnect-max-in-status.md) | P2 | planned | ready | S | [reconnect-zero-retries](done/p0-reconnect-zero-retries.md) | — («спроба N з M» бере N і M з двох різних джерел) |
| [list-key-modifier-guards](p2-list-key-modifier-guards.md) | P2 | planned | draft | S | [streams-transfer-hotkeys](done/p2-streams-transfer-hotkeys.md) | — (switch по `e.key` не перевіряє модифікаторів) |
| [browser-filter-cursor-reset](p2-browser-filter-cursor-reset.md) | P2 | planned | ready | S | — | — (погоджено 2026-07-23) |
| [accessibility-doc-audit](p2-accessibility-doc-audit.md) | P2 | planned | ready | M | — | — (підпирає кожен майбутній NVDA-чекліст: документ зараз суперечить сам собі) |

## v0.2.0

> **Тема — фічі з готовим дизайном.** Записи, де рішення вже ухвалені (`type: planned`)
> і лишилася реалізація, включно з останньою незакритою фазою застосунку —
> **3H Post-processing**. 9 записів: 5 × S, 3 × M, 1 × L.

| Slug | P | Тип | Стан | Зусилля | Залежить від | Розблоковує |
|------|---|-----|------|---------|---------------|-------------|
| [post-processing](p1-post-processing.md) | P1 | planned | ready | **L** | [profile-scoped-settings](done/p0-profile-scoped-settings.md) | — (остання незакрита фаза **3H**; оживляє вкладку-заглушку в діалозі профілю) |
| [per-stream-ignorelist-ui](p1-per-stream-ignorelist-ui.md) | P1 | planned | draft | M | — | — (бекенд уже працює, наповнити список з інтерфейсу неможливо; довідка описує це як дірку) |
| [command-palette-phase-3](p1-command-palette-phase-3.md) | P1 | planned | ready | S | — | [command-palette-phase-4](p2-command-palette-phase-4.md), [command-palette-mode-prefixes](p3-command-palette-mode-prefixes.md) |
| [import-duplicate-metadata-update](p2-import-duplicate-metadata-update.md) | P2 | planned | ready | M | — | — (рішення ухвалено 2026-07-23) |
| [wishlist-match-tray-notification](p2-wishlist-match-tray-notification.md) | P2 | planned | ready | M | [wishlist-match-invisible](done/p1-wishlist-match-invisible.md) ✅ | — (четверта категорія `ToastKind`; слід, без якого гейт прапорцем був би заборонений, тепер є — журнал збігів) |
| [command-palette-phase-4](p2-command-palette-phase-4.md) | P2 | planned | **blocked** | S | [command-palette-phase-3](p1-command-palette-phase-3.md) | — |
| [autostart-notice-lost-when-minimized](p2-autostart-notice-lost-when-minimized.md) | P2 | planned | ready | S | [hotkey-registration-silent-at-startup](done/p1-hotkey-registration-silent-at-startup.md) | — (репліка про автозапуск переїжджає на гейт «перший показ вікна»; гейт витягти в спільний тип, не копіювати) |
| [focus-active-item-on-playback-start](p2-focus-active-item-on-playback-start.md) | P2 | idea | draft | S | — | — |
| [streams-list-search](p2-streams-list-search.md) | P2 | idea | draft | S | — | — (гачок уже стоїть: `ZoneEntry.focusSearch?()` із [search-focus-hotkey](done/p2-search-focus-hotkey.md); зникне репліка «на цьому екрані немає пошуку») |
| [transport-boundary-silent-in-background](p2-transport-boundary-silent-in-background.md) | P2 | planned | draft | S | [transport-skip-silent-failure](done/p1-transport-skip-silent-failure.md) ✅ | — (у вікні носій є — кнопки на межі `disabled`; у фоні натискання на краю списку не лишає сліду) |

## v0.3.0

> **Тема — комфорт.** Ідеї (`type: idea`) з ясною цінністю, але без ухваленого дизайну:
> кожну треба спершу обговорити (режим **ОБГОВОРЕННЯ** за алгоритмом
> [README](README.md#алгоритм-для-агента)), і лише потім планувати. 14 записів.

| Slug | P | Тип | Стан | Зусилля | Залежить від | Розблоковує |
|------|---|-----|------|---------|---------------|-------------|
| [sleep-timer](p2-sleep-timer.md) | P2 | idea | draft | S | — | — |
| [wishlist-separate-folder](p2-wishlist-separate-folder.md) | P2 | idea | draft | S | — | — |
| [focus-on-screen-open-option](p2-focus-on-screen-open-option.md) | P2 | idea | draft | S | — | — |
| [command-palette-fuzzy-search](p2-command-palette-fuzzy-search.md) | P2 | idea | draft | S | — | — |
| [command-palette-dual-language-search](p2-command-palette-dual-language-search.md) | P2 | idea | draft | S | — | — |
| [pause-recording](p2-pause-recording.md) | P2 | idea | draft | M | — | — |
| [stream-manual-reorder](p2-stream-manual-reorder.md) | P2 | idea | draft | M | — | — |
| [track-log-only-mode](p2-track-log-only-mode.md) | P2 | idea | draft | M | — | — |
| [wishlist-conditions](p2-wishlist-conditions.md) | P2 | idea | draft | L | — | — (перетинається з [post-processing](p1-post-processing.md) у частині «дії після запису» — звіряти після 0.2.0, інакше два механізми на одну задачу) |
| [recording-stats](p3-recording-stats.md) | P3 | idea | draft | S | — | — |
| [player-station-image](p3-player-station-image.md) | P3 | idea | draft | S | — | — |
| [context-menu-at-cursor](p3-context-menu-at-cursor.md) | P3 | idea | draft | S | — | — |
| [lastfm-scrobbling](p3-lastfm-scrobbling.md) | P3 | idea | draft | M | — | — |
| [diagnostic-report-block](p3-diagnostic-report-block.md) | P3 | idea | draft | S | [about-app-info](done/p1-about-app-info.md) | — (хвіст about-app-info: збірка Windows, версія WebView2, кнопка «Скопіювати відомості для звіту») |

## unscheduled

> **Номера свідомо немає.** Тут лишаються дослідження (`type: research`), спірні ідеї,
> чиї «Відкриті питання» ставлять під сумнів саму пропозицію, і тригер-gated записи —
> ті, до яких повертаються лише за реальним приводом, а не за планом. 7 записів
> (2026-08-17 троє знято рішенням розробника — див. «Виконано»).
> Найперше рішення для цієї секції — **gate A4** ([mpv-playback-engine](p3-mpv-playback-engine.md)):
> його «go» закриває два інші записи разом.

| Slug | P | Тип | Стан | Зусилля | Залежить від | Розблоковує |
|------|---|-----|------|---------|---------------|-------------|
| [mpv-playback-engine](p3-mpv-playback-engine.md) | P3 | research | draft | L | — | [he-aac-mf-playback](p3-he-aac-mf-playback.md), [hls-stream-support](p3-hls-stream-support.md) (розвилка A4 — може закрити обидва) |
| [he-aac-mf-playback](p3-he-aac-mf-playback.md) | P3 | research | draft | M | — | залежить від рішення mpv-playback-engine |
| [hls-stream-support](p3-hls-stream-support.md) | P3 | idea | draft | L | — | залежить від рішення mpv-playback-engine |
| [stream-auth](p3-stream-auth.md) | P3 | research | draft | L | — | брати лише за реальною потребою (станція з платним/приватним mountpoint) |
| [command-palette-taxonomy](p3-command-palette-taxonomy.md) | P3 | idea | draft | M | — | спірна — «Відкриті питання» ставлять під сумнів саму суть пропозиції |
| [command-palette-mode-prefixes](p3-command-palette-mode-prefixes.md) | P3 | idea | draft | S | [command-palette-phase-3](p1-command-palette-phase-3.md) | спірна — українська розкладка без `>`/`@` |
| [profile-switch-orphaned-tasks](p3-profile-switch-orphaned-tasks.md) | P3 | idea | draft | M | — | **умовний** — лише за реальним тригером (незафіналізовані файли після перемикання профілю) |

---

## Виконано

| Запис | Коли | Що лишилось у спадок |
|-------|------|----------------------|
| [player-recording-badge-term](done/p2-player-recording-badge-term.md) | 2026-09-02 | Значок джерела в плеєрі перестав уживати «Запис» у значенні файлу: поруч із `LIVE` тепер стоїть **«Файл»** / "File" — пара за тією ж віссю «звідки звук», а не натяк, що саме цей файл зараз пишеться. Ключ `player_recording_badge` і компонент `RecordingBadge` не перейменовано — брехало значення, не ключ. Довідка `player.md` цитує нову мітку дослівно (конвенція розмітки 5), а пояснення пішло через множину — «один із ваших записів», тобто саме те значення, яке `CONTEXT.md` відводить збереженим файлам; сам `CONTEXT.md` не змінювався, суперечність зникла з боку мітки. Перевірено, що інших джерел файлу в плеєра немає (`--play` у CLI приймає лише станцію, `playFile` з фронтенду ніхто не кличе). Спадок — сторож `src/i18n/recordingBadge.test.ts`: читає обидві локалі з JSON і тримає, що значок не повторює кореня мітки вкладки «Запис» (`settings_tab_recording`) — корінь береться з тієї мітки, що справді про процес, тож тест чесний щодо того, яке слово забороняє; чого він не тримає, каже сам — що «Файл» правильне слово, несе довідка. Мапу покриття в [help-content-polish](done/p1-help-content-polish.md) оновлено. **NVDA-прогін не потрібен** (`a11y: false`) — змінився текст однієї мітки |
| [settings-labels-wording](done/p2-settings-labels-wording.md) | 2026-09-02 | Прапорець холодного старту перестав обіцяти різне двома мовами: uk-мітка «Автовідтворення при запуску» (натяк, що на старті просто щось почне грати) стала **«Відновлювати останнє відтворення при запуску»** — тим самим дієсловом, що й сусіднє **Відновлювати файл** рядком нижче, тож обидві мітки вкладки «Відтворення» читаються як одне рішення: «чи відновлювати» і «звідки». Англійська була точна й не змінилась; ключ `profile_autoplay_label` не перейменовано — запис коштував рядка в `messages/`, не рефакторингу. Друга половина запису (латиниця `tray` посеред українського рядка) **полагодилась сама** 2026-08-17 разом із [tray-notifications-label-scope](done/p1-tray-notifications-label-scope.md); до реалізації дожили лише дві прострочені цитати старого тексту в довідниках для розробників — дерево діалогу в `accessibility.md` і коментар до поля в `data-models.md`. Спадок — **сторож форми пари** `src/i18n/autoplayLabel.test.ts`: чекбокс мусить відкриватись тим самим словом, що й поле під ним, тож перепис форми дієслова зачепить обидві мітки. Чого сторож не тримає, він каже про себе сам: **що саме** відновлюється, рядковим тестом не перевіряється — цю половину несуть підпис під чекбоксом і `CONTEXT.md`, де «автовідтворення» лишилось терміном домену, а мітка інтерфейсу тепер інша. Рев'ю після реалізації зняло дві правки в довідці: `profiles.md` перемикача не називає (шов help-content-polish — «тут лише "окремо для кожного профілю"»), а uk-`player.md` не втрачає клаузи, яку англійська тримає. **NVDA-прогін не потрібен** (`a11y: false`) — нової структури не з'явилось, змінився текст однієї мітки |
| [wishlist-pattern-hint-anchoring](done/p1-wishlist-pattern-hint-anchoring.md) | 2026-09-02 | Підказка під полем патерна перестала перелічувати спецсимволи й замовчувати правило, заради якого вони потрібні: `pattern_hint` в обох локалях тепер веде якірністю і парою «працює / не працює» — «Патерн має збігтися з усією назвою: Tycho* працює, Tycho — ні». Дієслово й приклад узято з `docs/help/*/wishlist.md`, тож **довідка змін не потребувала** — брехала лише підказка, і це той рідкісний випадок, коли правило AGENTS.md про `docs/help/` законно незастосовне. `AddPatternDialog.tsx` теж не змінювався: підказка там у звичайному `<p>`, що переносить рядки. Верстку **виміряно на зібраному CSS**, а не на око — у діалозі рівно два рядки в обох локалях, у порожньому стані один рядок від 640 px і два нижче. Спадок — два сторожі проти повернення: `src/i18n/patternHint.test.ts` читає **обидві** локалі прямо з JSON (paraglide компілює одну на прогін, а розійшлись обидві) і тримає **форму**, не ім'я виконавця — щось із `*`, воно ж голе окремим словом, і обидва перед поясненням символів; `matcher.rs::anchored_to_the_whole_string_not_a_substring` привʼязує обіцянку підказки до поведінки матчера, тож підрядковий збіг колись зробить брехуном тест, а не користувача. **Ревʼю після реалізації** знайшло дві діри в самому сторожі: `/Tycho(?!\*)/` був слабший за власне повідомлення (проходив `Tychonaut`, тобто пара не була закріплена), а `node:fs` виявився самозваним приводом для полиці `build/` — за `resolveJsonModule` тест живе в `src/i18n/` і потрапляє під `tsc`. Обидві полагоджено там же, сторожа перевірено мутаціями. **NVDA-прогін не потрібен** (`a11y: false`), ручну перевірку в застосунку пройдено |
| [profile-delete-key-dead-end](done/p1-profile-delete-key-dead-end.md) | 2026-09-02 | Четвертий шлях видалення профілю нарешті гейтиться там само, де три інші: `Delete` на рядку `Default` чи активного профілю більше не відкриває підтвердження незворотної дії, щоб уже ПІСЛЯ згоди віддати сире англійське `Forbidden: Cannot delete 'Default' profile`. Усі одиночні шляхи звів у `ProfilesPanel.handleRequestDelete`, правило живе в `src/lib/profileDelete.ts` (`profileDeleteRefusal` — причина або `null`; `profileDeleteErrorMessage` — мапа `Forbidden:` на той самий текст, з загальним запасним варіантом). `Default` перевіряється **перед** активним: він зазвичай і є активним, а «спершу перемкніться на інший» там було б неправдою. Носій відмови — тост у `role="log"`, тобто видно й чути одним механізмом (той самий вибір, що в `shellOpenError` і `playRefusal`); тип `"warning"`, як у `useHotkeyBusyFeedback` — відмова не є збоєм. **Ревʼю знайшло, що передумова запису хибна**: масове видалення «дає зразок» лише для активного профілю, а `Default`, коли він не активний, пропускався мовчки — «Видалено профілів: 0» без причини. Тому `partition_deletable_profiles` дістав третій кошик і `BulkDeleteProfiles` — поле `skipped_default` з хвостом `bulk_skipped_default`. Свідомо **не** зведено в єдиного власника правила: той самий предикат гейтить ще й перейменування, і збіг тут випадковий. Довідку не чіпали — `profiles.md` в обох локалях уже точна |
| [key-recorder-silent-rejection](done/p1-key-recorder-silent-rejection.md) | 2026-09-02 | Третя відмова рекордера гарячих клавіш перестала бути тишею: непридатна клавіша (пунктуація, `Enter`, numpad) дістає текст у тому самому `role="alert"`, що резерв і дублікат, — «Ця клавіша не підходить: потрібна літера, цифра, стрілка, Space, Pause або F-клавіша», — і поле **виходить із режиму запису**, бо саме вихід гарантує, що друга відмова поспіль чутна (той самий текст у тому самому вузлі `alert` мовчить). Grilling (9 рішень) розвів дві родини клавіш в озброєному стані: «вийти» (`Escape`, голий `Tab`/`Shift+Tab` без `preventDefault`, втрата фокуса) і «відмова» (будь-яка інша немодифікаторна клавіша; самі модифікатори мовчать, бо з них починається кожна комбінація). Дві знахідки понад опис: react-aria `Button` кличе `onPress` на **keyup** `Enter`/`Space`, тож після запису `Ctrl+Space` поле переозброювалось, а відмова на `Enter` стиралась би тієї ж миті — закрито реф-прапорцем «натискання спожито», що скидається в `onBlur`; і голе `Q` записується як **системна** комбінація — винесено в [key-recorder-bare-key](p1-key-recorder-bare-key.md). Звуження набору клавіш — наше, не бібліотеки (`global-hotkey 0.7.0` приймає `Enter`, `Home`, numpad, пунктуацію), і не мінялось. Підказка — один статичний рядок над вісьмома рядками вкладки, без `aria-describedby` (вісім повторів за прохід Tab). Словник дістав «Гарячу клавішу»: «гаряча клавіша» — дія, «комбінація» — клавіші, «хоткей» — лише в коді; чотири рядки `uk.json` переписано. Рев'ю двома осями додало: `Ctrl+Tab` — спроба, не навігація; `e.stopPropagation()` на події `useKeyboard` — no-op із `console.error`, прибрано. NVDA-прогін 8/8 без розбіжностей |
| [hotkey-registration-silent-at-startup](done/p1-hotkey-registration-silent-at-startup.md) | 2026-09-02 | Зайнята комбінація (термін у CONTEXT.md разом із «гарячою клавішею» і «комбінацією»; «хоткей» з UI прибрано) тепер має два носії: одноразову репліку при старті (polite + warning-тост, з назвою дії) і позначку «зайнято іншою програмою» в рядку вкладки, разом із `aria-label` кнопки; червоний блок під рядками прибрано. Три механізми, варті спадку. **Пам'ять `data/hotkeys-reported.json` фіксує доставлену репліку, не факт реєстрації** — перша версія помічала при реєстрації, і рев'ю (обидві осі незалежно) показало, що старт згорнутим без показу вікна робив конфлікт повідомленим назавжди; реєстрація лише забуває звільнене й перепризначене. **Гейт «перший показ вікна»** (`hotkey_busy::BusyNotice`: підписка вебв'ю ∧ фокус головного вікна, `Focused(true)` в `on_window_event`) — репліка автозапуску має ту саму діру і переїде на нього ([autostart-notice-lost-when-minimized](p2-autostart-notice-lost-when-minimized.md); гейт витягти, не копіювати). **Перереєстрація при відкритті вкладки** — єдиний спосіб дізнатися, що Windows звільнила комбінацію; нововиявлене вкладка оголошує ввічливо, вже відоме — мовчки. Окремої команди читання стану не знадобилось. Чекліст (8 сценаріїв; «чужу програму» дає PowerShell-скрипт із `RegisterHotKey`) виявив, що записати зайняту комбінацію рекордером неможливо — Windows віддає натискання власнику, — тож діалоговий шлях приймався через «Скинути до стандартних». `data-models.md` §9. Оцінку піднято S → M ще на grilling. NVDA-прогін 8/8 |
| [about-app-info](done/p1-about-app-info.md) | 2026-09-02 | Секція **Про програму** внизу вкладки «Загальні»: версія, адреса проєкту текстом і кнопка «Відкрити сторінку проєкту», що віддає адресу шелу — браузер за замовчуванням, webview лишається на місці. Grilling (9 питань) поправив сам запис: `useWebviewGuard` посилань не стереже (лише акселератори перезавантаження), версія вже жила в **трьох** файлах, а обидва механізми для адреси в коді були. Звідси рішення: адреса має **одну домівку** — `Cargo.toml` `homepage` (`PROJECT_HOMEPAGE = env!("CARGO_PKG_HOMEPAGE")`), команда `get_app_info` віддає версію (`package_info`, тобто конфіг Tauri) й адресу разом, а кнопка кличе **закриту команду без аргументів** `open_project_page` — у шел не потрапляє жоден рядок із фронтенду, загальної `open_url` свідомо немає. Адреса в застосунку — майбутня публічна `github.com/ruslan-rv-ua/tapir`, не поточний remote. Сторож трьох копій версії — `src/lib/versionSync.test.ts` (CLI `--version` читає Cargo, інтерфейс — конфіг Tauri; читає `version` саме з таблиці `[package]`). Клавіатура: версія й адреса — статичний текст (видимий носій), кнопка описана ними через `aria-describedby`, тож Tab у секції зупиняється раз і несе версію. Помилка шелу — **одне формулювання** на всі коди: адреса стала й на екрані над кнопкою, дія однакова. Довідка в двох місцях; `en/troubleshooting.md` тепер **999/1000 слів** — наступна правка мусить розщеплювати. Хвіст (збірка Windows, WebView2, «Скопіювати відомості для звіту») — [diagnostic-report-block](p3-diagnostic-report-block.md) у 0.3.0. NVDA-прогін 7/7 без розбіжностей |
| [transport-skip-silent-failure](done/p1-transport-skip-silent-failure.md) | 2026-09-01 | Єдиний L теми «чесний інтерфейс»: невдалий prev/next тепер повідомляє з кнопки, глобальної клавіші й SMTC; поверхню обирає фокус вікна (у фокусі — тост у вікні, поза ним — нативний `HotkeyFeedback`). Привіз [ADR «вухо, вікно, система»](../decisions/2026-09-01-response-surfaces-ear-window-system.md) — правило вибору поверхні, що описує всі п'ять випадків фонового відгуку, — четвертий канал «сам звук» у CONTEXT.md, `announce()` як функцію стора і вузьку команду `notify_transport_failure`. Розблокував [transport-boundary-silent-in-background](p2-transport-boundary-silent-in-background.md). |
| [volume-hotkey-no-feedback](done/p1-volume-hotkey-no-feedback.md) | 2026-08-31 | Дзеркальний бік ADR про видимий носій: правило одностороннє («промовлене мусить бути видимим»), тож видиме число гучності, яке привіз батьківський запис, саме по собі нічого озвучувати не зобов'язувало — і глобальна клавіша лишалась німою в обидві сторони. **Головний спадок — вибір каналу замість прапорців.** Рівень міняють **п'ять** шляхів, і два з них вебв'ю не бачить у принципі (перемикання профілю, старт сесії), тож «оголошувати на діф `player-status`» довелося б глушити в місцях, які нічого не знають про жест. Замість цього репліку шле **тільки** обробник клавіші — наявним `player-announce` з новим `kind: "volume"`; решта чотирьох шляхів мовчать самі собою, без жодного `suppress`-прапорця. Приклад того, як «хто саме має право говорити» дешевше за «хто має замовкнути». **Друге — точка емісії проти гонки.** Наївна гілка `Released` обганяє власну зміну: `Pressed` спавнить `apply_volume_change` асинхронно, тож на тапі 60–100 мс прозвучало б **попереднє** число. Емісія стоїть після виходу з циклу автоповтору, де рівень осів за побудовою; щоб одинарний тап не чекав усі 350 мс, початкова затримка ріжеться кроком повтору з перевіркою `held` на кожному зрізі — нової константи не з'явилось, лаг < 80 мс. Уся конструкція тримається на тому, що `set_volume` емітить `player-status` **синхронно до повернення**: саме це гарантує, що стан (і `muteCleanup`, який знімає `muted` при виході з нуля) доходить раніше за репліку. Юніт-тестом цей шов не закривається — він і був головним пунктом прогону. **Третє — `F9` дістала клаузулу про звук безумовно**, останньою: рівень, а на тиші старе «Звук вимкнено», ніколи обидва (`isSoundOff` тримає «вимкнено» і «нуль» одним станом). Тоста немає свідомо: при схованому вікні клавіша лишається німою — прийнята межа, оборотна кількома рядками. Прогін 11/11 чисто; відкрите питання «чи дублює NVDA на сфокусованому повзунку» закрите ним же — **не дублює**, тож придушення через `document.activeElement` не додавалось. Побічна знахідка ціною чекліста: типові комбінації гучності **не мігрували** — свіже встановлення має `Ctrl+Alt+↑/↓`, а зроблене до 2026-06-11 досі `Ctrl+Shift+↑/↓` (сторож `stored_volume_combos_are_not_migrated`), тож у довідці й чеклістах перепризначувану клавішу треба називати **дією**, а комбо лишати `settings.md` як єдиному власнику |
| [sound-hotkeys-feedback-announce-only](done/p1-sound-hotkeys-feedback-announce-only.md) | 2026-08-31 | Запис, що привіз [ADR про видимий носій](../decisions/2026-08-31-visible-carrier-for-announced-facts.md), і сам став першим його застосуванням. Спадок ділиться надвоє. **Перше — правило й обидва його напрямки.** Промовлений факт зобов'язаний мати на екрані носія, і лікується це **двома** ходами, не одним: числа повзунків дістали видимий текст, а два оголошення про збереження налаштувань (`settings_saved`, `profile_settings_saved`) **прибрано разом із ключами** — результат дії «зняв прапорець» це стан прапорця, і він видимий, а «записано на диск» внутрішня механіка, якою застосунок пообіцяв займатись сам. Половину «прибрати факт» варто пам'ятати окремо: інтуїція тягне лише додавати носіїв. **Друге — і найдорожче — передумова запису виявилась заниженою.** Запис стояв на тому, що числа «є, але лише в `aria-valuetext`, тобто для однієї модальності». Перший же компонентний тест показав інше: **react-aria-components відкидає проп `aria-valuetext` на `<SliderThumb>`** — `useSliderThumb` жорстко ставить свій із `state.getThumbValueLabel()`, а проп до прихованого `<input>` не доходить узагалі. Тобто числа не було **ніде** в правильному вигляді: скрінрідер читав позицію як сирі мілісекунди («135 000») і гучність як голе «45» без одиниці. Критерій «видимий текст і `aria-valuetext` з однієї змінної» без ремонту атрибута нездійсненний за побудовою, тож правка ширша за задуману. Обхідного шляху через `formatOptions` немає й не буде: `style: "percent"` на шкалі 0–100 дає «4 500%», а формату `m.time_format_min_sec` («2 хв 14 с») жоден `Intl` не має — і навіть робочий `{ style: "unit", unit: "percent" }` завів би **другий** форматер, тобто рівно той дрейф, від якого §6 і стоїть. Обидва атрибути, які RAC тримає на прихованому input (`tabIndex` і `aria-valuetext`), тепер патчить один [`useSliderThumbInput`](../../src/hooks/useSliderThumbInput.ts) — **точка входу для будь-якого нового повзунка**; `useLayoutEffect`, а не `useEffect`, бо позиція тикає щосекунди з бекенд-події, і пасивний ефект лишав би сире число відкритим до наступного кадру. Третє, дрібніше: `PlaybackPosition` при невідомій тривалості повертав `null` — позиції не було ні в якому вигляді; тепер число є, свідомо **без мітки** (мітка на неінтерактивному `<div>` усе одно ігнорується, а §4 ADR не вимагає озвучувати видиме). Побічна знахідка: розділи 4.3/4.4 `docs/accessibility.md` — **ескіз, а не опис коду** (їхній `formatOptions` дав би «4 500%», таблиці клавіш суперечать реалізації, ключі повідомлень у camelCase); чотири розбіжності виписано в [accessibility-doc-audit](p2-accessibility-doc-audit.md), окремо не відкривались. Прийнято з NVDA-прогоном, хоч запис і `a11y: false` — бо ремонт `aria-valuetext` виявився правкою саме для скрінрідера. Розблоковує [volume-hotkey-no-feedback](done/p1-volume-hotkey-no-feedback.md) |
| [wishlist-match-invisible](done/p1-wishlist-match-invisible.md) | 2026-08-31 | Вішліст обіцяв «повідомляти про трек» і в словнику, і в довідці, а робив рівно одну річ — клав текст у `sr-only`-регіон: для людини без скрінрідера він не робив **нічого** спостережуваного. Дзеркально, `track-ignored` промовляв **кожен** рекламний блок — теж без сліду на екрані. Спадок — **правило, а не вкладка** ([ADR про носії для подій станції](../decisions/2026-08-31-carriers-for-station-events.md)), і воно добудовує межу, яку сусідній ADR про видимий носій свідомо лишив відкритою (§3: події, яких людина не викликала, ним не керуються). Три речі варті запам'ятовування. Перша — **подія станції відповідає на три різні питання, і поверхні за ними закріплені**: «що сталося, поки мене не було» → носій-стан у вікні, «що відбувається зараз, коли вікна немає перед очима» → сповіщення в треї, «що щойно сталося, поки я тут» → оголошення; це не три способи сказати те саме, і одна подія може дати всі три. Друга — **носій-стан первинний, подія проєктується поверх нього**: порядок не стилістичний, бо [ADR 2026-08-17](../decisions/2026-08-17-tray-toast-categories.md) дозволяє гейтити тост прапорцем **лише** коли подія має інший слід, тож завести тост першим означало б завести його незнімним — саме тому залежність із [wishlist-match-tray-notification](p2-wishlist-match-tray-notification.md) спрямована так, а не навпаки. Третя — **межа між формами носія це обсяг, а не важливість**: рідкісне (одиниці за ніч) іде в хронологію, рутинне (десятки) дістає позначку на місці, і змішувати не можна в жоден бік — стрічка, куди зливають усе, стає фоном, а позначка не переживає наступної події. Звідси й найнеочевидніша половина запису: **рутинна подія, що дістала позначку, оголошення втрачає** — але тільки разом із появою позначки, ніколи окремо, інакше це не ремонт, а тиха втрата. Тому `track-ignored` прибрано **цілком** (подія, payload, підписка, ключ `announcement_track_ignored`), а не просто заглушено: носієм став `TrackInfo.ignored` → «(ігнорується)» **видимим текстом** сегмента треку, а не в `aria-label`. Реалізація: кільцевий буфер на 200 у [match_log.rs](../../src-tauri/src/wishlist/match_log.rs) всередині `AppState` — сесійний, на диск не йде, чиститься в `switch_profile`; профіль для цього не годиться **двічі** (пишеться цілком на кожному коміті й **експортується**, тобто історія прослуховування поїхала б у файлі, яким діляться). Подія `wishlist-match` тепер несе **весь рядок журналу** — той самий тип, що віддає `get_wishlist_matches`, тож живий рядок виходить точно таким, як після перечитування, і окремої події «журнал змінився» не існує. Журнал — **подій, а не здобичі**: дій над рядком немає, бо в мить збігу файлу ще може не бути (`emit_wishlist_match` спрацьовує на початку треку, файл з'явиться на `finalize_track` — і не завжди), а місце, де живуть файли, вже є і зветься «Записи». Grilling відкинув шість варіантів, і кожен зафіксовано в ADR, бо до кожного наступний читач потягнеться заново: мітка на збереженому файлі (оживити `SavedTrack.is_wishlist_match` — найдовговічніший слід, але список сканується з диска щоразу, тож це нова персистентна сутність); «останній збіг» на рядку патерна (відповідає на добре питання «чи правило ще працює», але ранкове питання — «що спіймалось», не «що спіймалось останнім»); лічильник на рядку потоку (помирає разом із зупинкою запису, тобто не доживає до ранку — а ранок і є сценарій); окрема секція в боковій панелі (казала б «самостійна частина застосунку», хоча без свого списку правил журнал порожній за побудовою); дві поверхні події за станом вікна (буквальна настанова Microsoft — але прапорець тоді або гейтить половину, або мітка зі словом «в треї» бреше); прив'язати оголошення до прапорця тостів (людина, що зняла прапорець заради тиші вночі, втратила б і денне оголошення). Побічно закрито найчастішу причину «вішліст не працює»: збіг перевіряється **тільки в `recording_task`**, і про це не було сказано ніде — тепер це єдине місце інтерфейсу, де сказано, живе в порожньому стані журналу, поруч зі словами «за цей сеанс», без яких він брехав би після перезапуску. Дві дрібні розвилки без прецеденту: `Ctrl+N` на вкладці журналу **перемикає** на «Бажані треки» й відкриває діалог там (мовчазна відмова була б гіршою, а обіцянка довідки «додає у вкладку, яка зараз відкрита» лишається чесною), і тулбар на цій вкладці не рендериться взагалі — разом із ним довелось розгалузити обидві точки, на які сідали `Tab` і `Shift+F6`. **Свідома розбіжність із чернеткою сценаріїв:** чернетка вимагала, щоб порожній журнал *відмовлявся* від фокуса; зроблено навпаки — приймає, бо зона, що мовчки відмовляє, це рівно той баг, який лікує фікс у `useCompositeList.restoreFocus` («wishlist Tab→status-bar»); гарантія «цикл `F6` не зупиняється» лишилась і перевірена окремо. Обмеження, назване в ADR навмисно: **у правила немає механічного сторожа** — у сусіда з тостами категорія є обов'язковим аргументом єдиної функції показу, а «подія має носія» аргументом функції не робиться, тож перевіряє людина на рев'ю. **NVDA-прогін проведено 2026-08-31, усі 9 сценаріїв пройдено, зауважень немає** |
| [unknown-format-falls-back-to-mp3](done/p1-unknown-format-falls-back-to-mp3.md) | 2026-08-31 | Єдиний `L` у 0.1.0 і найглибший запис теми «чесний інтерфейс»: `unwrap_or(AudioFormat::Mp3)` називав MP3-ом усе, чого не знав `Content-Type`, — станція в OGG діставала файл `.mp3` із не-MP3 усередині, ID3v2 поверх Ogg і назву «(MP3 128k)», запечену в профіль. Спадок — **політика, а не правка рядка** ([ADR](../decisions/2026-08-31-refuse-unknown-format-rather-than-guess.md)): докази перед вердиктом (`Content-Type`, далі магічні байти, **дефолту немає**), вердикт **трійковий** (впізнаний і вміємо / чужа сім'я / невпізнаний), і чуже **не записується взагалі** — бо записувач копіює байти, а межа треку це довільний зсув, який MP3 і ADTS витримують, а Ogg і FLAC ні. Тому найінтуїтивніший варіант — «зберігати як є з чесним розширенням» — замінив би брехню на сміття; він названий в ADR саме тому, що майбутній читач потягнеться до нього першим. Друга половина спадку — **межа відмови**: вона поправляє [ADR 2026-08-13](../decisions/2026-08-13-reconnect-attempt-semantics.md), бо той накривав випадок буквально («з'єднались, ефір не пішов → спроба витрачена») і давав протилежний висновок. Спроба — про **станцію, що підводить**; коли відмовляє сам Tapir, спроба не витрачається й перепідключення не планується. Третя — **одна `detect` на два викликачі**: `connect` знімає перші байти в `IcyConnection.prefix`, тож probe при додаванні й старт запису судять за тими самими доказами, і двох розбіжних вердиктів про один потік не існує; читачі запису й плеєра віддають префікс першим, щоб початок ефіру не пропав. Вердикт кешується в профілі (`unsupported_codec` поруч із `format`, пишуться разом) і живить трьох споживачів — видимий носій у рядку, фаст-фейл планувальника без з'єднання, коротке замикання Play; звідси й `ScheduleResultReason::UnsupportedCodec`, без якого двогодинне вікно давало б ~120 стартів щохвилини. Code-review спіймало **дві помилки моделювання**, обидві варті запам'ятовування: порожнє тіло або мовчання станції — це **не** вердикт про формат, а той самий випадок, що й обрив (інакше десятисекундне мовчання застрягало б у профілі міткою «невпізнаний»); і каталог станцій **не** сміє мітити незнайомі рядки — `StationResult.codec` вільне поле чужого API, де трапляються `AACP` і `MP3,AAC`, тож «усе незнайоме — чуже» відмовило б станції, яку Tapir пише, ламаючи обіцянку §7 «хибної відмови бути не може». Принагідно: у `docs/testing/test-streams.md` не було **жодного** OGG на 19 записів — додано два перевірені, з парами «та сама станція, що пишеться / що ні». Прийнято **без NVDA-прогону** (`a11y: false`) — нових зупинок фокуса й live-поверхонь немає; три ручні перевірки (носій у рядку з холодного старту, фокус на кнопці підтвердження після попередження, третій вид рядка імпорту) пройдено. Хвіст винесено окремим записом — [transport-skip-silent-failure](done/p1-transport-skip-silent-failure.md) |
| [bandwidth-limit-dead-setting](done/p1-bandwidth-limit-dead-setting.md) | 2026-08-31 | Перший запис теми «мертві налаштування прибрані»: `bandwidthLimitKbps` жив у `settings.json` і в документації, але жодного споживача в Rust не мав — `architecture.md` описував тротлінг у `StreamManager`, якого ніколи не існувало. Рішення виявилось **уже ухваленим ззовні**: підфазу 3I-4 відхилено 2026-06-15 (радіо-бітрейт 128–320 kbps і так малий, кейсу насичення каналу немає), а поле пережило відхилену фазу — запис лише виконав другу половину того рішення. Спадок — **дві звички, не код**: перша — перед грилінгом «реалізувати чи прибрати?» шукати, чи рішення вже не записане у фазах; друга — форма сторожа при дорелізному видаленні поля, успадкована від [profile-scoped-settings](done/p0-profile-scoped-settings.md): один тест перевіряє **обидві** половини сумісності — що старий `settings.json` із зайвим ключем завантажується (serde ігнорує невідомі поля) і що ключ не пишеться назад. Побічно виправлено опис самого запису: UI-редактора поле не мало ніколи, «рубильник» існував лише на диску — нагадування, що `touches:` у беклозі це оцінка, а не факт. |
| [tray-notifications-label-scope](done/p1-tray-notifications-label-scope.md) | 2026-08-17 | Прапорець «Сповіщення при зміні треку» вимикав і всі сповіщення планувальника — хто прибрав балаканину про треки на п'яти записах, тихо втрачав і «плановий запис пропущено». Спадок — **категорія як зобов'язання**: `ToastKind` + `is_enabled(kind, &UiSettings)` у [notify.rs](../../src-tauri/src/tray/notify.rs), де категорія є обов'язковим аргументом єдиної функції показу, тож п'ятий тост не скласти, не відповівши на питання гейта; раніше ця відповідь жила рівно в одному коментарі. Правило поділу — **наявність іншого сліду**, а не поверхня чи наслідок: зміна треку (слід у плеєрі) і плановий запис (слід у списку розкладу — `last_result` пишеться для всіх чотирьох подій, гілки без фіксації немає) дістають по власному прапорцю, зворотний зв'язок фонового хоткея не гейтиться ніколи. Grilling відкинув три спокуси, і кожна зафіксована в [ADR](../decisions/2026-08-17-tray-toast-categories.md): винести розклад з-під гейта «за прецедентом хоткеїв» (симетрія оманлива — у хоткея слід один, і це означало б будити рапортом «розпочато» о третій ночі, тобто ламати саме той сценарій, заради якого прапорець винесли в профіль); лишити провали поза гейтом (після чесної мітки це підпис, що бреше у другий бік); група `role="group"` із короткими мітками (підпис групи не оголошується, поки в неї не зайти — те саме, чим 2026-08-08 обґрунтував фізичну межу діалогів). Звідси й слово «в треї» в обох мітках: гейт накриває тост ОС, але **не** оголошення live region, тож коротка мітка обіцяла б тишу, якої не буде — `CONTEXT.md` §«Сповіщення в треї» розводить «сповіщення» й «оголошення» назавжди. Виняток із фільтра 2 ADR 2026-08-08 лишається **один** на два поля — врізку про це дописано в самому ADR, щоб наступний читач не порахував два й не вирішив, що спрацював поріг «2–3 винятки → переглянути фільтр». Міграції немає свідомо (до релізу): `UiSettings` без `deny_unknown_fields`, старе `trayNotifications` ігнорується, обидві категорії піднімаються ввімкненими — тест у `profile.rs` тримає цю обіцянку. Принагідно вичищено єдину латиницю в українському інтерфейсі: «Згортати до tray» → «до трею», разом із двома цитатами в довідці. Прийнято **без NVDA-прогону** (`a11y: false`) — нової структури не з'явилось, доступне ім'я обох прапорців під автоматичним гейтом `getByRole`; ручну перевірку Tab'ом по вкладці пройдено |
| [tray-layer-not-localized](done/p1-tray-layer-not-localized.md) | 2026-08-17 | Найбільший запис 0.1.0: нативний шар (меню значка, підказка, тости, діалог виходу й діалог невдалого старту) більше не зашитий українськими літералами — Rust читає ті самі `src/i18n/messages/*.json`, що й paraglide (`include_str!`, розбір раз на локаль, `src-tauri/src/i18n.rs`). Спадок — дві конвенції, винесені в [ADR](../decisions/2026-08-17-native-layer-localisation.md): **одна подія — один ключ** (планувальник, причини пропуску, «розпочато/зупинено» беруть ті самі ключі, що live region; `tray_*` лишається для міток, яких на іншій поверхні немає — межа проходить по природі рядка, не по словах) і **локаль поза `AppState`** (`t()` синхронна, бо синхронна `build_menu`; діалог старту мусить працювати тоді, коли `AppState` створити не вдалося). Ключі адресуються переліком у коді, `cargo test` вимагає кожен в обох мовах — описка не доживає до рантайму; `plural_streams` видалено, форму обирає `plural_suffix` за конвенцією фронтенду (нуль окремим `_zero`, а не мовним правилом). Попутно: `Прев'ю: X` у меню стало `Станція: X` (CONTEXT.md — «Прев'ю» термін домену, не мітка інтерфейсу), а grilling знайшов шосту поверхню поза `tray/` — діалог невдалого старту в `lib.rs`, який показується саме тоді, коли все інше вже не працює. Прийнято **без NVDA-прогону** (`a11y: false`); дві ручні перевірки — перемикання мови без перезапуску й перший старт із `--minimize` — пройдено. Кнопки `MessageBoxW` лишаються мовою Windows свідомо: `user32` малює їх зі своїх ресурсів |
| [quick-controls-overlay](done/p3-quick-controls-overlay.md) | 2026-08-17 | **Відхилено** (не реалізовано) — рішення розробника при розподілі по версіях. Спадок — межа, а не код: **другого вікна в застосунку не буде** ні як другий WebView2 (~50 МБ RAM поверх «portable single EXE»), ні як нативний Win32-popup із власним MSAA/UIA-деревом. Тому **фонова поверхня одна — трей**: меню значка плюс глобальні клавіші лишаються єдиним способом керувати Tapir, не піднімаючи вікно. Це підвищує ставку двох записів у 0.1.0: [tray-layer-not-localized](done/p1-tray-layer-not-localized.md) (єдина фонова поверхня зобов'язана бути двомовною — обхідного шляху більше немає) і [sound-hotkeys-feedback-announce-only](done/p1-sound-hotkeys-feedback-announce-only.md) (відгук на глобальні клавіші лишається balloon tips плюс видима поверхня у вікні). Сценарій «5–10 кроків, щоб змінити дію з іншого застосунку» приймається як ціна — дешевшає він лише розширенням набору глобальних клавіш. Усі чотири відкриті питання (клавіша, конфігурованість, друге Tauri-вікно проти Win32, toggle) вилучено з OPEN-QUESTIONS — секція P2 там спорожніла |
| [window-fullscreen-f11](done/p3-window-fullscreen-f11.md) | 2026-08-17 | **Відхилено** (не реалізовано) — потреби від користувача так і не було, а вона й була умовою запису. Спадок: **`F11` подавлена гардом назавжди**, і §5 «Прийняті рішення» у [webview-reload-guard](done/p2-webview-reload-guard.md) («подавити зараз, справжній fullscreen окремо») закривається **відмовою**, а не реалізацією — примітку там дописано, щоб «зараз» не читалося як «тимчасово». Формально клавіша лишається **вільною** (гард робить лише `preventDefault()`, у `SHORTCUTS` її немає), тож пул вільних F-клавіш її містить — але саме fullscreen на неї більше не планується. Потреба «більше площі для слабозорих» тепер має єдиний шлях — масштаб ([webview-zoom-hotkeys](p1-webview-zoom-hotkeys.md), P1 у 0.1.0) |
| [screen-reader-direct-speech](done/p3-screen-reader-direct-speech.md) | 2026-08-17 | **Відхилено** (не реалізовано) — і, що важливіше, **знято тригер**: запис стояв `blocked` з умовою «повернутись, якщо balloon tips виявляться недостатніми», тепер умови немає, `blocked_reason` вилучено. Спадок: озвучення йде **лише** через `aria-live` (`LiveAnnouncer`) плюс tray balloon tips — третьої поверхні мовлення не буде; **нативної DLL поруч із EXE не буде** (LGPL-комплаєнс Tolk + NVDA Controller Client і крос-SR тести поза проєктом), «portable single EXE» лишається інваріантом; Narrator лишається читачем першого класу, бо гібрид «SR з контролерним API → Tolk, решта → aria-live» відпадає разом із записом. Наслідок для черги: [sound-hotkeys-feedback-announce-only](done/p1-sound-hotkeys-feedback-announce-only.md) (P1, 0.1.0) втрачає резервний вихід «швидкий фідбек колись озвучимо напряму» — видима поверхня стає **єдиною** можливою відповіддю, і політика тостів, яку той запис ухвалює, є остаточною |
| [search-focus-hotkey](done/p2-search-focus-hotkey.md) | 2026-08-17 | `Ctrl+F` веде фокус у поле пошуку поточного екрана. Спадок — **опційний метод на контракті зони**: `ZoneEntry.focusSearch?()` ([useZoneNavigation.ts](../../src/hooks/useZoneNavigation.ts)) реалізують лише зони з текстовим пошуком, а клавіша бере **першу таку** з `orderedZonesRef`. Мапи «секція → зона» немає свідомо: той список уже містить лише зони поточної секції плюс постійні, тож розсинхронитись немає з чим, і майбутньому пошуку потоків ([streams-list-search](p2-streams-list-search.md), відкрите питання про клавішу там закрито) вистачить реалізувати метод — репліка на Потоках зникне сама. Чому не `focus` зони: він дорівнює `restoreFocus` ([useFocusBoundary.ts](../../src/hooks/useFocusBoundary.ts)), тобто **останній торканий елемент** — із сортування на пошук він не привів би. Фокус уже в полі → `select()` (спільний `lib/focusOrSelect.ts`, бо тіло було байт-у-байт однакове у двох компонентах). Клавіша **глобальна без `when`-гейта**: на чотирьох екранах без пошуку йде assertive-репліка «На цьому екрані немає пошуку» — тиша у відповідь на конвенцію, обіцяну скрізь, нерозрізненна з «застосунок завис». Другий шар — **гард акселераторів**: `KeyF` приєднано до `KeyR` у [webviewAccelerators.ts](../../src/lib/webviewAccelerators.ts), бо Tier-2 виходить першим рядком на `isInModal()`, тож із будь-якого відкритого діалогу клавіша долітала до WebView2 і відкривала Find bar. Цим **спростовано підставу**, з якою `Ctrl+F` виключили з переліку в [webview-reload-guard](done/p2-webview-reload-guard.md) §8 («споживає Tier-2») — примітку в реєстрі виправлено, а не просто прибрано. Шари навмисно різної ширини: реєстр вимагає `!shiftKey`, гард shift-агностичний, тож `Ctrl+Shift+F` має погашений дефолт і не має дії — `preventDefault` на комбінації без дефолту безкоштовний, а вгадувати shift-варіанти WebView2 — та сама дірка, від якої стоїть родина `F5`. Знахідка код-рев'ю, полагоджена там же: критерій «фокус лендиться з будь-якої зони секції» тримався на висновку з двох окремих half-тестів (синтетичні зони проти прямого виклику `focusSearch`) — додано наскрізний тест на обох панелях, де справжні зони склеєні зі справжнім слухачем, як у `App.tsx`. **NVDA-прогін проведено 2026-08-17, усі 8 сценаріїв пройдено, зауважень немає** |
| [zero-volume-reads-as-muted](done/p2-zero-volume-reads-as-muted.md) | 2026-08-17 | Друга знахідка grilling'у довідки, закрита кодом: тиша від зведеного в нуль рівня тепер той самий стан, що тиша від перемикача. Спадок — **предикат замість поля**: `muteControl.isSoundOff(muted, volume)` живить значок, підпис і `aria-pressed` кнопки плеєра та клаузу `f9_muted`; `$muteState.muted` сам по собі більше не означає «тиша», і кожен новий читач стану мусить брати предикат — ціна, яку [ADR](../decisions/2026-08-16-silence-is-mute-or-zero-volume.md) фіксує свідомо разом із відкинутим об'єднанням станів (`muted ⟺ volume === 0` поширило б автовідновлення на свідомо зведений рівень). Нових ключів i18n нуль: наявні `player_muted`/`player_unmuted`/`f9_muted` описували **стан**, а не спосіб входу. Пам'ять рівня живиться з подій `player-status` (`rememberVolumeLevel` з `App.handlePlayerStatus`), бо `Ctrl+Alt+Up/Down` обробляє Rust — webview жесту не бачить, лише наслідок; `FALLBACK_VOLUME` переїхав у `stores/player.ts` і звузився до дефолту `savedVolume`, тобто до холодного старту з нулем у профілі. `muteCleanup` не чіпано: обидві гілки й далі при `muted === true`, тож нульовий рівень свідомо переживає зупинку й зміну станції — різниця між **перемикачем** (тимчасове перекриття) і **рівнем** (налаштування). Знахідка код-рев'ю власного дифу, полагоджена там же: `toggleMute` читав рівень із `$playerStatus`, який оновлює лише подія бекенду, тож друге швидке натискання бачило стару нульову гучність і **піднімало** звук замість вимкнення — виправлено оптимістичним записом рівня, як у `PlaybackPosition` після seek. Уточнення семантики понад критерій: пам'ять — останній ненульовий рівень із подій, тож стрибок повзунком з `60%` повертає `60%`, а покроковий спуск `Ctrl+Alt+Down` — `5%`. `CONTEXT.md` дістав термін «Вимкнений звук». **NVDA-прогін проведено 2026-08-17, усі 8 сценаріїв пройдено, зауважень немає** |
| [help-muted-playback-symptom](done/p2-help-muted-playback-symptom.md) | 2026-08-16 | Чотирнадцята позиція `troubleshooting.md` — «Відтворення йде, а звуку немає», сьома в порядку, перша у трійці «що чути». Спадок ширший за позицію й лежить у двох правилах для майбутніх текстів. **Перше:** `LiveAnnouncer` — `sr-only`, тож `announce()` не має видимої поверхні взагалі; будь-яка порада «натисніть клавішу, і Tapir скаже» є текстом лише для частини читачів і мусить називати видиму поверхню поруч (тут — кнопку плеєра). Записано в мапу [help-content-polish](done/p1-help-content-polish.md); чинний рядок такого класу лишився в `player.md:33` і чекає на [sound-hotkeys-feedback-announce-only](done/p1-sound-hotkeys-feedback-announce-only.md). **Друге:** розділ дістав конвенцію передачі читача сусідній позиції — дозволена рівно там, де заголовок-симптом стоїть перед заголовками-причинами того ж симптому, як загальне правило не поширюється. Свідомо відхилено: `Ctrl+M` як перевірка (її відгук є сам звук, якого в цьому симптомі немає) і дослівна назва підпису кнопки (перемикається за станом, тож у «здорового» читача такої кнопки на екрані немає). Друга знахідка сесії — нульова гучність дає ту саму тишу, липкіше й непоміченою: [zero-volume-reads-as-muted](done/p2-zero-volume-reads-as-muted.md) + [ADR](../decisions/2026-08-16-silence-is-mute-or-zero-volume.md) |
| [hotkeys-expansion](done/p2-hotkeys-expansion.md) | 2026-08-14 | Друга хвиля аудиту шорткатів: `Ctrl+M` (mute) і `F9` («що зараз відтворюється») у Tier 2, `F4` (теги) і полагоджена `F2` (перейменування) на рядках Записів — усі три reserved проти KeyRecorder. Головний спадок — не клавіші, а **два власники формулювань**: [muteControl.ts](../../src/lib/muteControl.ts) (дія + guard «виклик у польоті» + текст стану; точка входу для відкладеного OS-`Ctrl+Shift+U` — мосту лишиться викликати `toggleMute()`) і `playbackAnnounce.ts`, куди переїхали `sourceName` (був у трьох місцях), `describePlayback` (union фактів, локалізує викликач) і `trackLabel`. Баг, який це вилікувало: кнопка mute озвучувала назву команди («Вимкнути звук») уже після вимкнення, тобто протилежне своєму новому лейблу. Знахідки код-рев'ю, полагоджені тут же: порожній `artist` з ICY давав «Станція —  — Трек» (`trackLabel`, його ж тепер кличе `windowTitle.ts`), а `F2` не перевіряла модифікаторів — тепер обидві клавіші пари відмовляються від матчу під будь-яким, тож `Alt+F4` лишається закриттям вікна. Свідоме відхилення: `Ctrl+M` при зупиненому плеєрі — тихий no-op (інакше `applyMuteCleanup` скасував би mute наступною ж подією, і анонс став би неправдою). Хвіст — `PlayerPanel.trackDisplay` і `StreamContextMenu` досі рендерять трек самі. **NVDA-прогін проведено 2026-08-14, усі 13 сценаріїв пройдено, зауважень немає** |
| [help-troubleshooting](done/p1-help-troubleshooting.md) | 2026-08-14 | Шостий і останній запис довідки: вкладка `troubleshooting` — **15 із 15, довідка повна**. Grilling перед реалізацією (11 питань) звірив мапу не лише з кодом, а й **з усіма 13 написаними розділами** — і саме друге дало головну знахідку: **сім із дванадцяти позицій мапи вже вичерпно пояснені у своїх розділах** (треки не діляться, місце на диску й аварійне завершення — `recording.md`; клавішу забрала інша програма — `background.md`; плановий не стартував — `scheduling.md`; файл не відкрився — `songs.md`; каталог недоступний — `browser.md`). Звідси жанр розділу: **«відповідь без механіки»** — причина + дія на місці (1–3 речення), механіка лишається у власника, бо переказ зламав би шов «один власник пояснення» з `help-config` і дав би два джерела правди на одну поведінку. Форма — **13 заголовків `###`, по одному на симптом**: читач приходить рівно з однією проблемою, а заголовок — єдина ціль переходу, яку виробляє конвеєр (посилань немає, таблиці заборонені, а конвенція 5 не дає виділити симптом усередині рядка списку); ціна — 13 `###` проти 3–6 деінде. Порядок — за стадією, на якій симптом помічають. **Дві позиції додано понад мапу, обидві з коду**: звук зник після від'єднання пристрою (пристрій запам'ятовується **за назвою**, фолбеку на системний немає — `engine.rs:946`, відтворення просто мовчить) і треків збереглося менше, ніж було в ефірі (типово відкидаються перший неповний і все коротше за 30 с — `recording.md` називає обидва фільтри з боку **фічі**, а не з боку симптому). Скрінрідери втратили власний заголовок: одне речення всередині ескалаційної позиції («не озвучується = вада Tapir, а не ваші налаштування»), бо єдині можливі рецепти є обходами дефектів і мусили б старіти разом із ними; критерій переформульовано з «рівно двічі» на «рівно два **місця**» — рядок сумісності називає трьох читачів в одному реченні. Захист від старіння: **тест меж обсягу** (120 слів на обидві локалі, 1000 — на `en`) замість задуманого чорного списку фраз-заглушок — нижня межа строго перекриває список і не конфліктує з обов'язковим «поки що недоступно» зі стиль-гайду, а рахунок ведеться в JS, бо `wc -w` тут повертає **0** на кирилиці; `AGENTS.md` отримав `docs/help/` як **окрему поверхню** (четверта аудиторія) з її заборонами й посиланням на спеку — гола вимога «оновлюй довідку» породила б саме таблиці й посилання; `_TEMPLATE.md` — **умовний** пункт критеріїв, бо безумовний перетворився б на ритуал. Попутно з `AGENTS.md` прибрано два неіснуючі NVDA-чеклісти. **Рев'ю після реалізації спіймало реальну помилку у власному тексті**: порада натиснути `F2` не працює саме в тій половині симптому, яку називає заголовок, — у потоку, що перепідключається (тобто **записується**), поле адреси замкнене; виправлено в обох локалях. Обсяг `en` — 846 слів, найбільший розділ довідки (попередній максимум 683): причина в змісті, 13 симптомів, скорочення можливе лише викиданням позицій. Дві знахідки заведено записами: [unknown-format-falls-back-to-mp3](done/p1-unknown-format-falls-back-to-mp3.md) (нерозпізнаний `Content-Type` мовчки стає `Mp3`, файл дістає чуже розширення) і [about-app-info](done/p1-about-app-info.md) (ні версії, ні адреси проєкту в інтерфейсі — а довідка веде користувача саме до звіту про проблему). **NVDA-прогін скасовано розробником** (`a11y: true` → `false` — тепер `false` в усіх шістьох записах довідки): запис додає один `Tab`, один `TabPanel`, два ключі i18n і два markdown-файли, жодного нового патерну взаємодії. Побічно звірено на майбутнє: «N з M» ніде не задано явно — `aria-posinset`/`aria-setsize` у react-aria `Tabs` немає, позицію рахує WebView2 |
| [help-config](done/p1-help-config.md) | 2026-08-13 | П'ятий із шести записів довідки: дві нові вкладки — `settings` (після `templates`) і `background` (перед `troubleshooting`, якого ще немає) — і три файли тексту (uk + en), плюс одне речення в `player.md`. Grilling перед реалізацією (16 питань) звірив мапу покриття парасольки з кодом і виправив/додав **30 позицій** (9 profiles, 8 settings, 13 background); склад обох діалогів налаштувань звірявся **з кодом**, не з `docs/keyboard-shortcuts.md` — той уже мав дрейф (перемикач системних медіа-кнопок стоїть в `AudioTab.tsx`, а не у вкладці «Гарячі клавіші»). Ключові рішення: `settings.md` перелічує **по секціях**, а не по контролях (близько двадцяти позицій уже пояснені в `recording.md`/`scheduling.md`/`player.md`, тут лише називаються); жирний — за першою згадкою **в межах свого розділу**, не всієї довідки; типові комбінації восьми глобальних клавіш називає лише `settings.md`, а `background.md` описує їх за дією; заборони в `profiles.md` вплетені в кроки, а не зведені в регламент; CLI поданий двома групами («діють лише при запуску» проти «діють і на вже запущеному»). Правило 6 стиль-гайду того ж дня стало однією стелею 200–1000 слів замість дворівневої системи — тому `settings.md` міг понести повний склад обох діалогів без розщеплення. **Ревʼю після реалізації знайшло чотири помилки, усі виправлено в тому ж коміті**: діалог перемикання профілю описувався як такий, що називає кількість активних записів — насправді кількість є лише в діалозі виходу з програми (`background.md`), звідти й протекла хибна деталь; жирним було процитовано **aria-label** кнопки бокової панелі («Управління профілями») замість видимого тексту («Профіль» + назва) — та сама помилка, від якої стоїть конвенція розмітки 5; одна мітка в `settings.md` стояла в родовому відмінку замість називного (єдине відхилення на всі чотирнадцять готових файлів); і локалі розійшлися змістом — uk казала «дві комбінації» (два конкретні шорткати) там, де en казала «два види» (категорії), хоча зарезервованих клавіш близько шістнадцяти. Чотири хвости заведено окремими P2-записами під час grilling ([tray-layer-not-localized](done/p1-tray-layer-not-localized.md) — P1, найбільший: фоновий шар видно лише тоді, коли вікна немає, і він увесь українською; [hotkey-registration-silent-at-startup](done/p1-hotkey-registration-silent-at-startup.md), [key-recorder-silent-rejection](done/p1-key-recorder-silent-rejection.md), [profile-delete-key-dead-end](done/p1-profile-delete-key-dead-end.md), [settings-labels-wording](done/p2-settings-labels-wording.md)). **NVDA-прогін не потрібен** (`a11y: false`) — один прогін на готовий діалог несе [help-troubleshooting](done/p1-help-troubleshooting.md), останній і завершальний запис довідки |
| [help-automation](done/p1-help-automation.md) | 2026-08-13 | Четвертий із шести записів довідки: заглушки `wishlist` і `scheduling` переписано (uk + en), нових вкладок не додано. Grilling перед реалізацією (20 питань) звірив мапу покриття з кодом і виправив 20 позицій (9 у wishlist, 11 у scheduling); числом була одна (стелі запасу `30`/`60`), решта — поведінка. Три знахідки перевертають зміст розділів: патерн **якірний** (звіряється весь рядок «Виконавець - Назва», `Tycho` не ловить `Tycho - Dive`); вішліст **нічого не фільтрує** — збіг лише повідомляє, трек пишеться як будь-який; планувальник живе **лише в активному профілі** й лише поки Tapir запущений. Порядок дії правил зведено з чотирьох кроків до двох: ігнор-лист **потоку** з інтерфейсу не заповнити взагалі, тож його прибрано з опису. `scheduling` отримав четвертий виняток у правило 6 стиль-гайду (стеля 600 слів замість 400) — єдиний екран, де застосунок діє без користувача, тож «чому не сталося» мусить стояти там, де планують. `CONTEXT.md`: новий термін «Вішліст і Ігнор-лист» за зразком «Прев'ю». **Ревʼю після реалізації** (дві паралельні осі) знайшло внутрішню суперечність у власному тексті: вступ `wishlist.md` обіцяв, що ігноровані треки «не осідають на диску», хоча розділ через три абзаци сам пояснював, що в суцільному файлі ефіру вони лишаються — виправлено на вужчу, правдиву обіцянку. Також дописано пропущені позиції мапи (одноразова дата в минулому відхиляється, пункт із видаленим потоком усе одно вмикається, масове видалення пунктів розкладу) і відновлено втрачені при попередній правці UK-мітки **Повторюваний**/**Одноразовий**. Два хвости в беклог, обидва про підпис, вужчий за поведінку: [wishlist-pattern-hint-anchoring](done/p1-wishlist-pattern-hint-anchoring.md) (підказка патерна мовчить про якірність) і [tray-notifications-label-scope](done/p1-tray-notifications-label-scope.md) (прапорець «Сповіщення при зміні треку» гейтить і сповіщення розкладу). **NVDA-прогін не потрібен** (`a11y: false`) — один прогін на готовий діалог несе [help-troubleshooting](done/p1-help-troubleshooting.md) |
| [help-listening](done/p1-help-listening.md) | 2026-08-13 | Третій із шести записів довідки: три нові вкладки — `browser` (після `streams`), `songs` (після `scheduling`), `player` (після `recording`) — і три файли тексту (uk + en). Grilling перед реалізацією (17 питань) звірив мапу покриття парасольки з кодом і виправив **15 позицій**; числами були лише три (крок гучності `5`%, поріг «попередній» `0` = вимкнено, крок перемотування `5` с), решта — поведінка. Найважливіші: панель виводу пристрій лише **показує** (вибір і оновлення списку — налаштування → Аудіо); «попередній»/«наступний» ходять по **відфільтрованому** списку відповідного екрана й ніколи не змішують ефір із записами; позначка «неповний трек» береться з суфікса `_incomplete` в імені файлу, тож перейменування її знімає; у популярних станцій довантаження немає взагалі. Двічі повторився один патерн — одиничний і масовий шлях поводяться по-різному (видалення записів: помилка проти тихого пропуску; додавання станцій: «вже додано» проти двох цифр). Значок «Запис» у плеєрі виявився таким, що означає **файл**, а не процес, — суперечить `CONTEXT.md`; лагодити мовчки в довідці заборонено конвенцією розмітки 5, тож заведено окремий запис [player-recording-badge-term](done/p2-player-recording-badge-term.md) (P2). Новий термін глосарію — **Прев'ю**: третій вид джерела звуку поряд з ефіром і файлом, тимчасовий, без сусідів і без запам'ятовування. **Ревʼю після реалізації спіймало дві реальні розбіжності з кодом**, обидві виправлено в тому ж коміті: «продовжити останнє» описувалось як дія лише над файлом, хоча `resume_last` має і гілку `ColdStart::PlayStream` — станція теж повертається в ефір (виправлено і в самій парасольці, бо звужене формулювання було моєю ж правкою з grilling-сесії); і невдале відкриття файлу в зовнішній програмі описувалось як тиша, хоча `SongsPanel.tsx` показує конкретний тост про причину. **NVDA-прогін не потрібен** (`a11y: false`) — один прогін на готовий діалог несе [help-troubleshooting](done/p1-help-troubleshooting.md) |
| [reconnect-zero-retries](done/p0-reconnect-zero-retries.md) | 2026-08-13 | Знахідка grilling `help-recording` (розбіжність «0» між `manager.rs`, підказкою й `StreamItem.tsx`) сама пройшла окрему grilling-сесію (12 питань) перед реалізацією й виросла з `S` до `M`: другий шар помилки — `attempt = 0` скидався на успішному `connect`, а не на першому аудіобайті, тож станція, що приймає з'єднання й одразу рве потік, ніколи не впиралась у стелю спроб. Ухвалено: `0` = «не перепідключатися» (не «необмежено» — цього стану в домені більше нема), дефолт 10 спроб, стеля 10000 клампиться на бекенді. Три копії правила «пробувати ще раз?» в `manager.rs` звелися в одну `plan_retry`. Рішення й відхилені альтернативи — в нового ADR [reconnect-attempt-semantics](../decisions/2026-08-13-reconnect-attempt-semantics.md); словник отримав розділ «Перепідключення і спроба». **Код-рев'ю після реалізації знайшло дві реальні прогалини**, обидві виправлено в тому ж коміті: кламп стелі 10000 був підключений лише до збереження налаштувань (`apply_settings_patch`), не до `Profile::load`/`save_imported` — профіль, збережений до появи стелі (або зібраний вручну), відновлював би нескінченність чорним ходом; і на шляху read-error `plan_retry` рахував повний план (з `compute_backoff_delay`) двічі на ту саму спробу — з нього виділено дешевий `would_retry` без backoff-обчислення. Третя знахідка рев'ю — свідомо не виправлена тут: `StreamItem` читає живі `maxRetries`, а бекенд працює зі знімком налаштувань на старті запису, тож під час активного перепідключення вони можуть розійтися; це вже відщеплений і розблокований [reconnect-max-in-status](p2-reconnect-max-in-status.md). Наявні профілі не мігруються (пре-реліз) — старий `maxRetries: 0` лишається, поки користувач не пересохранить налаштування вручну |
| [help-recording](done/p1-help-recording.md) | 2026-08-13 | Другий із шести записів довідки: нова вкладка `streams` і переписані заглушки `recording` (як працює запис) та `templates` (файли й імена). Вкладки переставлено у відносний порядок специфікації одним рухом (`overview, navigation, shortcuts, profiles, streams, wishlist, scheduling, recording, templates`) — новий тест `helpContent.test.ts` пильнує послідовність як підпослідовність повного 15-вкладкового порядку, тож рештні чотири записи вставляють свої вкладки на місце, не переставляючи чужі. Шов `streams.md`/`recording.md` проведено по об'єкту, не по екрану: перший володіє списком і масовими діями, другий — процесом одного потоку (питання «як записати цю станцію?» → recording, «як записати всі?» → streams). Grilling перед реалізацією (14 питань) знову звірив мапу покриття парасольки з кодом і знайшов **три розбіжності з поведінкою**, не лише з формулюванням: перепідключення вимкнене за замовчуванням (`max_retries: 0` = жодної спроби, а не «необмежено», як обіцяє підказка налаштувань); поріг вільного місця блокує **старт** запису, а не зупиняє активний; на екрані «Потоки» **немає** поля пошуку — лишились фільтр і сортування, ключі `streams_search_label`/`zone_streams_toolbar` виявились мертвими й видалені. Сесія породила три нові записи: [reconnect-zero-retries](done/p0-reconnect-zero-retries.md) (P0 — розбіжність семантики нуля в UI проти бекенду), [accessibility-doc-audit](p2-accessibility-doc-audit.md) (P2 — `accessibility.md` описує неіснуючий `SearchField` і суперечить сам собі), [streams-list-search](p2-streams-list-search.md) (P2, idea — чи потрібен пошук узагалі). Два рішення сесії виявились наскрізними й перенесені в саму парасольку, а не лишені в цьому записі: обсяг 200–400 слів міряється по **англійському** файлу (українська виходить коротшою на тому самому змісті); перехресні згадки словами дозволені лише на вкладку, що вже існує на момент злиття свого запису. Тест-заглушку `resolves the stub sections` замінено, а не перецілено на іншу заглушку: нова версія перевіряє непорожність HTML для кожної вкладки в обох локалях — закриває мовчазний фолбек `getHelpHtml` на `""`, і не вимагає правки в жодному з наступних записів. Попутно виправлено: `songs_incomplete_badge` (uk) — «незавершений» → «неповний» (термін уже закріплений у `CONTEXT.md` і в обох рядках налаштувань; мок власного тесту `SongItem.test.tsx` уже чекав саме «неповний»); `%n` додано до підказки `settings_template_help` в обох локалях — плейсхолдер живий у `sanitize.rs`, але відсутній у підказці. Новий термін у `CONTEXT.md` — «Суцільний файл ефіру» (не «файл потоку» дослівно за міткою інтерфейсу: трек за визначенням теж є файлом потоку, тож мітка два поняття не розрізняє). **Ревʼю після реалізації спіймало реальну помилку в написаному тексті**: `recording.md` обіцяв статуси **«Записується»**/**«Помилка»** в колонці рядка, а `StreamItem.tsx` під час запису показує тривалість і на помилці провалюється в «Очікування» — ці слова існують лише в accessible name, не у видимій колонці; переписано нейтрально щодо модальності. **NVDA-прогін не потрібен** (`a11y: false`) — один прогін на готовий діалог несе [help-troubleshooting](done/p1-help-troubleshooting.md) |
| [help-intro](done/p1-help-intro.md) | 2026-08-13 | Перший із шести записів довідки: `overview` переписано з переліку можливостей на онбординг (портативність → перший запуск → шість екранів → швидкий доступ), додано новий розділ `navigation` — 8 вкладок із 15. Головний урок **не в тексті, а в специфікації**: grilling перед реалізацією звірив мапу покриття з кодом і знайшов **вісім хибних тверджень** у парасольці, прийнятій за п'ять днів до того (`F5`/`Shift+F5` уже не інертні — їх забрав [streams-transfer-hotkeys](done/p2-streams-transfer-hotkeys.md); рядок стану має 3 сегменти, не 5; `Ctrl+K` шукає дії й потоки, а не станції та пісні; `F2`/`Ctrl+C` живуть лише на Потоках, бо `useCompositeList` синтезує їх усюди, але гілку має тільки `StreamList`; `Space` не наскрізний перемикач і в Браузері взагалі інертний; `Delete` у Браузері не діє; жоден із трьох модифікаторів `Enter` не наскрізний — `Ctrl+Enter` узагалі означає різне на Потоках і Записах). Усі вісім виправлено **в самій парасольці**, бо мапа там працює як приймальний чекліст — решті п'яти `help-*` починати зі звірки своєї ділянки з кодом, а не з писання прози. Наслідки для канону: модифікатори подано **правилом, а не таблицею** (діють лише з `Enter`, на сфокусований рядок, значення — в екранних розділах); зони **не перелічені** (їх близько двох десятків і склад залежить від екрана — названо лише три постійні); рядок про інертні `F3`/`F7`/`F11` знято зовсім, а пункт про відсутність контекстного меню поза списками лишено (клавішу меню тиснуть із наміром, тиша читається як поломка; чесне пояснення «акселератори WebView2» заборонене правилом 3 стиль-гайду). В `overview` прибрано обіцянку чистого реєстру — увімкнений автозапуск пише `HKCU\…\Run` ([autostart.rs](../../src-tauri/src/autostart.rs)), нюанс переїхав у карту `background.md`; мітку кнопки виправлено на дослівну «Додати приклади потоків»; рядок сумісності формулюється **«працює з»** NVDA/JAWS/Narrator і ніколи «перевірено з» — проганявся лише NVDA. `navigation` додано третім винятком у правило 6 (600 слів): він деферент для шести розділів, і втиснутий у 400 змусив би їх дублювати пояснення. Захист від старіння — два тести **статичним розбором тексту**, без рендеру: парність `uk`/`en` (розсинхрон тихий — `getHelpHtml` мовчки падає на український фолбек) і «вкладки мінус `shortcuts` = файли», де виняток зашитий іменованим літералом, щоб наступна генерована вкладка змусила усвідомлено правити тест. `CONTEXT.md` отримав секцію навігаційних термінів з en-еквівалентами (Зона/zone, Рядок/row, Виділення/selection, Палітра команд/command palette, Рядок стану/status bar) — для міток інтерфейсу захист є (жирний = рядок з `uk.json`), для прозових слів не було жодного. **Рев'ю після реалізації спіймало випадкове видалення**: нову секцію термінів вставили *замість* хвоста секції «Знімок», знищивши абзац про виняток `activeProfile` (поведінка жива в [profile_commands.rs](../../src-tauri/src/commands/profile_commands.rs)) — відновлено; урок: правка `CONTEXT.md` у кінець файлу легко з'їдає хвіст попередньої секції, дивитись diff, не результат. Попутно: `Alt+4` (Розклад) переведено ⬜→✅ у [keyboard-shortcuts.md](../keyboard-shortcuts.md) — реєстр застарів; дев'ять генерованих paraglide-модулів прибрано з індексу (тека в `.gitignore`, але їх колись force-додали). `HelpDialog.test.tsx` прив'язано до «Перший запуск», а не до заголовка вкладки: після перейменування мітка `Tab` і `h2` збіглися, і `getByText` знаходив два вузли. **NVDA-прогін не потрібен** (`a11y: false`) — один прогін на готовий діалог несе [help-troubleshooting](done/p1-help-troubleshooting.md); прийнято за візуальною перевіркою розробника |
| [streams-transfer-hotkeys](done/p2-streams-transfer-hotkeys.md) | 2026-08-09 | `F5` = «Копіювати в профіль…», `Shift+F5` = «Перемістити в профіль…» у списку потоків — клавіша з Total Commander, модель ні (ціль питає `StreamTransferDialog`, другої панелі немає). Виділення дзеркалить `Delete`: bulk за `size > 0`, інакше сфокусований рядок. Одиночний `Shift+F5` дістав той самий запобіжник, що вже стояв у меню рядка як `isDisabled` (`isRecordingLike` + `$playerStatus`) — без нього клавіатура обходила «не переносити активний потік»; bulk-маршрут запобіжника не має (бекенд сам пропускає активні). A9: `doTransfer` (move) отримав явну передачу фокуса на вцілілий рядок — той самий механізм, що вже був у `doBulkTransfer`; третя копія стала одним спільним `handOffFocusAfterRemoval`. Обидві клавіші — reserved у `SHORTCUTS` (F1-довідник + відмова KeyRecorder), `F3`/`F7`/`F11` лишились вільними. uk-лейбл — «Перемістити», не «Перенести» як писала спека: усі п'ять наявних рядків дії кажуть «Перемістити». Розблокувало [list-key-modifier-guards](p2-list-key-modifier-guards.md). **NVDA-прогін проведено 2026-08-09, усі 12 сценаріїв пройдено, зауважень немає** |
| [help-content-polish](done/p1-help-content-polish.md) | 2026-08-08 | запис перероблено з «дописати 5 заглушок» на **специфікацію** всієї вбудованої довідки й закрито як документ — роботу везуть шість дочірніх записів (`help-intro` … `help-troubleshooting`), кожен зі своїми вкладками й i18n-ключами, тож порожньої вкладки-заглушки в застосунку не існує ніколи. Головне рішення — **принцип універсального дизайну**: попередня редакція вимагала «текст для сліпого користувача», тепер навпаки — Tapir звичайний застосунок для всіх, дія описується від наміру з обома шляхами разом («кнопка **Додати потік** або `Ctrl+N`»), зворотний зв'язок нейтрально щодо модальності («Tapir повідомить, що…»), а слова «NVDA»/«скрінрідер» на всю довідку зустрічаються **рівно двічі** (рядок сумісності в `overview` і абзац у `troubleshooting`); розділ про клавіатуру зветься «Навігація і клавіатура», а не «Доступність». Структура — 15 вкладок замість 7: шість «екранних» названо **дослівно як мітки ActivityBar** і впорядковано як `src/lib/sections.ts`, решта — наскрізні (`navigation`, `player`, `templates`, `settings`, `background`, `troubleshooting`). Дві термінологічні розв'язки поїхали в `CONTEXT.md`: «Запис» (процес) проти «Записів» (файли й екран) — тому вкладка `recording` зветься «Як працює запис», інакше два нерозрізнювані на слух пункти стояли б поруч; і **«метадані ефіру»** як єдина користувацька заміна «ICY-метаданих». Розмітка обмежена свідомо: **без таблиць** (у `.help-content` немає правил для `table`) і **без посилань узагалі** — обробника кліків немає, `default-src 'self'` верхньорівневої навігації не стримує, тож клік по `http`-посиланню вивів би webview із застосунку без дороги назад. Наскрізні механіки — канон у `navigation.md` + блок «Клавіші на цьому екрані» на 3–6 пунктів у кожному екранному розділі (повторюємо те, що на екранах **різне**, а не всю модель виділення шість разів). Межа щодо DEVELOPERS.md зсунута в одному місці: **CLI-аргументи входять** у довідку підрозділом «Запуск з параметрами», бо сценарій «автозапуск у конкретному профілі, згорнутим» з UI недосяжний узагалі. Захист від старіння розкладено по дітях: тести парності локалей — у перший (розсинхрон тихий, `getHelpHtml` мовчки падає на український фолбек), анти-заглушковий тест і правило в `AGENTS.md` — в останній. `a11y: true` рівно один раз, на останньому записі: один NVDA-прогін на готовий діалог замість шести по одному патерну. Свідомо **без ADR** — рішення відкочується правкою тексту, домівка в нього вже є. Сесія `/grill-with-docs`. Попутно знайдено дрейф у `docs/keyboard-shortcuts.md`: перемикач системних медіа-кнопок стоїть в `AudioTab.tsx`, а не у вкладці «Гарячі клавіші» — звідси вимога до дітей звіряти склад налаштувань **з кодом**, не з документацією |
| [profile-scoped-settings](done/p0-profile-scoped-settings.md) | 2026-08-08 | п'ять полів переїхали з `GlobalSettings` у `Profile` (`disk_space_threshold_gb`, `auto_advance`, `resume_file_from` → `recording`/`player_session`; `sort_by`, `show_tray_notifications` → новий блок `profile.ui`), `ResumeFileFrom` — у `profile.rs`. Міграції немає свідомо: дорелізна версія, поля просто зникають із `settings.json`. Шов запису — **один**: `get_recording_settings`/`save_recording_settings`/`set_profile_autoplay` зникли, їх замінила пара `get_profile_settings(name)`/`update_profile_settings(name, patch)`, що працює й з **неактивним** профілем (активний → `commit_profile`, неактивний → read-patch-write через `Profile::load` + `save_detached`). Патч секційний, бо межа володіння проходить усередині `player_session`: `recording`/`ui` їдуть секціями цілком (у них немає бекендових полів), а `autoplay_on_startup`/`auto_advance`/`resume_file_from` — поокремо, інакше запис затер би `volume`, слід відтворення й `last_result`. Уся логіка — чиста `Profile::apply_settings_patch`; наскрізного FS-тесту немає свідомо (`portable::profiles_dir()` без точки підміни). Команда не створює неіснуючий профіль — це закриває реальне вікно «видалив профіль → відкладений патч дописав файл-зомбі». Область дії стала **фізичною**, а не підписаною: `Ctrl+,` — три вкладки, тільки глобальне; `Ctrl+Shift+,` — чотири вкладки, тільки профільне, з іменем профілю в заголовку. Матчинг по `e.code === "Comma"`, тож українська розкладка (кома під Shift) нічого не ламає, а `ctrlOrMeta` уже вимагав `!e.shiftKey` — наявний матчер правити не довелося. Діалог монтується на рівні `App` через стор (не в `ProfilesPanel`), інакше комбінація працювала б лише на екрані профілів. «Постобробка» — APG «disabled but focusable»: `aria-disabled` через ref (RAC `filterDOMProps` його не пропускає), **не** `isDisabled`, інакше react-aria прибрала б вкладку з навігації стрілками й користувач NVDA ніколи б про неї не дізнався. `$profileSettings` наповнюється на старті в `App.tsx`, а не при відкритті діалогу — з нього читають `StatusBar` і `stores/streams.ts`, тож інакше перший кадр смикався б. Гілка `feature/profile-scoped-settings`. **NVDA-прогін проведено 2026-08-08, прийнято з розбіжностями**: 11 сценаріїв із 13, чотири хвости названо при прийманні (profile-settings-silent-numeric-save, profile-sort-state-after-switch, profile-force-close-unreachable, nvda-checklist-profile-settings-fixes), але **записів за ними так і не заведено** — з'ясовано 2026-08-14 при звірці посилань; сценарії 9 і 10 не проганялись свідомо. Головний урок прогону — обіцянка примусового закриття діалогу недосяжна з UI: діалог модальний, а `$profileList` оновлюється лише з екрана профілів, тож стану, який описують історії 19–20, не існує |
| [settings-commit-seam](done/p1-settings-commit-seam.md) | 2026-08-07 | механізм коміту **узагальнено, а не скопійовано**: інваріант порядку живе тепер в одному `src-tauri/src/store.rs` (`Commit`, `Persist`, `Store<T>`, `Writer<T>`, `write_json_atomically`), а `profile_store.rs`/`settings_store.rs` — тонкі адаптери (ключ воріт + шлях + `save_detached`). `Persist::key` — ім'я профілю (файлів багато) проти константи для налаштувань (файл один). 5 сайтів `GlobalSettings`: три на `commit_settings` (`save_settings`, `set_output_device`, `switch_profile` крок 9), два на `save_detached` (створення дефолту в `load`, гасіння autostart у `lib.rs` до `AppState::new`). `GlobalSettings::save` видалено — після міграції не лишилося жодного `settings.write().await`. `save_settings` мала ту саму інверсію «диск перед пам'яттю», що й `save_recording_settings` — прибрано. Відкат у `switch_profile` **збережено** свідомо: `activeProfile` читається лише при старті, тож наступного коміту можна не дочекатися, а розбіжність відправила б застосунок у чужий профіль; реалізовано через `Commit::Skip` (диск уже старий — пишемо лише пам'ять). `SessionState`/`data/state.json` — третій агрегат тієї ж форми, свідомо не чіпали. **NVDA-прогін не потрібен** |
| [profile-commit-seam](done/p0-profile-commit-seam.md) | 2026-08-07 | новий `src-tauri/src/profile_store.rs` — єдине місце, що пише файл активного профілю; `AppState::commit_profile(\|p\| Commit::Save\|Skip)` замінив 31 копію «lock → clone → spawn_blocking(save)» у 12 файлах. `ProfileWriter` бере лише номер під профільним локом (синхронно), а біля запису відкидає знімок, старіший за вже записаний того самого профілю (ворота ключовані іменем — вікно `switch_profile` пише різні файли). `Profile::save` видалено (не приватизовано) — компілятор сам перелічив кожен пропущений сайт. fsync (`sync_all` до `rename`) на кожному записі. Дві зміни поведінки: `set_volume` більше не персистить (гучність — сесійне поле, пише `persist_session_snapshot`/`graceful_shutdown`, як позиція відтворення); `save_recording_settings` втратив інверсію «диск перед пам'яттю» (був єдиним таким сайтом). `GlobalSettings` (та сама форма, 2 сайти) свідомо поза обсягом — той самий шов, наступним кроком. Гілка `refactor/profile-commit-seam`, злито FF у develop. **NVDA-прогін не потрібен** — жодне оголошення не змінюється |
| [webview-reload-guard](done/p2-webview-reload-guard.md) | 2026-08-07 | Гард — два capture-слухачі на `window` ([useWebviewGuard.ts](../../src/hooks/useWebviewGuard.ts) над предикатами [webviewAccelerators.ts](../../src/lib/webviewAccelerators.ts)), викликається поруч із `useGlobalShortcuts()`. `F3`/`F5`/`F7`/`F11` матчаться **без розбору модифікаторів** (кожен варіант F5 у WebView2 — reload; перелічувати поіменно означало б лишити дірку на невгаданому `Ctrl+Shift+F5`); `KeyR` — єдиний виняток, де саме модифікатор робить клавішу акселератором (`(ctrl\|\|meta) && !alt`, бо AltGr рапортує ctrl+alt). Лише `preventDefault()`, **ніколи** `stopPropagation()`: гард гасить дефолт, не забирає клавішу — інакше майбутній `F5` = «Копіювати в профіль» ([streams-transfer-hotkeys](done/p2-streams-transfer-hotkeys.md)) не спрацював би, а KeyRecorder не записав би ці клавіші під OS-хоткей. Дві свідомі відмінності від Tier-2: `e.repeat` **не** відкидається (кожен повтор несе власний дефолт), і немає гейта на `isInModal` (reload з відкритого діалогу так само руйнівний). Контекстне меню — carve-out через наявний `isTextEntryTarget`, підйомом по `parentElement` від цілі події (не `document.activeElement`). Жодних режимних гілок — поведінка однакова в dev/vitest/прод. Devtools у debug відкриває Rust: `open_devtools()` під `cfg(debug_assertions)` **між** `show()` і `set_focus()`, щоб останнім фокус забрало головне вікно (інваріант «webview ініціалізується у OS-foreground»). `SHORTCUTS` не змінено — резервувати погашені клавіші не стали. Гілка `develop` напряму. **NVDA-прогін проведено 2026-08-07, усі 16 сценаріїв пройдено, зауважень немає**; сценарій-замір (Edge) підтвердив F3/F7 інертними (повернуто в пул вільних F-клавіш — [hotkeys-expansion](done/p2-hotkeys-expansion.md)), F11 зайнятим (fullscreen) |
| [full-edit-stream](done/p1-full-edit-stream.md) | 2026-08-07 | `update_stream(stream_id, name, url?, icy_name?, bitrate?, format?)` — форма аргументів дзеркалить `add_stream`, і саме `url` є перемикачем: `None` — чисте перейменування з недоторканими похідними полями, `Some` — `resolve_playlist_url` і перезапис `format`/`bitrate`/`icy_name` переданим **включно з `None`** (вони описують адресу, а не рядок: після переїзду старе «AAC 64k» — брехня, яку NVDA прочитав би як факт; перше ж підключення їх перезаповнить). Окрему `update_stream_url` відхилено — ім'я й адреса в одному сабміті інакше стали б двома IPC і двома `save()` з частковим збоєм між ними. Уся логіка — в чистій `build_edited_stream` над `&[StreamInfo]` за зразком `build_added_stream`. Потік, чиє ім'я дорівнює **старому** URL, лишається безіменним (icy_name з probe через `naming::disambiguate`, інакше новий URL) — інакше рівність `name == url`, за якою `icy_rename` впізнає неназваний потік, зламалась би назавжди. Порожня назва трактується так само (зберегти `""` означало б рядок, який NVDA читає як ніщо, і теку `%s` без імені); шлях чистого перейменування не чіпали — там і далі `name.trim()`. UI: поле URL першим в **обох** режимах, але `autoFocus` в edit-режимі лишився на імені (F2 — м'язова пам'ять «перейменувати»). Змінена адреса проходить probe + `checkStreamConflicts({url, name, excludeId})`, незмінена — жодної нової перевірки. Обидва попередження озвучуються **разом**: перевірка після сабміту стає недійсною, тож притримане попередження — це попередження, яке не почують. Під час запису поле — `readOnly` + `aria-disabled` з поясненням через `aria-describedby`, а **не** нативний `disabled`: той випадає з обходу по Tab, і NVDA не дійшов би ні до поля, ні до пояснення (домашній патерн `SelectionToolbar`/`ActivityBar`); критерій «поле URL disabled» читати як «недоступне для правки». Побічно: предикат «потік записується» винесено в `src/lib/streamState.ts::isRecordingLike` — чотири копії каскаду станів (`StreamsPanel`, `StreamItem`, `StreamContextMenu` + новий виклик) стали однією функцією; Rust-дзеркало — `move_blocked_by_state`. Auth і per-stream ignorelist свідомо винесено ([stream-auth](p3-stream-auth.md), [per-stream-ignorelist-ui](p1-per-stream-ignorelist-ui.md)). Гілка `feature/full-edit-stream`, TDD. **NVDA-прогін проведено 2026-08-07, усі 9 сценаріїв пройдено, зауважень немає** |
| [settings-sidebar-tabs](done/p2-settings-sidebar-tabs.md) | 2026-08-07 | `SettingsDialog` перейшов на `orientation="vertical"` за зразком `HelpDialog` — обидва діалоги тепер звучать однаково, і це був головний аргумент, сильніший за напрямок стрілок. Вертикальний режим RAC поблажливіший: `TabsKeyboardDelegate.getKeyLeftOf/getKeyRightOf` не мають orientation-guard, тож живі **всі чотири** стрілки, тоді як у horizontal ↑/↓ мертві (`getKeyAbove/Below` повертають `null`) — перевірено і в сорсах `@react-aria/tabs@3.11.1`, і негативним контролем: зі знятим `orientation` усі три нові тести падають, `{ArrowDown}` не рухає вибір узагалі. Це страховка проти того, що NVDA не озвучує `aria-orientation` (JAWS озвучує) — клавіші не змінюються, а додаються. Понад початковий обсяг: `TabList` дістав власну мітку `settings_sections_label` («Розділи налаштувань») замість повторного `settings_title` — у бічній панелі таблист став окремою зоною, і мітка, тотожна заголовку діалогу, змушувала NVDA двічі поспіль вимовити «Налаштування» (симетрично до `help_sections_label`). З'явився перший `SettingsDialog.test.tsx` — доти діалог покривали лише тести чотирьох вкладок, тобто гейт `pnpm test` про сам діалог не знав нічого. Сесія `/grill-with-docs`; свідомо без ADR і без `CONTEXT.md` (зміна відкочується одним revert, запис беклогу — достатня домівка), мертві ключі `settings_tab_reconnection`/`settings_tab_audio` не чіпали. Гілка `feature/settings-dialog`. **NVDA-прогін проведено 2026-08-07, усі 5 сценаріїв пройдено, зауважень немає** |
| [open-stream-with-default-app](done/p1-open-stream-with-default-app.md) | 2026-08-07 | шел не отримує сирий URL потоку — той резолвиться в `http(s)://…/live` ще на додаванні, і єдина асоціація для нього браузер; замість цього пишеться одноелементний `data/tmp/<назва>.m3u8` (`stream_playlist_path`, `to_m3u8`) і відкривається файл. `data/tmp/` створюється й чиститься в `ensure_data_dirs()` при старті (`portable::tmp_dir`/`clear_dir_contents`), а не після відкриття — видалення відразу гонилося б з холодним стартом плеєра. `shell_open`/`map_shell_error` винесено зі `songs_commands.rs` у спільний `commands/shell_open.rs` з новим кодом `write_failed`; фронтенд отримав окрему `streamOpenErrorMessage` (не переоформлення `shellOpenErrorMessage` — набори кодів різні). UI: пункт «Відкрити у медіаплеєрі» в `StreamContextMenu` (після record, перед edit) і `Alt+Enter` у `StreamList.onAction`, обидва на сфокусованому рядку, ігнорують виділення; успіх мовчазний, помилка — тост. `aria-keyshortcuts` на рядку свідомо не додано (рішення `b06a9c1`). Гілка `feature/open-stream-with-default-app`, TDD. **NVDA-прогін проведено 2026-08-07, усі 8 сценаріїв пройдено, зауважень немає**; код `no_assoc` вручну не перевірявся (немає машини без медіаплеєра) |
| [open-song-with-default-app](done/p1-open-song-with-default-app.md) | 2026-08-06 | команда `open_song_in_app` (`songs_commands.rs`) віддає файл шелу через `ShellExecuteW` з дієсловом `open` на blocking-потоці з парними `CoInitializeEx`/`CoUninitialize` (STA — вимога MSDN, потоки tokio апартаменту не мають); нова фіча `Win32_System_Com` у `Cargo.toml`. Код повернення (`HINSTANCE` ≤ 32) мапиться чистою `map_shell_error` у стабільні `not_found`/`no_assoc`/`generic`, які `src/lib/shellOpenError.ts` перетворює на локалізований toast — контракт свідомо винесено з Songs, бо той самий знадобиться [open-stream-with-default-app](done/p1-open-stream-with-default-app.md). UI: пункт «Відкрити у програмі» між `play` і `explorer`, діє на рядок під фокусом і **ігнорує виділення** (модель Провідника відкрила б N вікон з одного натискання). Клавіатура: `Alt+Enter` = зовнішня програма, `Ctrl+Enter` = Провідник (раніше шорткату не було зовсім), обидві — на фокусованому рядку, на відміну від `Delete`. Модифікатори діють лише на `Enter`, тож `Alt+Space` лишився чистим play. Побічно: `ActionModifiers` нарешті має поле `alt` (+ `CompositeRow.onActivate` прокидає `altKey`) — це закриває однойменний пункт у запису для потоків; `Alt+Enter` зареєстровано `reserved` у `shortcuts.ts`, інакше його можна було б перекрити глобальним хоткеєм. Гілка `feature/open-song-with-default-app`, TDD. **NVDA-прогін проведено 2026-08-06, усі 8 сценаріїв пройдено, зауважень немає** |
| [log-rotation](done/p1-log-rotation.md) | 2026-08-06 | стратегія ротації зафіксована як `RotationStrategy::KeepSome(1)` прямо в конфігурації плагіна (`lib.rs`) — актив + один датований архів, разом ≤ ~2× `max_file_size`; поле `log_rotation`/`logRotation` прибрано наскрізь (схема + `Default` у `settings.rs`, TS-тип у `tauri.ts`, 6 тест-фікстур, 5 місць у `data-models.md`) разом із функцією `rotation_strategy_for` і чекбоксом «Зберігати всю історію логів» (ключ `settings_log_keep_history`). Обидва положення прибраного перемикача були хибні: `KeepOne` **видаляв** попередній лог замість архівування (на `debug` — за хвилини), `KeepAll` повертав необмежений ріст, а сам чекбокс читався інвертовано щодо поля в `settings.json`. Міграції немає — serde ігнорує застарілий ключ, той випадає при наступному `save()`. Єдиний важіль у UI — макс. розмір (10 МБ, 1–100). Гілка `chore/log-rotation-keepsome`. **NVDA-прогін проведено 2026-08-06, усі 6 сценаріїв пройдено, зауважень немає** |
| [wishlist-stale-list-ref](done/p1-wishlist-stale-list-ref.md) | 2026-08-06 | `patternListCallbackRef` у `WishlistPanel.tsx` повертає cleanup (React 19) із guard `patternListRef.current === zone` — обидва `TabPanel` ділять один ref, а RAC тримає деселектнуту панель ще один коміт (`useExitAnimation`), тож порядок attach(нова)→detach(стара) затирав посилання на живий список: тулбарна «Видалити вибрані» мовчки no-op, а проксі-зона `wishlist-list` відхиляла фокус і F6 пропускав список. Два регресійні тести (тулбарний шлях + F6 через `ZoneHarness`) — наявний ignorelist-bulk свідомо ходить рядковим ✕ і цього не ловив. Гілка `fix/wishlist-stale-list-ref`, TDD. **NVDA-прогін проведено 2026-08-06, усі 7 сценаріїв пройдено, зауважень немає** |
| [stream-name-disambiguation](done/p0-stream-name-disambiguation.md) | 2026-08-06 | чистий модуль `src-tauri/src/naming.rs` — єдине місце, де вирішується ім'я потоку (`collision_key` = санітизоване ім'я без регістру, суфікс `(AAC 64k)`/`(AAC)`/`(2)`, присвоюється **один раз** при додаванні); викликається з `build_added_stream`, `plan_appended` (браузер), `plan_import`, `recording_task` (ICY). IPC `check_stream_conflicts` дає діалогу попередження про дубль URL і про колізію імені; `ProbeVerdict` тепер несе `icyName`/`bitrate`/`format` і вони зберігаються в потік — потік, доданий без імені, одразу дістає `icy_name` замість URL. `%s` більше ніколи не бере сире ICY-ім'я. Гілка `feature/stream-name-disambiguation`, TDD. **NVDA-прогін проведено 2026-08-06, усі 12 сценаріїв пройдено**; він виявив втрату фокуса після попередження (форма на час probe стає `disabled` → фокус падає на `<body>`) — виправлено в `90dc66e` явним фокусом на кнопку підтвердження |
| [command-palette-results-a11y](done/p1-command-palette-results-a11y.md) | 2026-07-23 | `palette_no_results` замість хардкоду; оголошення кількості результатів (у т.ч. «0») через глобальний `LiveAnnouncer`/`useAnnounce` (polite, дебаунс 300 мс) — без окремого регіону в модалці й без змін моделі пунктів. Гілка `feature/command-palette-results-a11y`, TDD. **NVDA-прогін оголошення не проведено** — рекомендовано перед релізом |
| [resume-last-playback](done/p1-resume-last-playback.md) | 2026-07-23 | `PlayerSession.autoplay_on_startup` (per-profile opt-in) + `AutoplayGuard` one-shot + `set_profile_autoplay` IPC + `ProfileSettingsDialog`; злито fast-forward у develop (`26b7f1e`), локально, не запушено. **NVDA-прогін нового діалогу та авто-старту не проведено** — рекомендовано перед релізом |
| [stream-name-trim](done/p0-stream-name-trim.md) | 2026-07-22 | `.trim()` у `add_stream` і `update_stream` (`stream_commands.rs`) — назва потоку більше не зберігається з провідними/завершальними пробілами |
| [streams-empty-focus-audit](done/p2-streams-empty-focus-audit.md) | 2026-07-20 | гіпотеза підтверджена + третій мертвий шлях (одиничне move-to-profile); імперативний `onEmpty()` у `handleConfirmDelete`/`doTransfer` (`StreamList.tsx`) за зразком `223fadb`; 3 регресійні тести «SINGLE-op empty transitions» у `StreamsPanel.test.tsx` |
| [wishlist-example-patterns](done/p2-wishlist-example-patterns.md) | 2026-07-19 | CTA «Додати приклад» у власній зоні `wishlist-empty` (`WishlistPanel.tsx`) — не в `PatternList`'s `emptyExtra` (той варіант виявився keyboard-unreachable, rev R1); фіксований масив `examplePatterns.ts`; ключі `wishlist_add_example`/`wishlist_examples_adding`/`wishlist_examples_added`/`wishlist_examples_failed`. Застосувало патерн `streams-ctrlk-empty-hint` |
| [streams-ctrlk-empty-hint](done/p2-streams-ctrlk-empty-hint.md) | 2026-07-19 | бейдж «Команди — Ctrl+K» у порожньому стані `StreamsPanel` (не Tab-стоп); константа `PALETTE_COMBO` читає комбінацію з `SHORTCUTS`, тож бейдж не розходиться з F1-довідкою; ключ `streams_empty_palette_hint`. Завершує ADR 2026-05-31 §6 (S3) — патерн для `wishlist-example-patterns` |
| [browser-add-probe](done/p2-browser-add-probe.md) | 2026-07-19 | подія `browser-station-probe-result` + `useBrowserProbeFeedback` (App-wide, озвучує лише невдачі/підсумок); `spawn_probe_added()` у `browser_commands.rs` — detached probe після збереження, `buffer_unordered(5)` |
| [add-stream-probe](done/p2-add-stream-probe.md) | 2026-07-19 | IPC `probe_stream(url) -> { ok, error }` + `probe_once()` у `stream_io_commands.rs` (спільний 5-с `SINGLE_PROBE_TIMEOUT`); sync-spinner + «Все одно додати» в `AddStreamDialog` |
| [volume-nan-validation](done/p2-volume-nan-validation.md) | 2026-07-19 | хелпер `sanitize_volume()` у `player/engine.rs` — єдина точка санітизації гучності на межах IPC (`set_volume`) і профілю (`PlayerEngine::new`); non-finite → `0.0` |
| [activity-bar-help-button](done/p1-activity-bar-help-button.md) | 2026-07-19 | кнопка Help у footer `ActivityBar.tsx` (над Settings, спільний атом `$helpOpen` з `F1`); roving-order бару тепер 8 елементів |
| [crash-recovery](done/p1-crash-recovery.md) | 2026-07-18 | `data/state.json` (`cleanShutdown` + `activeRecordings[{streamId, url?}]`), снапшот-писар (`Notify` + 30с interval + 500мс debounce, `crash_recovery.rs`), auto-resume в setup-хуку, deferred подія `crash-resume`, `useCrashResumeFeedback` (NVDA polite + toast) — `Profile.active_recording_urls` прибрано |
| [resume-file-from-setting](done/p2-resume-file-from-setting.md) | 2026-07-18 | `resume_file_from` у `GlobalSettings`, `plan_file_resume`/`emit_resuming` у `playback_control.rs`, NVDA-анонс позиції — нульового виносу немає, чисто адитивно |
| [playback-toggle-stop-pause](done/p1-playback-toggle-stop-pause.md) | 2026-07-18 | `PlayerSession.last_active` + оживлені `last_stream_id`/`last_file_position`, `playback_control.rs`, cold-start resume — база для `resume-last-playback` (вище) |
| [autostart](done/p2-autostart.md) | 2026-06-25 | winreg-автостарт (`autostart.rs`), CLI `--minimize`; сам **не** грає — авто-гра лише через `autoplay_on_startup` (`resume-last-playback`) |

---

## Перехресні рішення

1. ✅ **resume-last-playback vs playback-toggle-stop-pause** — _вирішено 2026-06-25 (A1), базу
   реалізовано 2026-07-18; політику фіналізовано 2026-07-19; **код реалізовано й злито
   2026-07-23** (`26b7f1e`, NVDA-прогін ще не проведено)._ `resume-last-playback` — надбудова над
   `PlayerSession`; **окремого `last_playback.json` немає.** Політика —
   `autoplay_on_startup: bool` (без `always_paused` — дублює cold-start `Ctrl+Shift+K`;
   без `restore`) — нове поле **в `PlayerSession`** → per-profile. Авто-гра — явний opt-in.
2. ✅ **state.json (crash-recovery, done) vs стан відтворення (resume-last-playback)** — _знято (A1)._ Стан
   відтворення живе в `PlayerSession` (профіль), crash — у `data/state.json`; два чітко
   різні сховища, звіряти нема чого.
3. ✅ **autostart ↔ відтворення** — _вирішено (A3 через A1), обидві сторони реалізовано й злито._
   Autostart сам **не** грає; авто-гра ⇔ активний профіль має `autoplay_on_startup = true`
   (поле вводить [resume-last-playback](done/p1-resume-last-playback.md)).
4. **mpv-playback-engine ↔ he-aac-mf-playback / hls-stream-support.** Якщо mpv проходить PoC — винести
   обидва записи в `done/` як зняті. _(A4 — відкрите.)_
